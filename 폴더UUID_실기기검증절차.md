# 폴더 UUID 전환 — 실기기 검증 절차

대상 커밋: `6e103b2` (브랜치 `feat/sync-v2-structure-recovery`)

**켜고 끄는 설정은 없다.** 동기화를 켠 작품이면 폴더 동기화도 함께 돈다.
동기화를 끄면 같이 꺼진다. 따로 만질 것이 없다.

각 항목은 **한다 → 보여야 한다 → 아니면 무슨 뜻이다** 세 줄이다. 결과를
적어 두면 다음에 고칠 때 쓴다.

---

## 준비물

- 아이패드 실기기 1대
- 맥 (Xcode 26.6)
- **아이패드 시뮬레이터 1대** — 실기기가 하나뿐이므로 "아이패드 ↔ 아이패드"를
  보려면 시뮬레이터를 두 번째 기기로 삼는다
- Windows PC (3부에서만)
- 기내 모드 (끊김 재현용)

---

## 준비 0 — 백업부터. 건너뛰지 말 것

동기화를 켠 작품을 처음 열면 **이관이 돈다.** 기존 폴더의 UUID가 바뀌고 서버에
폴더가 새로 등록된다. 되돌리기 어렵다.

1. **테스트 전용 작품을 새로 만든다.** 이름은 `폴더테스트`.
2. 진짜 원고가 든 작품으로 하고 싶다면, 파일 앱에서 작품 폴더를 통째로 복사해
   둔 다음에 한다.

> 중요한 원고가 든 작품으로는 하지 말 것. 아직 검증 전이다.

---

## 준비 1 — 설치

맥에서:

```bash
cd "/Users/chocos/Documents/작가님 힘내세요"
git log --oneline -1
```

`6e103b2` 가 나오는지 확인한다. 그다음 Xcode에서 `WriterPad.xcodeproj` 를 열고:

1. 상단 기기 목록에서 **연결한 아이패드**를 고른다
2. `WriterPad` 대상 → Signing & Capabilities → Team이 본인 계정인지 확인
3. ⌘R
4. 아이패드에서 설정 → 일반 → VPN 및 기기 관리 → 개발자 앱 신뢰

시뮬레이터에도 같은 방법으로 설치하고, **같은 계정으로 로그인**한다.

---

# 1부 — 아이패드 ↔ 아이패드

여기가 이번 작업이 고치려던 곳이다.

### 1-1. 이름 끝 공백

1. `폴더테스트` 작품을 연다
2. 메모장 안에 폴더를 만든다. 이름은 `가 나 다 ` (**끝에 공백 하나**)
3. 저장한다

**보여야 하는 것** — `가 나 다` 로 저장된다 (끝 공백 제거)

**아니면** — 이름 정리가 깨진 것

### 1-2. 금지 이름

1. 폴더 이름을 `CON` 으로 바꾼다

**보여야 하는 것** — 이전 이름으로 되돌아가고 **2초 팝업**이 뜬다

### 1-3. 첫 실행 이관

1. 앱을 완전히 종료했다 다시 연다
2. `폴더테스트` 를 연다. 동기화가 한 바퀴 돌 때까지 기다린다

**보여야 하는 것** — 폴더 구조가 **하나도 안 바뀐다.** 이름도 위치도 그대로

**폴더가 사라지거나 이름이 바뀌면** — 이관 오류. 즉시 중단하고 알릴 것

3. 앱을 껐다 다시 연다

**보여야 하는 것** — 역시 그대로. 이관은 작품당 한 번만 돈다

### 1-4. 빈 폴더 이름 변경 — **이번 작업의 핵심**

1. 아이패드에서 **빈** 폴더 `가 나 다` 를 만든다
2. 동기화를 기다린다 (저장 상태 표시가 멈출 때까지)
3. 시뮬레이터에서 작품을 열어 `가 나 다` 가 보이는지 확인한다
4. **아이패드에서** 이름을 `가 나 다 바` 로 바꾼다
5. 동기화를 기다린다
6. 시뮬레이터에서 작품을 닫았다 다시 연다

**보여야 하는 것** — 시뮬레이터에 **`가 나 다 바` 하나만.** `가 나 다` 는 없다

**둘 다 있으면** — 가장 중요한 실패다. 화면 사진을 남길 것

### 1-5. 반대 방향

1-4를 **시뮬레이터에서 바꾸고 아이패드에서 확인**하는 식으로 반복한다.

### 1-6. 문서가 든 폴더

1. 폴더 안에 원고를 만들고 몇 글자 쓴다
2. 폴더 이름을 바꾼다
3. 시뮬레이터에서 확인한다

**보여야 하는 것** — 폴더 하나, 원고 그대로, 열면 내용이 보인다

**원고가 사라지면** — 심각. 즉시 중단

### 1-7. 폴더 이동

1. 폴더 `A` 와 `B` 를 만든다
2. `A` 를 `B` 안으로 옮긴다
3. 시뮬레이터에서 확인한다

**보여야 하는 것** — `B/A` 하나. `A` 가 원래 자리에 남으면 안 된다

### 1-8. 폴더 통째로 삭제

1. 폴더 `상위` → 그 안에 `하위` → 그 안에 원고 파일 하나를 만든다
2. 동기화를 기다린다
3. `상위` 를 통째로 휴지통으로 옮긴다
4. 동기화 상태 표시를 지켜본다

**보여야 하는 것** — 오류 없이 끝난다. 시뮬레이터에서도 셋 다 사라진다

**`FOLDER_NOT_EMPTY` 오류가 뜨면** — 삭제 순서가 안 먹은 것

### 1-9. 복원

1. 폴더를 휴지통으로 옮긴다. 동기화를 기다린다
2. 휴지통에서 복원한다
3. 시뮬레이터에서 확인한다

**보여야 하는 것** — 원래 자리에 폴더 하나

### 1-10. 오프라인 → 재연결

1. 아이패드를 **기내 모드**로
2. 빈 폴더 이름을 바꾼다
3. 앱을 완전히 종료한다
4. 앱을 다시 연다 (아직 기내 모드)
5. 기내 모드를 끈다
6. 동기화를 기다린다
7. 시뮬레이터에서 확인한다

**보여야 하는 것** — 새 이름 폴더 하나

**둘이 되면** — 재시도가 같은 작업으로 안 나간 것

### 1-11. 목적지 충돌

1. 폴더 `가` 와 `나` 를 만든다. 동기화를 기다린다
2. 아이패드를 **기내 모드**로
3. 아이패드에서 `가` → `다`
4. 시뮬레이터에서 `나` → `다`. 동기화를 기다린다
5. 아이패드 기내 모드를 끈다

**보여야 하는 것** — 어느 쪽도 상대를 덮어쓰지 않는다. **자료가 사라지면 안
된다.** 한쪽이 이름이 안 바뀐 채 남는 것은 정상이다

---

# 2부 — Windows 양방향

**Windows는 아직 `folders` 표를 모르고 `tree_order` 만 쓴다.** 그래서 아이패드가
두 방식 사이의 다리 역할을 한다. 이 부는 그 다리가 실제로 도는지 보는 것이다.

### 2-1. 아이패드 → Windows

1. 아이패드에서 빈 폴더 이름을 바꾼다
2. 동기화를 기다린다
3. Windows에서 동기화한다

**보여야 하는 것** — Windows에서 새 이름 폴더 하나

**둘 남으면** — Windows 쪽 `tree_order` 처리 문제다. 이번 작업 범위 밖이지만
기록할 것

### 2-2. Windows → 아이패드 — **다리 확인**

1. **Windows에서** 빈 폴더 이름을 바꾼다
2. 아이패드에서 동기화한다

**보여야 하는 것** — 아이패드에 새 이름 폴더 **하나**

3. 아이패드에서 동기화를 **한 번 더** 한다

**보여야 하는 것** — 그대로 새 이름. **이름이 옛것으로 돌아가면 안 된다**

**돌아가면** — 다리가 안 도는 것. 가장 중요한 확인 항목이다

4. 시뮬레이터에서도 확인한다

**보여야 하는 것** — 시뮬레이터에도 새 이름 하나

### 2-3. Windows → 아이패드 → Windows 왕복

1. Windows에서 폴더 이름을 바꾼다
2. 아이패드에서 동기화한다
3. 아이패드에서 다시 그 폴더 이름을 바꾼다
4. Windows에서 동기화한다

**보여야 하는 것** — Windows에 마지막 이름 하나

### 2-4. Windows 원고 무사한지

1. Windows에서 원고를 몇 글자 고친다
2. 아이패드에서 동기화한다
3. 아이패드에서 원고를 고친다
4. Windows에서 동기화한다

**보여야 하는 것** — 양쪽 내용이 맞는다. 폴더 작업이 원고를 건드리면 안 된다

**원고가 깨지면** — 심각. 즉시 중단

---

## 결과 적는 법

```
1-1  이름 끝 공백       : 통과
1-4  빈 폴더 이름 변경  : 실패 — 시뮬레이터에 폴더 둘 남음 (사진 있음)
2-2  Windows → 아이패드 : 통과
```

실패 항목은 **화면 사진**이 있으면 훨씬 빠르다.

## 오류를 만났을 때 로그 뽑기

1. Xcode → Window → Devices and Simulators
2. 아이패드 선택 → Open Console
3. 검색창에 `writerpad`
4. 문제를 재현하고 그 시각 전후를 복사한다

`FOLDER_` 로 시작하는 오류 코드와 `SyncV2` 로 시작하는 줄이 중요하다.

## 절대 하지 말 것

- 중요한 원고가 든 작품으로 검증하기
- 1-11(충돌) 도중 앱 강제 종료
- 서버 테이블 직접 수정
