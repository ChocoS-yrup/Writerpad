// Additive project restriction; existing authentication and send locks remain on.
OTHER_SWIFT_FLAGS = $(inherited) -DWRITERPAD_RECEIVE_VALIDATION -DWRITERPAD_BODY_VALIDATION -DWRITERPAD_GENERAL_VALIDATION
