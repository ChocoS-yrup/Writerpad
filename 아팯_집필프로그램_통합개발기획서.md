iPadOS Swift 집필 프로그램 통합 개발 기획서
작성 기준일: 2026-07-14

============================================================
0. 문서 목적
============================================================

이 문서는 Windows용 "작가님 힘내세요" 프로그램의 집필 모드를 Swift 기반 iPad 앱으로
재구현하기 위한 통합 기획서다.

지금까지 확정한 다음 내용을 하나로 합친다.

- iPadOS를 주 사용 환경으로 하는 집필 앱
- 기존 Windows 작품 구조 및 UTF-8 TXT 호환
- 프로젝트와 바인더 관리
- 권·화 자동 생성 규칙
- 스마트 텍스트 입력 규칙
- 자동 저장, 백업, 복원, 휴지통
- 전체·부분 TXT 추출
- Windows와 iPad 사이의 무손실 Supabase 동기화
- 향후 Mac Catalyst 확장이 가능한 코드 구조
- 개인 아이패드 직접 설치와 운영

최우선 목표는 다음과 같다.

1. 사용자가 입력한 원고를 어떤 상황에서도 조용히 잃지 않는다.
2. 인터넷이 없어도 집필을 계속할 수 있다.
3. Windows와 iPad에서 같은 원고를 편집해도 오래된 문서가 최신 문서를 덮어쓰지 않는다.
4. iPad의 한글 조합 입력과 외장 키보드 입력이 자연스럽게 작동한다.
5. 기능을 역할별로 분리해 이후 Mac Catalyst로 확장하기 쉽게 만든다.


============================================================
1. 개발 범위
============================================================

[구현 대상]

- 작품 목록
- 집필 바인더
- 원고, 캐릭터, 설정집, 메모장, 플롯 등 문서 관리
- 좌우 듀얼 에디터
- 자동 저장과 저장 상태 표시
- 자동 백업과 복원
- 휴지통
- 현재 문서 검색과 전체 검색
- TXT 전체·부분 추출
- Supabase 동기화
- 오프라인 작업과 재시도
- Windows/iPad 충돌 예방·감지·병합
- 편집기 설정
- 외장 키보드 단축키


============================================================
2. 플랫폼 전략
============================================================

[1차 지원]

- iPadOS 전용
- 최소 지원 버전: iPadOS 17.6. iPadOS 17 계열 호환성을 유지한다.
- 주 개발·실기기 검증 환경: iPad Pro 11-inch (M5), iPadOS 26.x.
- 실제 iPad와 Magic Keyboard 또는 일반 Bluetooth 키보드 우선
- 가로 화면, 세로 화면, Split View, Stage Manager와 iPadOS 26 창 최소화·최대화·크기 변경 대응
- iPadOS 26 전용 API는 가용성 검사를 거치며, 최소 지원 버전의 동작 경로를 제거하지 않는다.

[향후 확장]

- iPhone 화면은 1차 개발에서 제외한다.
- macOS는 별도 네이티브 앱보다 Mac Catalyst를 우선 고려한다.
- 핵심 기능과 화면을 분리해 Catalyst 활성화 시 대부분의 코드를 재사용한다.
- 1차 개발 중에도 주요 단계마다 Catalyst 빌드 가능 여부만 확인한다.

[권장 Xcode 구성]

- 언어: Swift
- 화면: SwiftUI
- 편집기: UIKit UITextView + TextKit
- 메타데이터: SwiftData
- 원고 원본: UTF-8 TXT
- 네트워크: supabase-swift
- 동시성: async/await, actor
- 테스트: XCTest 또는 Swift Testing


============================================================
3. 핵심 설계 원칙
============================================================

1. Local First
   서버보다 로컬 TXT 저장을 먼저 완료한다.

2. 단일 원본
   원고 본문은 TXT 파일을 원본으로 사용한다. SwiftData에 같은 본문을 중복 저장하지 않는다.

3. 역할 분리
   화면, 파일 저장, 바인더 규칙, 백업, 검색, 동기화를 서로 다른 모듈로 분리한다.

4. 비동기 안전성
   파일 쓰기와 동기화 큐 처리는 actor로 직렬화한다.

5. 원자적 저장
   임시 파일 작성 후 교체하는 방식으로 중간 저장 실패 시 기존 원고를 보호한다.

6. 조용한 덮어쓰기 금지
   revision이 다른 원고는 서버에서 덮어쓰지 못하게 한다.

7. 경로와 문서 정체성 분리
   파일 이름이나 위치가 바뀌어도 document_id는 유지한다.

8. IME 존중
   한글 조합 중에는 에디터 본문을 강제로 다시 설정하거나 문서를 교체하지 않는다.


============================================================
4. 권장 소스 구조
============================================================

WriterPad/
├─ App/
│  ├─ WriterPadApp.swift
│  └─ AppEnvironment.swift
├─ Domain/
│  ├─ Models/
│  │  ├─ Project.swift
│  │  ├─ DocumentNode.swift
│  │  ├─ BackupSnapshot.swift
│  │  ├─ SyncOperation.swift
│  │  └─ SaveState.swift
│  ├─ Rules/
│  │  ├─ BinderRuleService.swift
│  │  └─ TextRuleEngine.swift
│  └─ Protocols/
├─ Data/
│  ├─ Local/
│  │  ├─ LocalDocumentStore.swift
│  │  └─ ProjectRepository.swift
│  ├─ Backup/
│  ├─ Trash/
│  └─ Migration/
├─ Sync/
│  ├─ SupabaseService.swift
│  ├─ SyncEngine.swift
│  ├─ SyncQueue.swift
│  ├─ EditLeaseService.swift
│  ├─ ThreeWayMergeService.swift
│  └─ ConflictResolver.swift
├─ Features/
│  ├─ Projects/
│  ├─ Binder/
│  ├─ Editor/
│  ├─ Backup/
│  ├─ Search/
│  ├─ Export/
│  └─ Settings/
├─ Platform/
│  ├─ SmartTextView.swift
│  ├─ iPadTextEditor.swift
│  └─ DocumentPicker.swift
├─ Resources/
└─ Tests/

한 파일은 가능하면 500줄을 넘기지 않는다.
View 안에서 파일이나 Supabase 테이블을 직접 수정하지 않는다.


============================================================
5. 로컬 작품 구조
============================================================

원고는 앱 샌드박스의 Application Support 아래에 저장한다.

Projects/
└─ 프로젝트명/
   └─ 집필모드/
      ├─ 메인/
      │  ├─ 원고/
      │  ├─ 캐릭터/
      │  ├─ 설정집/
      │  ├─ 메모장/
      │  ├─ 플롯/
      │  ├─ 흐름정리/
      │  ├─ 복선/
      │  └─ 장소/
      ├─ 휴지통/
      ├─ 백업/
      │  ├─ 자동저장/
      │  ├─ 복원전/
      │  └─ 충돌/
      └─ 설정.json

[파일 규칙]

- 모든 텍스트 문서는 .txt 확장자다.
- 인코딩은 UTF-8이다.
- 바인더 화면에서는 .txt 확장자를 숨긴다.
- Windows 프로그램과 가져오기·내보내기 형식을 호환한다.

[SwiftData 저장 대상]

- 프로젝트 UUID와 이름
- document_id
- 부모 document_id
- 상대 경로
- 문서 종류
- 사용자 지정 순서
- 수정 시각
- 내용 SHA-256 해시
- 마지막 서버 revision
- 삭제 상태
- 마지막 열린 문서
- 좌우 에디터의 커서 위치
- 펼쳐진 폴더
- 영구 동기화 재시도 작업


============================================================
6. 작품 관리
============================================================

[기능]

- 새 작품 생성
- 작품 목록
- 마지막 작품 자동 열기
- 작품 이름 변경
- 작품 순서 변경
- 작품 삭제 전 확인
- 기존 Windows 작품 폴더 가져오기
- 작품 전체 내보내기

[가져오기 원칙]

- 원본 폴더는 수정하지 않는다.
- 전체 검사 후 가져오기를 시작한다.
- UTF-8이 아닌 문서는 오류 목록에 표시한다.
- 중복 프로젝트, 중복 화수, 잘못된 권·화 이름을 보고한다.
- 가져오기 실패 시 일부 데이터만 남기지 않는다.


============================================================
7. iPad 화면 구성
============================================================

[가로 화면]

┌────────────────────────────────────────────────────────────┐
│ 작품 │ 검색 │ 좌우분할 │ 저장·동기화 상태                 │
├────────────────┬───────────────────────────────────────────┤
│                │                                           │
│     바인더     │                편집기                     │
│                │                                           │
└────────────────┴───────────────────────────────────────────┘

[세로 화면]

- 편집기를 최대한 넓게 표시한다.
- 바인더는 사이드바 버튼으로 열고 닫는다.
- 세로에서는 좌우 듀얼 에디터를 표시하지 않고 좌우분할 버튼도 비활성화한다.
- 이전 가로 화면에서 사용자가 켠 좌우분할 선호와 양쪽 문서·커서 상태는 보존하며, 가로·충분한 폭으로 돌아오면 자동 복원한다.
- 창 크기 변경은 화면 방향 변경으로 취급하지 않는다. iPadOS 26 창 최소화·최대화 중에도 세로 화면에서 듀얼 에디터가 켜지거나 버튼 상태가 반복 전환되어서는 안 된다.

[상태 표시]

- 기존 Windows 프로그램처럼 큰 하단 상태바를 사용하지 않는다.
- 상단의 좌우분할 버튼 옆에 작은 상태 배지를 둔다.
- 배지를 누르면 세부 상태와 재시도 버튼을 표시한다.

[SwiftUI 방식]

- NavigationSplitView: 바인더와 편집기
- contextMenu: 우클릭·길게 누르기 메뉴
- sheet: 추출, 백업 이력, 충돌 해결
- fileImporter/fileExporter: 파일 앱 연동
- ShareLink 또는 공유 시트: 추출 파일 공유


============================================================
8. 바인더 최상위 항목
============================================================

1. 원고
2. 캐릭터
3. 설정집
4. 메모장
5. 메인 스토리 틀
6. 흐름 정리
7. 복선
8. 장소
9. 휴지통

[공통 기능]

- 폴더 펼치기·접기
- 파일과 폴더 생성
- 이름 변경
- 사용자 지정 순서
- 드래그 앤 드롭
- 휴지통 이동
- 우클릭 또는 길게 누르기 메뉴
- 빈 문서와 작성된 문서 아이콘 구분
- 마지막 펼침 상태 복원

[원고 규칙]

- 원고 바로 아래에는 N권 폴더만 생성할 수 있다.
- 권 안에는 N화 TXT만 생성할 수 있다.
- 권 이름은 변경할 수 없다.
- 화 이름의 숫자+화 접두사는 변경할 수 없다.
- 같은 화 번호를 중복 생성할 수 없다.
- 원고 안의 파일을 다른 최상위 카테고리로 이동할 수 없다.
- 외부 문서를 원고 안으로 이동할 수 없다.
- 원고 최상위 항목은 항상 첫 번째다.
- 원고 내부는 화 번호 기준 자연 정렬을 기본으로 한다.

이 규칙은 BinderView가 아니라 BinderRuleService에서 검사한다.


============================================================
9. 원고 탭 컨텍스트 메뉴
============================================================

원고 최상위 항목을 우클릭하거나 iPad에서 길게 누르면 다음 두 메뉴를 표시한다.

1. 새 권 추가
2. TXT 추출

Mac Catalyst에서는 우클릭, iPad 터치 환경에서는 길게 누르기로 같은 메뉴가 열린다.


============================================================
10. 새 권 자동 추가
============================================================

[생성 규칙]

- 1권: 001화.txt ~ 025화.txt
- 2권: 026화.txt ~ 050화.txt
- 3권: 051화.txt ~ 075화.txt
- 4권: 076화.txt ~ 100화.txt

[계산]

- 새 권 번호 = 현재 존재하는 가장 높은 권 번호 + 1
- 시작 화 = (새 권 번호 - 1) × 25 + 1
- 종료 화 = 새 권 번호 × 25
- 파일명 = 세 자리 숫자 + "화.txt"

[동작 순서]

1. 원고 폴더에서 N권 형식의 폴더를 검사한다.
2. 가장 높은 권 번호를 찾는다.
3. 임시 생성 폴더를 만든다.
4. 빈 UTF-8 TXT 파일 25개를 임시 폴더에 생성한다.
5. 모두 성공하면 임시 폴더를 정식 N권 폴더로 변경한다.
6. 바인더를 새로고침한다.
7. 새 권을 자동으로 펼친다.
8. 새 권의 첫 번째 화를 자동으로 연다.
9. 생성 작업을 Supabase 동기화 큐에 등록한다.

[안전장치]

- 기존 권이나 화를 덮어쓰지 않는다.
- 파일 하나라도 실패하면 임시 폴더 전체를 제거한다.
- 일부 화만 들어 있는 불완전한 권을 남기지 않는다.
- 생성 중에는 새 권 추가 메뉴를 잠시 비활성화한다.
- 로컬 생성이 끝난 다음 서버에 일괄 반영한다.


============================================================
11. 텍스트 편집기
============================================================

[기본 기능]

- UTF-8 일반 텍스트 편집
- 실시간 글자 수
- 실행 취소·다시 실행
- 복사·잘라내기·붙여넣기
- 선택 영역 처리
- 커서 위치 기억
- 문서 전환 후 자동 포커스
- 빈 문서 안내 문구
- 현재 문서 검색과 결과 강조
- 타자기 스크롤
- 글꼴·크기·줄 간격·여백 설정
- 좌우 듀얼 에디터
- 활성 에디터 테두리 표시
- 외장 키보드 단축키

[구현 방식]

- SwiftUI TextEditor만 사용하지 않는다.
- UIViewRepresentable로 UITextView를 감싼 iPadTextEditor를 만든다.
- SmartTextView는 UITextView를 상속해 삭제와 키 명령을 처리한다.
- 검색 하이라이트, 선택 범위, 스크롤, IME 상태는 TextKit을 이용한다.
- 자동 저장과 텍스트 규칙은 에디터 클래스에서 분리한다.

[서식 범위]

원고가 TXT이므로 부분별 굵게, 색상, 밑줄은 저장 대상에서 제외한다.
글꼴·크기·줄 간격·여백은 화면 설정으로만 저장한다.


============================================================
12. 한글 조합 입력 안전장치
============================================================

- iPad의 키보드 언어를 강제로 변경하지 않는다.
- markedTextRange가 존재하면 아직 한글 조합 중으로 판단한다.
- 한글 조합 중에는 편집기 전체 본문을 다시 설정하지 않는다.
- 조합 중 문서 전환을 요청하면 조합 완료 후 전환한다.
- 문서 전환마다 UITextView 인스턴스를 새로 만들지 않는다.
- SwiftUI의 id를 화 전환마다 변경하지 않는다.
- 조합 중에도 로컬 저장은 가능하지만 화면 본문을 재주입하지 않는다.
- 소프트웨어 키보드와 외장 키보드에서 모두 테스트한다.
- Windows에서 사용한 IME 강제 전환·키 재전송 방식은 iPad에 이식하지 않는다.


============================================================
13. 스마트 텍스트 입력 규칙
============================================================

규칙은 순수 Swift 구조체 TextRuleEngine에서 처리하고 자동 테스트한다.

[13-1. 여는 기호 자동 쌍 생성]

' 입력  -> '' 생성, 커서는 가운데
" 입력  -> "" 생성, 커서는 가운데
[ 입력  -> [] 생성, 커서는 가운데
( 입력  -> () 생성, 커서는 가운데
{ 입력  -> {} 생성, 커서는 가운데

[13-2. 선택 영역 감싸기]

텍스트를 선택한 상태에서 여는 기호를 입력하면 다음과 같이 감싼다.

테스트 선택 + [ 입력 -> [테스트]

[13-3. 닫는 기호 건너뛰기]

커서 바로 뒤에 같은 닫는 기호가 있으면 중복 입력하지 않고 기존 기호를 넘어간다.

적용 기호: '  "  ]  )  }  」  』

[13-4. 빈 쌍 삭제]

커서가 빈 쌍의 가운데 있을 때 Backspace를 누르면 양쪽을 함께 삭제한다.

적용 쌍: ''  ""  []  ()  {}  「」  『』

[13-5. 닫는 기호 앞에서 Enter]

커서 바로 뒤에 닫는 기호가 있으면 닫는 기호를 넘어간 다음 줄을 바꾼다.

적용 기호: '  "  ]  )  }  」  』

[13-6. 말줄임표]

... 입력 -> … 한 글자로 교체

[13-7. 특수 인용부호]

ㄴㄴ 입력 -> 「」, 커서는 가운데
ㄱㄱ 입력 -> 『』, 커서는 가운데

이 두 규칙은 한글 조합이 완전히 끝난 뒤 실행한다.

[13-8. 장면 전환선]

현재 Windows 프로그램과 동일하게 * 한 번을 입력하면 다음 전체 문자열을 삽입한다.

\n\n * * *\n\n

화면상 결과는 앞뒤에 빈 줄이 있는 " * * * " 장면 전환선이다.

- 전환선 바로 뒤에서 Backspace를 누르면 전체 삭제한다.
- 전환선 바로 앞에서 Delete를 누르면 전체 삭제한다.
- 삽입과 삭제는 각각 한 번의 Undo 작업으로 처리한다.

[13-9. 설정]

- 스마트 기호 입력: 기본 켜짐
- 말줄임표 자동 변환: 기본 켜짐
- 특수 인용부호 단축 입력: 기본 켜짐
- 별표 장면 전환선: 기본 켜짐

[13-10. 구현 구분]

입력 전에 처리:
- 자동 쌍 생성
- 선택 영역 감싸기
- 닫는 기호 건너뛰기
- Enter 처리
- 빈 쌍 Backspace
- 장면 전환선 삽입

조합 완료 후 처리:
- ... -> …
- ㄴㄴ -> 「」
- ㄱㄱ -> 『』


============================================================
14. 좌우 듀얼 에디터
============================================================

- 기본은 단일 에디터다.
- 좌우분할 버튼으로 두 문서를 나란히 연다.
- 같은 문서를 양쪽에서 동시에 편집하지 못하게 한다.
- 마지막 왼쪽 문서, 오른쪽 문서, 활성 에디터를 복원한다.
- 활성 에디터에만 강조 테두리를 표시한다.
- 분할 선호와 실제 표시 상태를 분리한다. 세로·좁은 폭에서는 단일 편집기만 표시하되 분할 선호와 양쪽 작업 상태를 보존하고, 가로·충분한 폭에서 자동 복원한다.
- 세로에서는 좌우분할 버튼을 비활성화하고, iPadOS 26 창 최소화·최대화·크기 변경 중에도 화면 방향 판정을 안정적으로 유지한다.
- 분할을 닫기 전에 해당 에디터를 즉시 저장한다.


============================================================
15. 자동 저장
============================================================

[저장 시점]

- 마지막 입력 후 약 800ms
- 다른 화로 이동하기 전
- 프로젝트를 변경하기 전
- 좌우분할을 닫기 전
- 앱이 inactive 또는 background 상태로 이동하기 전
- 사용자가 Command+S를 누를 때

[저장 순서]

1. 현재 텍스트 스냅샷 생성
2. 로컬 TXT를 원자적으로 저장
3. 로컬 메타데이터와 해시 갱신
4. 필요 시 자동 백업 생성
5. 영구 동기화 작업 큐 등록
6. 서버 업로드 시도

[동시성]

- LocalDocumentStore를 actor로 구현한다.
- 같은 문서의 저장 순서를 직렬화한다.
- 오래된 비동기 저장 결과가 최신 입력을 덮어쓰지 않게 save generation을 비교한다.


============================================================
16. 저장·동기화 상태 표시
============================================================

상단의 작은 상태 배지에 다음 상태를 표시한다.

- 편집 중
- 로컬 저장 중
- 로컬 저장됨
- 서버 동기화 중
- 서버 저장됨
- 오프라인 저장됨
- 동기화 대기 N건
- Windows에서 편집 중
- 자동 병합 완료
- 충돌 확인 필요
- 동기화 실패·재시도

"저장됨" 하나로 로컬 저장과 서버 저장을 혼동시키지 않는다.

예시:

- 로컬 저장됨 · 서버 동기화 중
- 오프라인 · 서버 대기 3건
- 로컬·서버 저장 완료


============================================================
17. 백업과 복원
============================================================

[백업 생성 시점]

- 일정 시간 이상 편집했을 때
- 다른 화로 이동할 때
- 문서를 닫을 때
- 서버 충돌이 발생했을 때
- 과거 버전을 복원하기 전
- 구조 변경이나 대량 동기화 전

[기능]

- 자동저장 이력 목록
- 날짜와 시각 표시
- 선택한 시점 미리보기
- 현재본과 차이 비교
- 복원 전 현재본 자동 백업
- 선택 시점 복원
- 백업 삭제
- 보관 지정

[권장 보존 정책]

- 최근 24시간: 최대 30개
- 최근 30일: 하루 1개
- 보관 지정 백업: 자동 삭제하지 않음

[복원 순서]

1. 현재 문서를 로컬 저장
2. 현재본을 백업/복원전에 저장
3. 선택한 백업의 UTF-8 및 경로 검증
4. 원고 교체
5. 해시와 revision 상태 갱신
6. 서버 동기화 큐 등록


============================================================
18. 휴지통
============================================================

- 삭제는 즉시 영구 삭제하지 않는다.
- 휴지통으로 이동하면서 원래 상대 경로를 기록한다.
- 원래 위치 복원과 선택 위치 복원을 제공한다.
- 이름 충돌 시 덮어쓰지 않고 새 이름을 제안한다.
- 개별 영구 삭제와 휴지통 비우기를 제공한다.
- 서버에서는 deleted_at tombstone으로 삭제 상태를 전파한다.
- 다른 기기에서 수정 중인 문서가 삭제된 경우 충돌 화면을 제공한다.


============================================================
19. 검색
============================================================

[현재 문서 검색]

- 다음 결과
- 이전 결과
- 결과 수
- 현재 결과 강조
- 검색어 변경 시 즉시 갱신

[프로젝트 전체 검색]

- 모든 TXT 검색
- 파일별 결과 그룹
- 결과 주변 문장 표시
- 결과 선택 시 해당 문서와 위치로 이동
- 검색 중 취소 가능
- 메인 스레드를 막지 않는 비동기 검색

[성능]

- 초기에는 파일을 직접 비동기 검색한다.
- 1,000화 테스트에서 병목이 확인될 때만 검색 색인을 추가한다.


============================================================
20. TXT 추출
============================================================

원고 최상위 항목의 "TXT 추출" 메뉴를 누르면 설정 시트를 연다.

[추출 종류]

1. 전체 원고
2. 시작 화·종료 화 범위 지정

[옵션]

- 빈 화 제외
- 화 제목 포함
- 공백 제외 300자 미만인 화부터 제외
- 저장 위치 선택

300자 미만 중단 옵션은 전체 추출에서만 기본 켜짐으로 하되 사용자가 끌 수 있다.

[정렬]

- 권 폴더와 관계없이 화 번호를 추출해 숫자 순으로 정렬한다.

[본문 형식]

[001화]

001화 내용

[002화]

002화 내용

[파일명]

- 작품명(1~250화).txt
- 작품명(50~100화).txt

[완료 결과]

- 지정 범위
- 실제 포함 화수
- 존재하지 않는 화
- 빈 문서로 제외된 화
- 저장한 위치

[iPad 구현]

- FileDocument 또는 Transferable로 UTF-8 TXT를 만든다.
- fileExporter로 파일 앱의 저장 위치를 선택한다.
- 공유 시트로 다른 앱에 전달할 수 있다.
- 1차 버전은 TXT만 구현하고 PDF는 제외한다.


============================================================
21. Windows·iPad 무손실 동기화 목표
============================================================

두 기기가 동시에 오프라인에서 같은 문장을 수정하면 충돌 자체를 완전히 없앨 수는 없다.
대신 다음을 보장한다.

1. 다른 기기의 편집을 미리 알린다.
2. 오래된 revision이 새 revision을 덮어쓰지 못하게 한다.
3. 서로 다른 부분의 수정은 자동 병합한다.
4. 같은 부분의 수정은 양쪽을 모두 보존하고 사용자에게 선택하게 한다.
5. 앱 종료 후에도 실패한 동기화 작업을 유지한다.
6. 서버 응답 유실과 중복 요청에도 버전을 한 번만 생성한다.

[사용하지 않을 방식]

- 마지막 저장 우선 덮어쓰기
- 기기 updated_at만 비교
- 메모리 전용 재시도 큐
- 영구 강제 락
- 파일 경로만으로 문서 식별


============================================================
22. 공통 문서 ID와 기기 ID
============================================================

[document_id]

- 모든 문서에 UUID를 부여한다.
- 이름 변경, 이동, 휴지통 이동 후에도 유지한다.
- relative_path는 화면과 파일 위치이며 문서의 정체성이 아니다.

[device_id]

- Windows와 iPad 각각 고유 UUID를 사용한다.
- 표시 이름 예: "Windows 데스크톱", "작가용 iPad"
- iPad device_id는 Keychain 또는 안전한 로컬 저장소에 보관한다.


============================================================
23. Supabase v2 데이터 구조
============================================================

[documents]

- id: UUID
- project_id: UUID
- owner_id: UUID
- relative_path: TEXT
- title: TEXT
- content: TEXT
- content_hash: TEXT
- revision: BIGINT
- metadata_revision: BIGINT
- updated_at: TIMESTAMPTZ
- updated_by_device: UUID
- deleted_at: TIMESTAMPTZ nullable

[document_versions]

- id: UUID
- document_id: UUID
- revision: BIGINT
- content: TEXT
- content_hash: TEXT
- device_id: UUID
- operation_id: UUID UNIQUE
- created_at: TIMESTAMPTZ

[edit_leases]

- document_id
- device_id
- device_name
- session_id
- expires_at

[devices]

- device_id
- device_name
- platform
- last_seen_at

[보안]

- 공개 스키마의 모든 테이블에 RLS를 적용한다.
- 본인 계정만 프로젝트 행을 읽고 쓸 수 있다.
- iPad에는 publishable/anon 키만 둔다.
- service_role 키를 Windows 또는 iPad 클라이언트에 넣지 않는다.
- 서버 RPC에서도 owner_id를 다시 확인한다.
- 클라이언트가 보낸 updated_at을 최신 판단 기준으로 사용하지 않는다.


============================================================
24. 원자적 서버 저장 RPC
============================================================

Windows와 iPad는 documents 테이블을 직접 덮어쓰지 않는다.

공통 서버 함수 예:

commit_document(
    document_id,
    base_revision,
    content,
    content_hash,
    operation_id,
    device_id
)

[처리 순서]

1. operation_id가 이미 처리됐는지 확인한다.
2. 대상 문서를 트랜잭션 안에서 잠근다.
3. 서버 revision과 base_revision을 비교한다.
4. 같으면 document_versions에 불변 버전을 추가한다.
5. documents의 content와 revision을 원자적으로 갱신한다.
6. 새 revision을 반환한다.
7. 다르면 덮어쓰지 않고 conflict 결과와 서버 revision을 반환한다.

[예]

- 클라이언트 base 15, 서버 15 -> revision 16으로 성공
- 클라이언트 base 15, 서버 17 -> 충돌, 덮어쓰기 거부

[operation_id]

서버 저장은 성공했지만 응답이 유실되어 같은 작업을 다시 보내도
operation_id UNIQUE 제약을 이용해 버전을 중복 생성하지 않는다.


============================================================
25. 편집 lease와 Realtime
============================================================

[편집 lease]

- 문서를 열면 약 90초짜리 lease를 등록한다.
- 편집 중 30초마다 heartbeat를 보낸다.
- 정상 종료·문서 이동 시 lease를 해제한다.
- iPad가 잠들거나 앱이 중단되면 lease는 자연 만료된다.

[다른 기기가 편집 중일 때]

"Windows 데스크톱에서 001화를 편집 중입니다."

선택:

- 읽기 전용으로 열기
- 다른 화 열기
- 그래도 편집하기

lease는 충돌을 예방하는 안내 장치다. 오프라인 상황까지 영구 차단하는 강제 락은 아니다.

[Realtime]

- 문서 저장, 이름 변경, 이동, 삭제, 편집 시작·종료 알림을 받는다.
- 비공개 Broadcast 채널을 우선 사용한다.
- Realtime 메시지 본문을 최종 원고로 신뢰하지 않는다.
- 알림 수신 후 서버 revision을 다시 조회해 판단한다.


============================================================
26. 영구 동기화 큐
============================================================

[공통 필드]

- operation_id
- document_id
- operation_type
- base_revision
- content_hash
- payload
- created_at
- retry_count
- last_error

[iPad]

- SwiftData 또는 별도 SQLite에 보관한다.

[Windows]

- 현재 메모리 큐를 SQLite 영구 큐로 변경한다.

[동작]

- 앱을 강제 종료해도 큐가 남는다.
- 같은 문서 작업은 순서대로 보낸다.
- 네트워크 복구 시 자동 재시도한다.
- 지수 백오프와 최대 대기시간을 적용한다.
- 사용자가 수동 재시도를 실행할 수 있다.
- 성공한 작업은 로컬 revision을 갱신한 후 큐에서 제거한다.


============================================================
27. 업로드 시점
============================================================

[로컬 TXT 저장]

- 마지막 입력 후 약 800ms

[서버 업로드]

- 마지막 입력 후 약 2~3초 동안 추가 입력이 없을 때
- 다른 화로 이동할 때
- 프로젝트를 바꿀 때
- 앱이 background로 들어갈 때
- 사용자가 즉시 저장할 때

키 입력마다 Supabase에 업로드하지 않는다.


============================================================
28. 문서를 열 때의 동기화 순서
============================================================

1. 로컬 TXT를 즉시 화면에 표시한다.
2. 로컬 미처리 동기화 작업을 확인한다.
3. 서버 최신 revision을 조회한다.
4. 로컬 수정이 없고 서버가 최신이면 서버본을 내려받는다.
5. 로컬 수정이 있으면 기준 revision을 이용해 병합 여부를 판단한다.
6. 편집 lease를 등록한다.
7. 서버 확인이 끝나면 상태 배지를 갱신한다.

서버 확인 때문에 빈 로딩 화면을 오래 보여주지 않는다.
"로컬 원고 표시 중·서버 확인 중" 상태에서 편집은 가능하며 입력은 로컬에 보존한다.


============================================================
29. 3방향 병합
============================================================

충돌 시 다음 세 원고를 비교한다.

- Base: 편집 시작 당시 공통 원고
- Local: 현재 기기의 수정본
- Remote: 다른 기기가 서버에 저장한 수정본

[자동 병합]

서로 다른 줄·문단을 수정했다면 자동 병합하고 Remote revision을 기준으로 다시 commit한다.

[사용자 선택]

같은 줄이나 같은 범위를 서로 다르게 수정했다면 자동 선택하지 않는다.

충돌 화면:

- 기준본
- Windows 수정본
- iPad 수정본
- 자동 병합 가능한 부분

선택:

- 자동 병합본 사용
- Windows본 사용
- iPad본 사용
- 직접 편집
- 양쪽을 별도 파일로 보관

[충돌 백업]

- 백업/충돌/001화_Windows_날짜.txt
- 백업/충돌/001화_iPad_날짜.txt
- 백업/충돌/001화_Base_날짜.txt

어떤 선택을 해도 반대쪽 원고를 삭제하지 않는다.


============================================================
30. 이름 변경·이동·삭제 충돌
============================================================

[이름 변경과 이동]

- document_id는 유지한다.
- metadata_revision을 따로 비교한다.
- 같은 문서가 양쪽에서 다른 이름으로 변경되면 사용자에게 선택하게 한다.

[삭제]

- 서버 행을 즉시 제거하지 않고 deleted_at을 기록한다.
- 다른 기기에서 삭제된 문서를 수정했다면 복원 여부를 묻는다.
- 복원을 선택하면 새 revision으로 되살린다.

[새 권 생성]

- 25개 문서에 개별 document_id를 부여한다.
- 로컬에서 전체 생성 성공 후 서버에는 일괄 작업으로 전달한다.
- 일부 문서만 서버에 생성됐을 경우 operation_id를 이용해 안전하게 재시도한다.


============================================================
31. Windows 프로그램 변경 범위
============================================================

iPad 앱만 v2 동기화를 사용하면 무손실 동기화를 보장할 수 없다.
기존 Windows 프로그램도 다음을 변경한다.

- 기존 상대 경로 문서에 UUID 부여
- 직접 documents update 제거
- commit_document RPC 사용
- revision과 base_revision 기록
- SQLite 영구 동기화 큐
- operation_id 중복 방지
- edit lease와 heartbeat
- Realtime 변경 알림
- 3방향 병합
- 충돌본 자동 백업
- metadata revision 기반 이름 변경·이동
- tombstone 기반 삭제

[전환]

1. Supabase 전체 백업
2. v2 테이블과 RPC 생성
3. 기존 문서 데이터 복사 및 UUID·revision 부여
4. Windows 프로그램을 v2로 전환
5. Windows 단독 안정성 검증
6. iPad 앱을 같은 v2 규약으로 연결
7. 모든 기기 전환 후 직접 테이블 수정 권한 차단

구형 Windows 프로그램이 새 규약을 우회해 직접 덮어쓰지 못하도록 전환 시점을 명확히 관리한다.


============================================================
32. 설정
============================================================

[편집 설정]

- 글꼴
- 글자 크기
- 줄 간격
- 좌우 여백
- 상하 여백
- 타자기 스크롤
- 자동 저장 대기시간
- 스마트 기호 입력 세부 옵션

[백업 설정]

- 자동 백업 사용 여부
- 최대 보관 수
- 보존 기간

[동기화 설정]

- Supabase 동기화 사용 여부
- 현재 기기 이름
- 마지막 서버 동기화 시각
- 대기 작업 수
- 수동 재시도
- 전체 새로고침
- 충돌 파일 폴더 열기

[화면 상태]

- 마지막 작품
- 마지막 왼쪽 문서
- 마지막 오른쪽 문서
- 마지막 활성 에디터
- 좌우분할 여부
- 바인더 너비
- 펼쳐진 폴더
- 문서별 커서 위치


============================================================
33. 외장 키보드 단축키
============================================================

- Command+S: 즉시 저장
- Command+F: 현재 문서 검색
- Command+Shift+F: 전체 검색
- Command+Z: 실행 취소
- Command+Shift+Z: 다시 실행
- Command+B: 바인더 열기·닫기
- Command+\: 좌우분할 전환
- Command+Option+Left: 이전 화
- Command+Option+Right: 다음 화

iPad와 향후 Mac Catalyst에서 같은 명령을 재사용한다.


============================================================
34. 성능 목표
============================================================

[시험 데이터]

- 권 40개까지 확장 가능
- 화 1,000개
- 화당 약 6,000자
- 캐릭터·설정·메모 문서 500개
- 백업 5,000개

[목표]

- 프로젝트 진입: 2초 이내 목표
- 화 전환: 체감 지연 없이 표시
- 입력 중 끊김 없음
- 로컬 저장·서버 업로드 중 타이핑 유지
- 전체 검색 중 편집 가능
- 바인더 펼치기 즉시 반응
- 메모리 부족 종료 방지

[최적화 순서]

1. 실제 기기에서 측정
2. 최상위 항목 즉시 표시, 하위 폴더 지연 로딩
3. 병목이 확인될 때만 폴더 캐시 추가
4. 변경된 부모 폴더 캐시만 무효화
5. 검색 병목이 있을 때만 검색 색인 추가

추측만으로 복잡한 캐시를 먼저 만들지 않는다.


============================================================
35. 필수 자동 테스트
============================================================

[프로젝트와 파일]

- 프로젝트 생성·이름 변경·삭제
- UTF-8 읽기·쓰기
- 원자적 저장 실패 복구
- 기존 Windows 작품 가져오기
- 파일명과 경로 유효성

[바인더]

- 권·화 생성 규칙
- 화 접두사 보호
- 이동 제한
- 사용자 정렬
- 펼침 상태 복원

[새 권]

- 권이 없으면 1권과 001~025화 생성
- 1권이 있으면 2권과 026~050화 생성
- 2권까지 있으면 3권과 051~075화 생성
- 생성 중 실패 시 전체 롤백
- 기존 파일 덮어쓰기 금지

[편집기]

- 따옴표·괄호 자동 쌍
- 선택 영역 감싸기
- 닫는 기호 건너뛰기
- 빈 쌍 삭제
- 닫는 기호 앞 Enter
- ... -> …
- ㄴㄴ -> 「」
- ㄱㄱ -> 『』
- 장면 전환선 삽입·삭제
- 한글 조합 중 자동 변환 금지
- 자동 변환 한 번 Undo
- 빠른 화 전환 중 첫 글자 손실 없음

[저장·복원]

- 800ms 자동 저장
- 화 전환 전 저장
- background 전 저장
- 자동 백업
- 차이 비교
- 복원 전 백업
- 휴지통 원래 위치 복원

[검색·추출]

- 현재 문서 검색
- 전체 검색
- 권과 무관한 화수 정렬
- 전체·부분 TXT 추출
- 없는 화 보고
- 빈 화 제외
- UTF-8 파일 저장

[동기화]

- Windows와 iPad 동시 저장
- 두 기기 모두 오프라인 편집
- 서로 다른 문장 자동 병합
- 같은 문장 충돌 화면
- 서버 성공 후 응답만 유실
- 같은 operation_id 두 번 전송
- 작업 도착 순서 뒤바뀜
- 기기 시간 불일치
- 이름 변경과 본문 수정 동시 발생
- 삭제와 본문 수정 동시 발생
- iPad background 전환
- Windows 강제 종료
- 서버 중단 후 복구
- 1,000화 초기 동기화

모든 파일 테스트는 실제 작품이 아니라 임시 폴더에서 실행한다.


============================================================
36. 단계별 개발 순서
============================================================

[1단계: Xcode 기반]

- iPadOS 프로젝트 생성
- SwiftData 연결
- AppEnvironment와 기본 의존성 구성
- Git 저장소와 테스트 타깃 구성
- 시뮬레이터와 실제 iPad 실행

[2단계: 로컬 문서 저장]

- 프로젝트 생성
- Application Support 경로
- UTF-8 TXT 읽기·쓰기
- 원자적 저장
- 기존 작품 가져오기
- 파일 계층 테스트

[3단계: 바인더]

- 9개 최상위 항목
- 지연 로딩 트리
- 생성·이름 변경·이동·삭제
- 원고 전용 규칙
- 원고 컨텍스트 메뉴
- 새 권 25화 자동 생성

[4단계: 네이티브 편집기]

- UITextView 래퍼
- 한글 IME 안전 처리
- 커서와 선택 범위
- 글자 수
- 타자기 스크롤
- 스마트 텍스트 규칙
- 외장 키보드

[5단계: 자동 저장과 상태]

- 800ms 로컬 저장
- 화 전환과 background 저장
- 상단 상태 배지
- save generation 보호

[6단계: 백업과 휴지통]

- 자동 이력
- 미리보기
- diff
- 복원 전 백업
- 원래 위치·선택 위치 복원

[7단계: 검색과 TXT 추출]

- 현재 문서 검색
- 전체 검색
- 전체·부분 TXT 추출
- 파일 앱 저장과 공유

[8단계: Supabase v2 서버]

- 데이터베이스 백업
- documents/document_versions/devices/edit_leases
- RLS
- commit RPC
- Realtime Broadcast

[9단계: Windows 동기화 v2]

- UUID와 revision
- 영구 SQLite 큐
- RPC 저장
- edit lease
- 병합과 충돌 백업
- 기존 데이터 이전

[10단계: iPad 동기화]

- 같은 v2 규약 적용
- SwiftData 영구 큐
- Realtime
- 상태 표시
- 3방향 병합
- 충돌 해결 화면

[11단계: 실제 환경 검증]

- iPad Pro 11-inch (M5), iPadOS 26.x에서 가로·세로
- 가로에서 좌우분할을 켠 뒤 세로 전환, 다시 가로 복귀 시: 세로 단일 표시·버튼 비활성·가로 자동 복원을 확인
- 세로에서 iPadOS 26 창 최소화·최대화와 반복 크기 변경 시: 좌우분할 표시·버튼 상태가 반복 전환되지 않음을 확인
- Split View와 Stage Manager
- Magic Keyboard
- 장시간 집필
- Windows와 번갈아 편집
- Wi-Fi 차단·복구
- 1,000화 성능

[12단계: 개인 배포]

- 고정 Bundle ID
- 실제 iPad에 개발 서명 설치
- 프로비저닝 프로파일의 실제 ExpirationDate 기록
- 만료 전 캘린더 알림
- 갱신 전 전체 TXT 백업
- 앱을 삭제하지 않고 같은 Bundle ID로 덮어 설치


============================================================
37. Git 진행 규칙
============================================================

- 각 단계는 별도 커밋으로 저장한다.
- 테스트가 통과한 상태만 커밋한다.
- 사용자 원고와 자동 백업은 Git에 넣지 않는다.
- Supabase secret/service_role 키는 Git에 넣지 않는다.
- 기존 Windows 저장소의 다른 사용자 변경을 함께 stage하지 않는다.
- 서버 스키마 변경은 SQL migration 파일로 관리한다.

[권장 커밋 예]

chore: create iPadOS project foundation
feat: add local UTF-8 document storage
feat: implement binder and volume rules
feat: add native iPad text editor
feat: add smart writing text rules
feat: add autosave and storage status
feat: add backup and trash recovery
feat: add search and TXT export
feat: add Supabase revision sync schema
feat: migrate Windows sync protocol
feat: add iPad conflict-safe sync
test: verify cross-device conflict recovery


============================================================
38. 개인 설치와 운영
============================================================

- App Store 등록 없이 본인 iPad에 Xcode로 직접 설치한다.
- 무료 Personal Team은 프로파일이 7일이므로 실사용에는 불편하다.
- 유료 Apple Developer Program의 개발 프로파일은 유형과 인증서에 따라 실제 만료일을 확인한다.
- 멤버십 만료 직전 최종 설치를 할 경우에도 프로파일의 ExpirationDate를 직접 확인한다.
- 앱 삭제나 iPad 초기화에 대비해 원고를 로컬 TXT와 Supabase에 이중 보관한다.
- Bundle ID를 바꾸지 않는다.
- 앱 갱신은 기존 앱 위에 덮어 설치한다.
- 서명 갱신 전 전체 작품 TXT 내보내기를 실행한다.


============================================================
39. 최종 완료 기준
============================================================

- iPad에서 작품 생성과 기존 Windows 작품 가져오기가 가능하다.
- 원고, 캐릭터, 설정집, 메모장 등 바인더를 관리할 수 있다.
- 원고 메뉴에서 새 권과 25개 화를 자동 생성할 수 있다.
- 전체·부분 원고를 UTF-8 TXT로 추출할 수 있다.
- 한글 입력과 모든 스마트 텍스트 규칙이 안정적으로 작동한다.
- 빠른 화 전환 중 첫 글자가 영문으로 잘못 입력되거나 유실되지 않는다.
- 입력 후 자동으로 로컬 저장되고 상태를 명확히 알 수 있다.
- 자동 백업, 차이 비교, 복원, 휴지통 복구가 가능하다.
- 인터넷 없이 집필할 수 있다.
- 연결 복구 후 영구 큐의 작업이 자동 동기화된다.
- Windows와 iPad가 같은 document_id/revision/RPC 규약을 사용한다.
- 오래된 원고가 최신 원고를 덮어쓰지 못한다.
- 서로 다른 수정은 자동 병합한다.
- 겹친 수정은 양쪽을 모두 보존하고 비교 화면을 제공한다.
- 1,000화 규모에서 편집과 검색이 원활하다.
- AI 관련 코드와 화면은 포함하지 않는다.
- 향후 Mac Catalyst를 추가할 수 있는 구조를 유지한다.


============================================================
40. 최종 설계 결론
============================================================

이 앱은 단순한 iPad TXT 편집기가 아니라 다음 성격을 갖는다.

- iPad에 최적화된 장편 소설 집필 도구
- 기존 Windows 작품 구조와 호환되는 로컬 우선 앱
- 한글 조합과 작가용 특수 입력을 고려한 네이티브 편집기
- 실수와 파일 손상에서 회복할 수 있는 버전·백업 시스템
- Windows와 iPad 사이에서 무손실을 우선하는 revision 기반 동기화 시스템

동기화는 "충돌을 숨기는 것"이 아니라 다음을 보장하는 방향으로 완성한다.

- 충돌 사전 경고
- 오래된 저장 차단
- 가능한 수정 자동 병합
- 불가능한 수정 사용자 비교
- 모든 원본과 충돌본 보존

개인 집필 프로그램에는 복잡한 실시간 공동편집 CRDT보다
편집 lease + revision 비교 + 3방향 병합 + 불변 버전 보관의 조합이 가장 적합하다.
