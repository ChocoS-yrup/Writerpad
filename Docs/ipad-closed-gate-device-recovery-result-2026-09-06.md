# iPad 닫힌 관문 일반 복구 시험 결과

2026-09-06 KST. 기존 실기기 Debug **ab89384**의 진단 JSONL을 11:21:05에
읽기 전용으로 수집했다. 이후 DB 차단·해소 전이 보완은 아직 미커밋·미설치 상태다.

## 이번 관찰 결과

- 사용자: 현재 시각 11:17:40, 연결을 되살린 시각 11:18:35, 체감 복구 약 5초.
- 사용자: 앱 재실행 후 A·B 문장 모두 보존 확인. 원고 본문을 도구로 읽지는 않았다.
- 기기 로그: 네트워크 available → UI synced **6.771초**.
- 기기 로그: 새 실행의 첫 기록 → UI synced **1.437초**.
- 새 실행의 인증 복원 구간 0.537초, 핸드셰이크 시작→성공 0.201초.
- 11:21:29 환경설정 조회: 저장된 계약 관문 키 0개, 열린 관문 **0개**.
- 수집 로그에 contractUpload 사건 **0개**. 실제 계약 전송 시험을 수행하지 않았다.

이번 일반 복구 및 재실행 시험에서는 자동 정상 표시 복귀와 사용자 확인상 문장
보존이 확인됐다. 과거 30초~2분 지연은 재현되지 않았다.

## 시각 대조와 해석 범위

기기의 network available 기록은 11:18:26, 이후 UI synced는 11:18:33이다.
사용자가 기록한 연결 복구 시각 11:18:35와 약 9초 차이가 있다. 기록 기준·시점의
차이를 확인하지 않았으므로 시각을 강제로 보정하거나 서로 다른 기준으로 지연을
계산하지 않았다. 6.771초는 동일 session의 monotonic uptime 차이이며 사용자
체감 약 5초와 별도로 보관한다.

11:18:27–11:18:33에 일반 upload drain 구간 6.335초가 기록됐다. 이는 한 cycle의
길이이며 개별 쓰기 RPC의 지연이나 영수증이 아니다. 그와 병렬로 pull도 실행됐다.
그 구간을 과거 지연의 원인으로 단정하지 않는다.

새 실행은 11:19:20에 기록이 시작됐고 11:19:21에 UI synced가 기록됐다. 첫 기록
이전의 앱 시작 시간은 측정하지 않았다. 이번 로그에서 인증·핸드셰이크 복구가
실행된 것은 확인되지만, 서버 document_versions 및 원고 본문은 별도 조회하지 않았다.

## 보관 자료

`build/recovery-device-20260906-1120/SyncDiagnostics/recovery.jsonl`: 163,721 bytes,
672개 유효 JSON 사건, 실행 session 2개. 회전 파일은 아직 없었다. 기록 필드가
정해진 진단 schema 안에 있는지 확인했다. 원본에는 원고·작품 이름·계정·토큰
필드가 없다. 원본 JSONL은 Git에 추가하지 않는다.

로그 SHA-256: `76d00387031ebf65468226092f2dfac7827b065e42414bf23dad2fa87048358b`.
설치 소스/바이너리 식별은 기존 설치 영수증을 참조했다. 이번에 기기 실행 파일을
다시 추출해 해시를 계산한 것은 아니다.

수치·시각·출처·제약은 `Docs/evidence/ipad-closed-gate-device-recovery-2026-09-06.json`에
기록했다. Windows에는 이번 결과를 앞선 제품·테스트 diff와 구분해서 전달한다.
추가 장애 시험을 반복할 필요는 없다. 남은 일은 Windows의 소스 재대조 후 양쪽
커밋/빌드를 고정하는 것이다. 실제 계약 관문은 계속 닫아 둔다.
