# iPad 일반 본문 검증 실행 연결 — 2026-09-12

합의 `general-body-20260912-v1`의 전용 화면·인증·단계 실행을 기존 제품 저장소에 연결한다. 구현 및 격리 검사·미서명 후보 빌드가 대상이며 실제 설치·서버 접근·관문·보류 변경은 포함하지 않는다.

전용 화면은 계정·로컬 기준 확인, 인증·실행 준비, 첫 수신, 저장·송신 1회, 최종 수신·5줄 대조를 제공한다. 준비에는 실제 로그인으로 검증된 메모리 세션과 시험 작품의 열린 관문, 현재 계정·작품 연결·활성 상태가 필요하다. 현재 token만 읽으며 자격 증명을 다시 저장하거나 자동 refresh하지 않는다. 준비당 새 handshake 1회로 ID_BASED/epoch 1/기존 계약 해시를 확인한다.

실행 권한은 해당 화면의 유효한 준비에만 귀속된다. 전역 송신 잠금은 계속 유지한다. local/server UUID가 다른 기존 작품 연결을 검증한 뒤 그 문맥에서만 로컬 파일 적용을 허용한다. 요청 직전 URLProtocol에서 append-only 기록을 남기고 URL·메서드·본문·인증 헤더·응답을 검사한다. 첫 수신 3 GET, document_commit 1 POST, 최종 수신 3 GET이며 자동 재시도·lease·Realtime은 포함하지 않는다.

각 변경 전에 원본의 SQLite·SwiftData·파일을 격리 복제해 제품 코드로 예상 상태를 만든다. 수신 첫 단계는 구조 기준을 확립하며, 본문 저장 단계는 그 새 기준을 필수로 요구한다. 저장 후 실제 큐에서 얻은 operation/batch ID 및 요청 바이트가 격리 예측과 일치해야 송신할 수 있다. 서버 응답 유실 시 processing 기록과 로컬 본문을 보존하고 재실행을 차단한다.

보존된 기준 자료를 사용하므로 준비를 위한 새 서버 기준 조회는 없다. 해당 보존본의 날짜는 밀리초까지만 남아 있어 1ms 이내 반올림 차이만 허용한다. 날짜 비교에는 원래 Date 값을 사용하고, 다른 모든 필드는 정확히 비교한다. 본문·revision·UUID·기존 이력·다른 작품 대기열 검사와 전체 해시 검사를 유지한다.

최종 검사 결과와 후보 식별값은 `build/ipad-general-validation-runtime-20260912/verification.json` 및 별도 4항목 실행 결과 문서에 기록한다. 중간 실패는 private 검사 로그에 보존하며 통과 결과로 세지 않는다.
