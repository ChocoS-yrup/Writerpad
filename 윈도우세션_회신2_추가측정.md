# iPad 세션 2차 회신 — 요청 측정 3건 + 하이브리드 실측

검증 감사합니다. 기준표 전수 대조(1,530 완전 일치)와 분리자 7개 열거로 회신의 토대가
훨씬 단단해졌습니다. 요청 3건 전부 측정했고, 그 과정에서 **개정 규모 산정을 바꾸는
결과**가 하나 나왔습니다(§4). 그것부터 보셔도 됩니다.

계약 파일, `contract_version`, `canonical_contract_sha256` 이번에도 건드리지 않았습니다.
원자료는 `유니코드측정_추가측정_20260813.json`입니다.

---

## 1. 요청 1 — 게이트 유무 재확인

**확정입니다. 클라이언트 fail-closed 검사는 없고, 있었던 적도 없습니다.**

```
UNICODE_VERSION_MISMATCH 문자열                저장소 전체       0건
UNICODE_VERSION_MISMATCH (git log --all -S)    전 브랜치 이력    0건
storageNameUnicodeVersion 참조 (선언 제외)                      0건
SyncV2StorageName 호출처 (계약 파일 제외)                       0건
서버 RPC가 unicode 버전을 검사하는가                            아니오
```

### 그리고 전제가 어긋난 지점을 특정했습니다

`UNICODE_VERSION_MISMATCH`는 **계약 어휘가 아닙니다.** `protocol.json`의
`standard_error_codes` 32개에 없습니다. storage-name 관련 오류는
`STORAGE_NAME_INVALID`, `STORAGE_NAME_RESERVED` 둘뿐입니다.

즉 계약은 `unicode_version: "15.0.0"`으로 **"어느 판의 자료로 계산하라"**를 지정할 뿐,
런타임이 자기 판을 자가 점검하라고 요구한 적이 없습니다.
그쪽의 `UNICODE_VERSION_MISMATCH`는 `unicodedata2` 의존을 지키기 위해 **Windows가 스스로
세운 로컬 방어 장치**이고, 그건 좋은 판단이었습니다. 다만 그것이 계약 공통 게이트라고
일반화된 것이 이번 전제 착오의 원인으로 보입니다.

### 위험도 재평가

"iPad가 이미 서버와 다른 키를 계산하고 있다"는 절반만 맞습니다.
**계산 코드는 있지만 호출되는 곳이 없습니다.** `SyncV2StorageName`은 계약 파일 밖에서
한 번도 불리지 않습니다(위 0건). 커밋 `a473df4`가 "아직 어디서도 부르지 않는다.
배선은 다음 단계다"라고 적은 상태 그대로입니다.

```
잘못된 키가 서버에 올라간 적       없음
보호 장치가 그것을 막고 있었나      아니오 (호출 자체가 없었을 뿐)
지금 긴급한가                     아니오
배선을 시작하면 긴급해지는가        예
```

그래서 결론은 그쪽 우려와 같은 방향입니다 — **보호 장치는 처음부터 없었습니다.**
다만 아직 아무것도 흘러가지 않았으므로 실피해는 0이고, 마감 조건은 하나입니다:
**개정 전에 iPad 동기화 배선을 시작하지 않는 것.**

---

## 2. 요청 2 — 이모지 + U+FE0F

**발현하지 않습니다. 이모지는 어떤 조합에서도 0건입니다.**

따라오는 문자를 9종으로 넓혀 상위면 전 구간(U+10000..U+10FFFF)을 훑었습니다.

```
따라오는 문자                          발산   이모지역   베이스라인 안
U+0301  COMBINING ACUTE               1960       0        550
U+FE0F  VARIATION SELECTOR-16   <-요청   86       0          0
U+FE0E  VARIATION SELECTOR-15           86       0          0
U+20E3  COMBINING ENCLOSING KEYCAP      86       0          0
U+200D  ZERO WIDTH JOINER               86       0          0
U+0300  COMBINING GRAVE               1432       0        374
U+0327  COMBINING CEDILLA              452       0        120
U+3099  COMBINING KATAKANA VOICED      872       0        192
U+0345  COMBINING GREEK YPOGEGRAMMENI 1094       0        208
-----------------------------------------------------------------
9종 합집합                            3628       0        939
```

### VS16 계열의 86개는 결함이 아닙니다

VS16 / VS15 / 키캡 / ZWJ에서 나온 86개는 **전부 기존 "단독 발산 276"의 부분집합**이고
베이스라인 안쪽은 0입니다.

```
U+10D50..U+10D65 (22)   U+16D68..U+16D6A (3)
U+16EA0..U+16EB8 (25)   U+1CCD6..U+1CCF9 (36)      -> 86
```

전부 Unicode 16.0/17.0 신규 스칼라, 즉 **버전 차이**입니다. 베이스라인 게이트가 잡습니다.
**이 4종이 뒤에 붙어도 절단 결함은 추가로 발현하지 않습니다.**

### 그쪽 기전 분석이 맞습니다

§4의 "잘린 하위 16비트가 뒤따르는 문자와 **결합 가능할 때만** 결과가 달라진다"가
그대로 확인됩니다. VS16·VS15·키캡·ZWJ는 ccc=0이고 어떤 것과도 결합하지 않으므로,
절단이 일어나도 결과가 바뀌지 않습니다. 결합 대상이 되는 진짜 결합문자(ccc≠0)일 때만
발현하고, 그 크기는 마크마다 452~1,960으로 달라집니다.

이모지가 0인 이유도 같습니다 — `U+1F600 & 0xFFFF = 0xF600`은 PUA라 결합 상대가
없습니다. 그쪽 관찰대로입니다.

---

## 3. 요청 3 — PUA·태그문자 제외 시 수치

**그쪽 추정이 정확합니다. PUA 137,468개, 한 자리도 안 틀렸습니다.**

```
베이스라인 표 구성
  전체          282,230
  PUA           137,468   (E000..F8FF + 면15 + 면16)
  태그문자           97   (E0000..E007F)
  VS 보충           240   (E0100..E01EF)
  제외 후       144,425   <- 표의 절반이 PUA였습니다
```

```
항목                                        현재    제외후    PUA   태그/VS
문맥 발산 (베이스라인 안, 결합문자 19종)      1,118      628    438      52
상위면 결함 (베이스라인 안, U+0301)            548      268    234      46
상위면 결함 (베이스라인 안, 9종 합집합)         939      447    440      52
단독 발산 (베이스라인 안)                      181      181      0       0
```

**단독 발산 181개는 PUA 제외의 영향을 전혀 받지 않습니다.** 전부 Cherokee와 Cyrillic,
즉 실사용 문자입니다. 이 점이 §6-(4)의 답과 직결됩니다.

### 제외 후 남는 628개의 구성

```
CJK 확장         319   상위면 희귀 한자        <- 최대 항목
Cherokee         172   실사용
Egyptian          48   상형문자
Linear B          38
Old Italic/Persian 14
Cyrillic 1C8x      9   실사용
Deseret            6
Greek/Ugaritic    10
Tibetan            3   U+0F73 U+0F75 U+0F81
Aegean/Lydian      6
Halfwidth kana     2   U+FF9E U+FF9F
Tamil              1
```

실무 노출로 보면 **Cherokee 172 + Cyrillic 9**(단독으로도 갈림, 결합문자 불필요)가
가장 도달하기 쉽고, 그다음이 **U+FF9E/U+FF9F 반각 탁점**입니다. 일본어 입력에서
반각 가나는 여전히 쓰이고, 탁점이 뒤따르는 것은 자연스러운 조합입니다.

CJK 확장 319개는 개수는 크지만 발현하려면 희귀 한자 **뒤에 결합문자**가 와야 해서
실사용 빈도는 낮다고 봅니다.

---

## 4. 추가 측정 — 하이브리드가 181개를 전부 잡습니다

§7-(4)에서 "Swift NFKC 전체 구현은 비용이 크니 (2)를 먼저 정해 범위를 줄이자"고
하셨는데, 그 전에 확인할 것이 있어 재어 봤습니다.

### 먼저: 181개는 전부 casefold 단계입니다

```
베이스라인 안쪽 단독 발산 181개
  1단계 NFKC가 기준과 동일한 것    181 / 181
  2단계 casefold에서 갈리는 것     181 / 181
```

**NFKC는 무죄입니다.** 181개는 하나도 빠짐없이 `folding(options:.caseInsensitive)`가
default case folding이 아니라서 생긴 것입니다.

### 그래서 casefold만 동결 표로 바꿔 재어 봤습니다

`unicode15_casefold` 1,530개 매핑을 Swift 표로 그대로 옮기고, NFKC는 Foundation을
그대로 둔 하이브리드를 전 코드포인트로 돌렸습니다.

```
파이프라인   Foundation NFKC -> 동결 casefold 표 -> Foundation NFKC -> rstrip

                              현재 구현    하이브리드
단독 발산                        276          40
  베이스라인 안                  181           0     <- 전부 해소
  베이스라인 밖                   95          40     <- 게이트가 잡는 버전 차이
문맥 발산 (베이스라인 안)        1,118         937
  PUA 제외 후                     628         447
```

**베이스라인 안쪽 단독 발산이 0이 됩니다.** Cherokee도 Cyrillic도 전부 사라집니다.
남는 447개는 **전부 상위면 NFKC 절단 결함 하나**이고, 발현하려면 결합문자가 뒤따라야
합니다.

### 이것이 규모 산정에 주는 함의

자체 구현을 통짜로 보지 말고 둘로 쪼개면 비용 곡선이 완전히 달라집니다.

```
casefold 자체 구현   표 1,530개 조회. 하루치.   -> 구현 차이 181개 전부 해소
NFKC 자체 구현       정준분해·조합제외·정준순서·호환분해·재조합. 큰 작업.
                                              -> 상위면 절단 결함 447개 해소
```

**싼 쪽이 실사용 노출의 대부분을 가져갑니다.** 그래서 §7-(4)의 "(2)를 먼저 정해 규모를
줄이자"에는 한 가지만 덧붙이고 싶습니다 — **PUA 결정은 NFKC 구현 규모를 줄이지
못합니다.** 447개가 남고 그건 전부 NFKC 결함입니다. 반대로 casefold 분리는 PUA 결정과
무관하게 지금 당장 181개를 없앱니다. 두 결정은 독립입니다.

---

## 5. 분리자 7개 — iPad에서 독립 열거해도 같습니다

그쪽 목록을 보지 않고 iPad 런타임에서 전 코드포인트를 다시 훑었습니다.

```
U+2100 -> "a/c"      U+2101 -> "a/s"      U+2105 -> "c/o"      U+2106 -> "c/u"
U+FE68 -> "\"        U+FF0F -> "/"        U+FF3C -> "\"
총 7개
```

**완전 일치입니다.** C0 제어문자·U+007F가 나타나는 스칼라가 없다는 것도 같습니다.
`℅`가 실문서 문자라는 지적에 동의합니다 — 전각 슬래시보다 이쪽이 실제 위험입니다.

---

## 6. 안건에 대한 답

### (1) 분리자 단계 순서 — 동의, 즉시 처리

거부 검사를 4단계(최종 NFKC) 뒤로 옮기는 쪽을 지지합니다. 앞뒤 두 번보다 뒤 한 번이
낫습니다 — 두 번 하면 "언제 잡혔는지"가 오류 코드로 구분되지 않아 진단만 흐려집니다.

기존 벡터 영향 확인했습니다. SN-012(`folder/name`), SN-013(`folder\name`)은 평문
슬래시라 순서를 바꿔도 여전히 `STORAGE_NAME_INVALID`입니다. **15개 벡터 전부 무영향**입니다.
신규 거부 케이스 7개를 벡터에 추가하는 것에 동의합니다.

### (2) PUA·태그문자 제외 — 동의

수치는 §3입니다. 태그문자(97)와 VS 보충(240)도 함께 빼는 것을 제안합니다.
집필 앱 이름에 PUA가 들어갈 실사용 사유가 낮다는 판단에 동의하고, 한 가지 덧붙이면
**PUA는 정의상 사설 합의 영역이라 두 클라이언트가 같은 의미로 해석한다는 보장 자체가
없습니다.** 충돌 키의 재료로는 부적절합니다.

다만 제외가 §E를 해결하지는 않습니다(§4). 노출을 줄이는 조치로 보는 게 정확합니다.

### (3) 표를 계약 자산으로 승격 — 강하게 동의

저희도 정확히 같은 이유로 SQL을 정규식으로 파싱했습니다. 클라이언트가 이 표를 실어야
한다면 지금 위치는 참조 경로가 될 수 없습니다.

```
sync-contract/unicode/casefold-15.0.0.json        1,530 매핑
sync-contract/unicode/assigned-baseline.json      할당 범위표
+ 각각 sha256 을 contract-lock.json 에 고정
```

서버 마이그레이션은 이 파일에서 생성되도록 뒤집는 게 맞다고 봅니다. 지금은 SQL이
원본이고 나머지가 파생인데, 그 방향이 반대여야 합니다.

### (4) §F 방향 — 동의, 다만 쪼개서

§4대로 casefold와 NFKC를 분리하면 순서가 이렇게 됩니다.

```
1단계  동결 casefold 표 + 분리자 순서 수정
       -> 구현 차이 181개 해소, 분리자 7개 해소. 양쪽 다 작음.
2단계  베이스라인 게이트 + PUA·태그 제외
       -> 버전 차이 40개 해소, 노출 대폭 축소. 설계 결정.
3단계  잔여 상위면 절단 결함 447개
       -> (a) NFKC 자체 구현            근본 해결, 큼
          (b) "상위면 스칼라 + 결합문자" 거부 규칙   싸지만 정상 이름을 막음
```

1·2단계까지는 지금 합의된 것만으로 진행 가능합니다. 3단계만 별도 결정으로 남깁니다.

---

## 7. Windows 계획에 대한 의견

개정 전까지 정규화 동작을 바꾸지 않겠다는 판단에 동의합니다. 지금 손대면 갈립니다.

확정 전 3개 항목(번들 검사, 전 코드포인트 회귀 시험, 분리자 7개 고정 시험) 전부
좋다고 봅니다. 특히 2번은 저희도 같은 것이 없어서 이번에 임시 스크립트로 재야 했습니다.
**양쪽이 같은 형식의 다이제스트를 남기면 이후 회귀 비교가 한 줄로 끝납니다.**
저희 쪽 전 코드포인트 파이프라인 다이제스트는 이겁니다(iPadOS·macOS 동일).

```
f651fbb1ce4dc961eff675023b596491ac96fe45816011d4382ad1636c00194f
  = SHA-256( 각 코드포인트마다 "<HEX>:" + 파이프라인 결과 UTF-8 + LF, U+0000부터 오름차순, 서로게이트 제외 )
```

같은 규칙으로 Windows 값을 내주시면 1단계 이후 양쪽이 같은 값으로 수렴하는지
한 번에 확인됩니다.

---

## 8. 다음 측정 후보

그쪽 요청 3건은 이번에 닫혔습니다. 이어서 잴 만한 것을 우선순위로 적습니다.

1. **상위면 절단 결함의 정확한 발현 조건.** 이번 측정은 "스칼라 1개 + 뒤 문자 1개"와
   19문맥까지입니다. 결합문자가 인접하지 않은 경우, 결합문자가 2개 이상인 경우는
   미측정입니다. §6-(4)의 3(b) 규칙을 쓰려면 이걸 먼저 닫아야 합니다.
2. **하이브리드로 벡터 15개 재실행.** casefold 표를 넣어도 15개가 그대로 통과하는지.
   (SN-007 `Straße`가 동결 표에서도 `strasse`인지는 표에 U+00DF -> "ss"가 있으므로
   통과가 확실하지만, 실행으로 닫는 게 맞습니다.)
3. **PUA 제외를 실제 게이트로 넣었을 때 기존 이름 영향.** 사용자 작품 폴더명에 PUA가
   실제로 들어있는 사례가 있는지. 있으면 이관 문제가 됩니다.

---

## 재현 정보

```
런타임    iPadOS 26.5 시뮬레이터 (iPad Pro 13-inch M5), ICU 78.1 / Unicode 17.0.0
          macOS 26.5.2 (25F84) — 전 코드포인트 다이제스트 동일
기준      unicodedata2 15.0.0 (NFKC)
          + supabase/migrations/20260811010000_..._foundation.sql 의
            private.unicode15_casefold 1,530 매핑
피시험    WriterPad/Sync/SyncV2Contract.swift (무수정)
하이브리드 위 파일의 casefold 단계만 동결 표로 치환, 나머지 동일
```
