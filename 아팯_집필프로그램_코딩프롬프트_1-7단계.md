# iPadOS 집필프로그램 코딩 프롬프트 — 로컬 개발 1~7단계

- 작성 기준일: 2026-07-15
- 원본: `아팯_집필프로그램_코딩프롬프트.md`
- 요구사항 기준: `아팯_집필프로그램_통합개발기획서.md`
- 범위: Supabase 연동 이전의 로컬 전용 개발

## 사용 방법

- 아래 프롬프트를 1단계부터 순서대로, 세부 단계 하나씩 실행한다.
- 각 세부 단계가 끝나면 테스트 결과와 미완료 항목을 확인한 뒤 다음 단계로 진행한다.
- 실패한 테스트가 있으면 다음 단계로 넘어가지 않는다.
- 실제 작가 원고가 아닌 임시 프로젝트와 fixture로 검증한다.
- Xcode 빌드와 실제 iPad 검증은 macOS에서 수행한다.
- 최소 지원 버전은 iPadOS 17.6으로 유지하고, 주 실기기 검증 환경은 iPad Pro 11-inch (M5)의 iPadOS 26.x로 기록한다.
- iPadOS 26 전용 API는 가용성 검사 뒤에만 사용하며 iPadOS 17.6 동작 경로를 유지한다. 창 최소화·최대화·크기 변경은 화면 방향 전환과 별도 사건으로 검증한다.
- 각 단계에서 변경한 파일만 별도 Git 커밋으로 남기고 기존 사용자 변경을 함께 stage하지 않는다.
- 구현할 수 없는 실제 기기 검증은 통과로 기록하지 말고 미검증으로 남긴다.

## 이번 분리본의 고정 결정

1. iPad 앱에는 AI 집필도우미 코드와 화면을 넣지 않는다.
2. `플롯`과 `메인 스토리 틀`은 시스템 고정 항목으로 구현하지 않는다.
3. 실제 원고는 `원고/N권/NNN화.txt` 구조로 저장한다.
4. 플롯 등 보조 자료는 사용자가 일반 문서 영역에 직접 폴더와 TXT 문서를 만들어 관리한다.
5. 고정 바인더 최상위 항목은 다음 8개다.
   - 원고
   - 캐릭터
   - 설정집
   - 메모장
   - 흐름 정리
   - 복선
   - 장소
   - 휴지통
6. Windows에서 사용할 수 없는 문자, 예약 이름, 경로 탈출, 대소문자·Unicode 정규화 충돌을 차단한다.
7. TXT 전체 추출은 공백을 포함해 300자 미만인 화를 처음 발견하면 그 화부터 이후 추출을 중단한다.
8. Supabase 연결, 인증, SQL, RPC, Realtime, edit lease, 실제 영구 동기화 큐, revision 병합은 이 문서 범위에서 구현하지 않는다.
9. 서버 기능은 protocol과 의존성 주입 경계만 준비하며 Windows v2 확정 전 서버 스키마나 payload를 고정하지 않는다.

---

# 1단계: Xcode 기반과 로컬 아키텍처

## 1-1. 저장소 조사와 개발 문서 준비

### 지금부터

`아팯_집필프로그램_통합개발기획서.md`와 이 1~7단계 코딩 프롬프트를 제품 요구사항 기준으로 삼아 iPadOS 집필 앱을 단계적으로 개발한다. 먼저 저장소 전체와 Git 상태, 적용되는 `AGENTS.md` 등의 작업 지침, 기존 Windows 앱 구조, 테스트, 비밀정보 관리 방식을 읽기 전용으로 조사하라. 아직 제품 코드는 구현하지 마라.

`Docs/` 아래에 요구사항 추적표, 목표 아키텍처, 모듈 의존성, 1~7단계 완료 조건, 위험 목록, ADR 색인을 작성하라. 본문 원본은 UTF-8 TXT, 메타데이터는 SwiftData, 로컬 우선, 원자적 저장, UUID 기반 문서 정체성, IME 존중, 향후 Mac Catalyst 재사용 원칙을 고정하라. AI 관련 코드와 화면, `플롯` 및 `메인 스토리 틀` 고정 항목은 iPad 앱 범위에서 제외하라.

Supabase와 Windows v2 규약은 미확정 외부 의존성으로 기록한다. 이번 1~7단계에서는 실제 서버 구현을 하지 않고 교체 가능한 protocol 경계만 둔다. 기존 사용자 변경과 실제 원고를 수정하지 말고 비밀키를 출력하지 마라. 마지막에 생성한 문서, 발견한 위험, 다음 단계 진입 조건, Git 변경 요약을 보고하라.

### 여기까지

## 1-2. Xcode 프로젝트 기반 구성

### 지금부터

1-1 문서와 기획서를 다시 확인하고 Xcode 기반을 구성하라. 최소 iPadOS 17.6, SwiftUI 앱, SwiftData, UIKit/TextKit 편집기 연동을 전제로 `WriterPad` 앱과 테스트 타깃을 만든다. 주 검증 환경은 iPad Pro 11-inch (M5), iPadOS 26.x로 기록한다. 기존 저장소에 Xcode 프로젝트가 있다면 새로 만들지 말고 현재 구성을 안전하게 보완하라. 지원 화면 방향, Split View와 Stage Manager, iPadOS 26 창 최소화·최대화·크기 변경, 고정 Bundle ID 설정 위치, 향후 Mac Catalyst를 켤 수 있는 빌드 설정을 준비하되 iPhone 전용 UI는 만들지 마라.

App, Domain/Models, Domain/Rules, Domain/Protocols, Data/Local, Data/Backup, Data/Trash, Data/Migration, Sync, Features, Platform, Resources, Tests 구조를 실제 그룹과 폴더에 반영하라. `AppEnvironment`를 통해 의존성을 주입하는 최소 골격을 만들고 View가 파일 시스템이나 향후 서버 구현을 직접 호출하지 못하게 경계를 세워라. Sync 폴더는 protocol과 빈 구현 경계만 허용하고 Supabase 패키지나 설정은 추가하지 마라.

사용자 데이터, 백업, `.env`, xcconfig 비밀값이 Git에 들어가지 않도록 `.gitignore`와 샘플 설정을 정리하라. 빈 앱을 시뮬레이터와 가능하면 실제 iPad에서 빌드하고 기본 테스트를 실행하라. 완료 시 변경 파일, 빌드·테스트 결과, 수동 Xcode 설정을 보고하라.

### 여기까지

## 1-3. 도메인 모델과 저장 경계

### 지금부터

화면 프레임워크와 저장 구현에 의존하지 않는 순수 Swift 도메인 모델을 구현하라. `Project`, `DocumentNode`, `BackupSnapshot`, `SaveState`와 필요한 열거형·값 객체를 만든다. `document_id`와 `project_id`는 UUID이며 이름 변경, 이동, 휴지통 이동 후에도 변하지 않아야 한다. 상대 경로는 문서 정체성이 아니라 위치 정보임을 타입과 문서로 명확히 하라.

문서 종류, 부모 ID, 상대 경로, 사용자 순서, 수정 시각, SHA-256, 삭제 상태, 커서 위치, 펼침 상태, 좌우 편집기 상태를 모델링하라. 본문 문자열은 SwiftData 모델에 저장하지 않는다. 저장소, 파일 저장기, 백업, 검색, 추출, 시계, UUID 생성기, 해시 계산기의 protocol을 정의해 테스트 대역을 주입할 수 있게 하라.

향후 동기화에 필요한 `document_id`, `project_id`, content hash용 확장 지점은 유지하되 서버 revision, metadata revision, operation payload의 최종 구조를 지금 확정하지 마라. 동기화 protocol은 `미설정`, `로컬 전용`, `향후 연결` 상태를 표현하는 최소 경계만 둔다.

Codable 또는 SwiftData 변환 왕복, UUID 유지, 경로 변경, 상태 전이, 본문 미중복을 테스트하라. 한 파일은 500줄을 넘지 않게 분리하고 공개 API를 문서화하라. 완료 시 모델 관계와 테스트 결과를 보고하라.

### 여기까지

## 1-4. SwiftData 메타데이터 계층

### 지금부터

프로젝트, 문서, 앱 화면 상태에 필요한 SwiftData 메타데이터 계층을 구현하라. 원고 본문은 절대로 SwiftData에 중복 저장하지 마라. 스키마 버전과 마이그레이션 계획을 처음부터 포함하고 UUID, 상대 경로, 부모 관계, 사용자 순서, 해시, 삭제 상태에 필요한 유일성 및 조회 전략을 설계하라.

`ProjectRepository`의 기본 CRUD, 마지막 프로젝트, 마지막 열린 좌우 문서, 활성 편집기, 바인더 너비, 펼쳐진 폴더, 문서별 커서 위치 저장 API를 actor 또는 명확한 격리 경계로 구현하라. 잘못된 부모 ID, 중복 document_id, 프로젝트를 넘는 부모 관계가 저장되지 않게 검증하라. 앱 최초 실행, 스키마 재오픈, 손상 메타데이터 오류 처리, 인메모리 테스트 컨테이너를 지원하라.

실제 동기화 큐와 서버 revision 필드는 Windows v2 확정 후 추가할 수 있도록 migration 여유만 남기고 현재 데이터베이스 계약으로 고정하지 마라. 앱 재시작을 모사해 상태 복원, 문서 ID 보존, 순서 보존, 본문 미저장, 마이그레이션 기본값을 확인하라. 실패를 숨기지 말고 사용자 원고에 손대지 않는 복구 경로를 정의하라.

### 여기까지

## 1단계 완료 조건

- iPadOS 17 이상 앱과 테스트 타깃이 빌드된다.
- 로컬 기능 모듈의 의존성 경계가 만들어졌다.
- 본문을 저장하지 않는 SwiftData 메타데이터가 재실행 후 복원된다.
- UUID와 상대 경로의 역할이 분리됐다.
- Supabase 구현이나 비밀값이 포함되지 않았다.

---

# 2단계: 로컬 문서 저장과 작품 관리

## 2-1. 로컬 경로 정책과 파일명 검증

### 지금부터

Application Support 아래의 로컬 작품 경로 계층을 구현하라. 기본 구조는 다음과 같다.

```text
Projects/
└─ 프로젝트명/
   └─ 집필모드/
      ├─ 메인/
      │  ├─ 원고/
      │  ├─ 캐릭터/
      │  ├─ 설정집/
      │  ├─ 메모장/
      │  ├─ 흐름정리/
      │  ├─ 복선/
      │  ├─ 장소/
      │  └─ 휴지통/
      ├─ 백업/
      │  ├─ 자동저장/
      │  ├─ 복원전/
      │  └─ 충돌/
      └─ 설정.json
```

`플롯`과 `메인 스토리 틀`은 고정 폴더로 만들지 마라. 플롯 자료가 필요한 사용자는 일반 문서 영역에서 직접 폴더와 TXT 문서를 만들 수 있어야 한다.

Windows 참조 구현과 테스트 작품에서 휴지통의 실제 호환 경로는 `메인/휴지통`이다. iPad도 이 경로를 사용하고 `집필모드/휴지통`이라는 별도 루트 폴더를 만들지 마라. 기존 Windows 작품의 `메인/플롯`과 `백업/전환직전`은 사용자 자료를 잃지 않도록 가져오기 시 보존하되 새 iPad 작품의 고정 폴더로 만들지 마라.

`PathPolicy`와 `ProjectPathResolver`를 만들어 모든 경로 생성을 한곳에서 처리하라. 프로젝트명, 파일명, 폴더명에서 경로 탈출, 절대 경로 주입, 빈 이름과 다음 Windows 금지 문자를 차단하라.

```text
< > : " / \ | ? *
```

제어 문자, 이름 끝 공백·마침표, `.`과 `..`, `CON`, `PRN`, `AUX`, `NUL`, `COM1`~`COM9`, `LPT1`~`LPT9`도 확장자와 대소문자에 관계없이 차단하라. 대소문자 비구분과 Unicode NFC 정규화 후 같은 이름도 충돌로 처리하라. 지나치게 긴 이름과 전체 상대 경로를 검사하되 정확한 길이 정책은 한곳의 설정값으로 관리하라.

`.txt` 확장자는 저장 시 유지하고 바인더 표시에서만 숨긴다. 외부 입력 경로가 프로젝트 루트 밖으로 나가지 않는지 표준화 후 다시 검증하라. 임시 디렉터리에서 표준 구조 생성, 한글 이름, 금지 문자, 예약 이름, 정규화 충돌, 경로 탈출, 재실행 멱등성을 테스트하라.

### 여기까지

## 2-2. UTF-8 TXT 원자적 저장

### 지금부터

`LocalDocumentStore` actor를 구현해 UTF-8 TXT 읽기·쓰기와 원자적 저장을 완성하라. 저장은 동일 디렉터리의 고유 임시 파일에 전체 내용을 쓴 뒤 가능한 범위까지 flush하고 안전하게 교체한다. 어느 단계에서 실패해도 기존 정상 원고가 유지되어야 한다. UTF-8만 허용하고 디코딩 실패, 접근 거부, 저장 공간 부족, 중간 교체 실패를 의미 있는 오류로 반환하라.

동일 문서의 저장 요청을 직렬화하고 각 요청에 save generation을 부여해 오래된 비동기 완료 결과가 최신 입력 상태나 해시를 덮어쓰지 못하게 하라. 저장 성공 후 SHA-256과 수정 시각을 계산하고 메타데이터 갱신 실패 시 파일과 색인을 다시 일치시키는 복구 전략을 구현하라. 임시 파일 잔재는 원본과 구분해 다음 실행에서 안전하게 정리하거나 복구 판단하라.

정상 왕복, 빈 문서, 한글·이모지, 큰 문서, 동시 저장 순서, 오래된 generation, 각 실패 지점 주입, 앱 재시작 후 임시 파일 처리를 자동 테스트하라. 실제 작품 파일을 사용하지 마라. 공개 API와 원자성 보장 범위를 보고하라.

### 여기까지

## 2-3. 작품 생성·목록·이름 변경

### 지금부터

작품 관리 기능의 도메인·데이터 계층을 구현하라. 새 작품 생성, 목록 조회, 마지막 작품 자동 열기, 이름 변경, 사용자 지정 순서 변경, 삭제 전 확인, 작품 전체 내보내기의 기반을 만든다. UI는 최소 연결만 하고 트랜잭션 안전성을 먼저 완성하라.

작품 생성은 임시 디렉터리에 전체 기본 구조와 메타데이터를 만든 뒤 모두 성공했을 때 정식 경로로 승격하라. 동일 이름과 정규화 충돌을 덮어쓰지 마라. 이름 변경 중 파일 이동과 SwiftData 갱신 중 하나가 실패하면 이전 상태로 복구하거나 재시작 시 완료할 수 있는 저널을 남겨라. 삭제는 즉시 제거하지 말고 후속 휴지통 정책과 연결 가능한 명시적 상태 전이를 사용하라.

프로젝트 생성, 재실행, 이름 변경, 순서 변경, 실패 롤백, 마지막 작품 복원을 임시 폴더와 인메모리 SwiftData로 테스트하라. 동일 작업을 두 번 요청해도 중복 프로젝트가 생기지 않아야 한다.

### 여기까지

## 2-4. 기존 Windows 작품 가져오기

### 지금부터

기존 Windows 작품 폴더 가져오기를 검사 단계와 실행 단계로 나눠 구현하라. 원본 폴더는 절대로 수정하지 않는다. 먼저 전체 트리를 읽어 UTF-8 여부, 예상 디렉터리 구조, 중복 프로젝트, 중복 화수, 잘못된 `N권`·`NNN화.txt` 이름, 숨김·알 수 없는 파일, 접근 불가 파일, 이름 정규화 충돌을 수집한 `ImportReport`를 만든다.

구형 작품에 `메인/플롯` 또는 `메인 스토리 틀` 폴더가 있더라도 iPad의 고정 카테고리로 승격하지 마라. 사용자 자료를 잃지 않도록 일반 사용자 폴더로 보존하고 가져오기 보고서에 표시하라. `백업/전환직전`도 삭제하거나 `백업/복원전`과 자동 병합하지 말고 레거시 백업으로 그대로 보존하라. 치명 오류와 경고를 구분하고 사용자가 확인한 뒤에만 가져오기를 시작한다.

가져오기는 앱 내부 임시 프로젝트로 복사하고 각 문서에 안정적인 UUID와 해시를 부여한 뒤 전부 성공할 때만 정식 프로젝트와 SwiftData 메타데이터를 등록하라. 중간 실패 시 복사본과 메타데이터를 모두 롤백하되 원본에는 손대지 마라. 정상 작품, 비 UTF-8, 중복 화, 잘못된 권, 읽기 실패, 대용량 트리, 중간 복사 실패를 fixture로 테스트하라. 파일 선택 UI는 얇게 연결하고 security-scoped resource 수명을 안전하게 관리하라.

### 여기까지

## 2단계 완료 조건

- Windows 호환 파일명 검증이 적용된다.
- UTF-8 TXT가 원자적으로 저장되고 실패 시 이전 원고가 남는다.
- 작품 생성·이름 변경·순서·마지막 작품 복원이 동작한다.
- Windows 작품 가져오기가 원본을 수정하지 않고 전부 성공 또는 전부 롤백된다.

---

# 3단계: 바인더와 권·화 관리

## 3-1. 바인더 데이터 제공 계층

### 지금부터

바인더 데이터 제공 계층과 8개 고정 최상위 항목을 구현하라. 순서는 원고, 캐릭터, 설정집, 메모장, 흐름 정리, 복선, 장소, 휴지통이며 원고는 항상 첫 번째다. `플롯`과 `메인 스토리 틀` 고정 항목은 만들지 마라. 사용자가 일반 문서 영역에 직접 만든 폴더와 TXT 문서는 정상적으로 표시하라.

파일 시스템 탐색 결과와 SwiftData의 UUID, 부모, 사용자 순서를 일관되게 결합하라. 최상위 항목은 즉시 표시하고 하위 폴더는 펼칠 때 비동기로 지연 로딩한다. `.txt` 확장자는 UI에서만 숨기고 빈 문서와 작성된 문서를 해시 또는 파일 크기로 구분한다. 펼침 상태와 사용자 순서를 저장·복원하고 외부 파일 변경 시 UUID를 무조건 새로 만들지 않는 재조정 규칙을 정의하라.

View가 직접 디렉터리를 열거하지 못하도록 `BinderRepository`와 ViewModel 경계를 둔다. 1,000화와 보조 문서 fixture에서 지연 로딩, 자연 정렬, 사용자 정렬, 펼침 복원, 확장자 숨김, 외부 변경 재조정을 테스트하라. 메인 스레드 파일 I/O가 없어야 한다.

### 여기까지

## 3-2. 원고 전용 규칙

### 지금부터

순수 Swift `BinderRuleService`를 구현하라. 원고 바로 아래에는 `N권` 폴더만, 권 안에는 `NNN화.txt`만 허용한다. 권 이름은 변경할 수 없고 화 이름의 숫자+`화` 접두사는 변경할 수 없다. 같은 화 번호를 중복 생성할 수 없다. 원고 문서를 다른 최상위 카테고리로 이동하거나 외부 문서를 원고로 이동할 수 없고 원고 최상위 순서는 변경할 수 없다. 원고 내부 기본 정렬은 숫자 자연 정렬이다.

규칙 판정은 UI와 파일 I/O에서 분리하고 생성, 이름 변경, 이동, 드롭 각각에 허용·거부 결과와 사용자용 이유를 반환하라. 숫자 앞자리 0, `1권`과 `01권`, `001화`, 1,000화 이상, Unicode 유사 문자, 확장자 대소문자, 중복 정규화 경계를 명시적으로 처리하라. 일반 문서 영역의 사용자 생성 폴더와 파일은 Windows 파일명 안전 규칙 외에는 불필요하게 제한하지 마라.

허용·거부 행렬을 표 기반 테스트로 작성하고 모든 원고 규칙을 최소 하나의 테스트에 연결하라.

### 여기까지

## 3-3. 일반 문서·폴더 명령

### 지금부터

바인더의 일반 문서·폴더 작업을 구현하라. 생성, 이름 변경, 사용자 순서, 드래그 앤 드롭 이동, 휴지통 이동, 우클릭·길게 누르기용 명령 모델을 제공한다. 모든 변경은 먼저 `BinderRuleService`와 `PathPolicy`로 검사하고 파일 시스템과 SwiftData가 어긋나지 않도록 트랜잭션 또는 복구 저널을 사용하라.

이름 충돌은 덮어쓰지 말고 안전한 대안을 제시한다. 이동과 이름 변경 후에도 `document_id`와 하위 문서 ID가 유지되고 상대 경로와 부모 ID만 갱신되어야 한다. 드롭 대상 미확정, 자기 하위로 폴더 이동, 프로젝트 루트 밖 이동, 열려 있는 문서 이동을 처리하라. 컨텍스트 메뉴는 기능 가능 여부와 거부 이유를 표시할 수 있게 한다.

정상 CRUD, ID 보존, 중간 실패, 중복 이름, 금지 이름, 잘못된 드롭, 대규모 하위 트리 이동을 자동 테스트하라.

### 여기까지

## 3-4. 새 권과 25개 화 자동 생성

### 지금부터

원고 최상위 컨텍스트 메뉴의 `새 권 추가`를 트랜잭션 방식으로 구현하라. 현재 존재하는 유효한 `N권` 중 가장 높은 번호+1을 새 권으로 한다. 시작 화는 `(권-1)*25+1`, 종료 화는 `권*25`, 파일명은 세 자리 숫자+`화.txt`로 계산한다. 권이 없으면 1권과 001~025화를 만든다.

고유 임시 폴더에 빈 UTF-8 TXT 25개와 각 문서 UUID·메타데이터를 준비하고 전부 성공한 경우에만 정식 `N권`으로 승격하라. 기존 권이나 화를 덮어쓰지 않고 일부 화만 남기지 마라. 생성 중 재진입을 막고 실패 시 임시 파일과 미완료 메타데이터를 정리한다. 성공하면 바인더 새로고침, 새 권 펼침, 첫 화 열기 의도를 결과로 반환한다.

서버 동기화 작업은 등록하지 마라. 대신 향후 일괄 생성 작업을 전달할 수 있는 protocol 호출 지점만 두고 현재는 로컬 no-op 구현을 사용하라. 0권, 1권·2권, 번호 공백, 잘못된 권 혼재, 기존 화 충돌, 25개 각 지점 실패, 동시 두 번 요청, 1,000화 이후 파일명 정책을 테스트하라.

### 여기까지

## 3-5. iPad 기본 화면 셸

### 지금부터

`NavigationSplitView`를 사용해 iPad 기본 화면을 구현하라. 가로에서는 바인더와 편집기를 함께, 세로에서는 편집기를 최대한 넓게 표시하고 바인더를 버튼으로 열고 닫는다. 상단에는 작품 전환, 검색, 좌우 분할, 작은 저장 상태 배지 영역을 두고 큰 하단 상태바는 만들지 마라.

바인더 ViewModel을 연결해 펼치기, 선택, 컨텍스트 메뉴, 길게 누르기, 가능한 환경의 우클릭, 드래그 앤 드롭을 제공한다. sheet는 추출과 백업 화면을 나중에 연결할 자리만 타입 안전하게 마련한다. 서버 충돌 화면 자리는 이 범위에서 만들지 않아도 된다. compact/regular width, 가로·세로, Split View, Stage Manager에서 레이아웃이 무너지지 않게 한다.

접근성 레이블, 키보드 포커스 이동, Dynamic Type의 합리적 범위를 적용하라. Preview와 UI 테스트에서 8개 최상위 순서, 바인더 열기·닫기, 문서 선택, 메뉴 노출, 상태 배지 위치를 확인하라.

### 여기까지

## 3단계 완료 조건

- 고정 최상위 항목이 8개이며 플롯 계열 고정 항목이 없다.
- 원고는 `원고/N권/NNN화.txt` 규칙을 따른다.
- 일반 영역에서는 사용자가 안전한 이름의 폴더와 TXT 문서를 만들 수 있다.
- 새 권 생성은 25개 화 전체 성공 또는 전체 롤백으로 처리된다.

---

# 4단계: 네이티브 편집기와 입력 규칙

## 4-1. UITextView 기반 편집기

### 지금부터

SwiftUI `TextEditor`에 의존하지 않고 `UIViewRepresentable`로 `UITextView`를 감싼 `iPadTextEditor`와 `UITextView` 하위 클래스 `SmartTextView`의 기반을 구현하라. TextKit을 통해 UTF-8 일반 텍스트, 커서·선택 범위, 스크롤, 현재 문서 검색 강조 확장 지점, 실행 취소·다시 실행, 복사·잘라내기·붙여넣기를 지원하라.

SwiftUI 상태와 UITextView 본문을 양방향 연결하되 매 update마다 전체 text를 재설정하지 마라. 외부 모델 버전과 실제 편집 변경을 구분하고 문서 전환 시 view 인스턴스를 불필요하게 폐기하지 않으며 first responder와 선택 범위를 안정적으로 관리하라. 문서 없음·빈 문서 안내는 원문과 분리된 placeholder로 구현한다. 저장과 스마트 입력은 editor 클래스에 직접 넣지 말고 경계만 둔다.

한글·이모지 왕복, 선택 유지, 동일 본문 재주입 방지, 문서 전환 포커스, Undo 기본 동작을 테스트하라.

### 여기까지

## 4-2. 한글 IME 안전장치

### 지금부터

`markedTextRange`가 존재하는 동안 한글 조합 중으로 판단하라. 조합 중 SwiftUI에서 UITextView 전체 본문을 다시 설정하거나 문서를 즉시 교체하지 마라. 문서 전환 요청은 조합 완료 후 실행되는 명시적 pending transition으로 관리하고 화 전환마다 SwiftUI `id`를 바꾸거나 UITextView 인스턴스를 새로 만들지 마라.

iPad 키보드 언어를 강제로 바꾸거나 Windows식 IME 강제 전환·키 재전송을 사용하지 않는다. 조합 중에도 로컬 저장용 스냅샷은 얻을 수 있지만 저장 완료가 화면 본문을 재주입해서는 안 된다. 소프트웨어 키보드, Magic Keyboard, Bluetooth 키보드, 빠른 문서 전환, background/foreground에서 첫 글자가 영문으로 바뀌거나 사라지지 않게 포커스 전이 상태 머신을 구현하라.

coordinator 단위 테스트와 UI 테스트를 작성하고 실제 iPad에서 한글 조합, 겹받침, 조합 중 전환, 빠른 이전·다음 화, 앱 복귀 직후 첫 글자를 검증하라. 실제 기기 미검증을 통과로 표시하지 마라.

### 여기까지

## 4-3. 스마트 쌍 입력

### 지금부터

스마트 텍스트 입력을 UIKit과 분리된 순수 Swift `TextRuleEngine`으로 구현하라. 입력 문자열, 선택 범위, 주변 문맥, 설정, 조합 상태를 받아 최소 편집 연산, 새 선택 범위, 처리 여부를 반환하게 한다. UTF-16 `NSRange`와 Swift String 인덱스 변환에서 한글·이모지 경계를 안전하게 처리하라.

입력 전 규칙으로 `'`, `"`, `[`, `(`, `{` 자동 쌍과 가운데 커서, 선택 영역 감싸기, 동일 닫는 기호 건너뛰기, 빈 쌍 Backspace 삭제, 닫는 기호 앞 Enter 시 기호를 넘은 뒤 줄바꿈을 구현하라. 규칙은 설정으로 끌 수 있고 서로 중복 적용되지 않아야 한다.

빈 선택, 다중 Unicode, 문서 시작·끝, 줄바꿈, 잘못된 NSRange, 조합 중 입력, Undo 단위 결과를 표 기반 테스트로 검증하라. `SmartTextView`에는 최소 어댑터만 연결하고 규칙 코드를 복제하지 마라.

### 여기까지

## 4-4. 조합 완료 규칙과 장면 전환선

### 지금부터

`TextRuleEngine`에 조합 완료 후 규칙과 장면 전환선을 추가하라. `...`는 `…`, `ㄴㄴ`은 `「」`, `ㄱㄱ`은 `『』`로 바꾸고 특수 인용부호의 가운데로 커서를 이동한다. 이 규칙은 `markedTextRange`가 완전히 사라진 뒤 최근 확정 입력 범위에만 적용한다.

별표 한 번 입력 시 정확히 `\n\n * * *\n\n`을 삽입한다. 전환선 바로 뒤 Backspace 또는 바로 앞 Delete는 전체 문자열을 삭제한다. 삽입, 삭제, 말줄임표, 특수 인용부호 변환은 각각 한 번의 Undo로 되돌아가야 한다. 명시적인 단일 `*` 입력 사건만 처리하고 붙여넣기나 기존 텍스트를 오작동으로 바꾸지 마라.

IME 이벤트, 붙여넣기, 빠른 입력, 전환선 양쪽 삭제, Undo/Redo, 설정 off를 자동 테스트하고 실제 iPad 한글 키보드로 검증하라.

### 여기까지

## 4-5. 편집기 사용성과 화면 설정

### 지금부터

실시간 글자 수, 문서별 커서·선택·스크롤 위치 기억, 안전한 자동 포커스, 타자기 스크롤, 글꼴, 글자 크기, 줄 간격, 좌우·상하 여백을 구현하라. 원고는 TXT이므로 굵게, 색상, 밑줄 같은 부분 서식은 저장하지 않고 시각 설정만 앱 설정으로 보존한다.

TextKit 레이아웃과 inset, paragraph style 적용 시 본문 문자열이나 Undo 스택이 바뀌지 않아야 한다. 글자 수 기준을 명시하고 큰 문서에서 매 키 입력마다 불필요한 전체 재계산을 하지 않도록 측정하라. 타자기 스크롤은 선택 이동과 검색 결과 이동에서도 예측 가능해야 하며 Reduce Motion을 존중한다.

설정 저장·재실행 복원, 한글·이모지 글자 수, 커서 범위 보정, 6,000자 이상 입력 성능, 설정 변경 전후 TXT 바이트 동일성을 테스트하라.

### 여기까지

## 4-6. 좌우 듀얼 에디터

### 지금부터

기본은 단일 편집기이며 상단 버튼으로 두 문서를 나란히 여는 좌우 듀얼 에디터를 구현하라. 왼쪽 문서, 오른쪽 문서, 활성 편집기, 분할 여부를 저장·복원하고 활성 쪽에만 강조 테두리를 표시한다. 바인더에서 선택한 문서는 활성 편집기에 연다.

같은 `document_id`를 양쪽에서 동시에 편집하지 못하게 한다. 이미 반대쪽에 열린 문서를 선택하면 그쪽을 활성화하거나 안내한다. 분할 선호와 실제 표시를 분리한다. 세로·좁은 폭에서는 단일 편집기와 비활성화된 좌우분할 버튼을 표시하되 분할 선호와 양쪽 상태는 보존하고, 가로·충분한 폭으로 돌아오면 자동 복원한다. iPadOS 26 창 최소화·최대화·크기 변경은 화면 방향 전환과 독립적으로 처리해 세로에서 분할 표시나 버튼 상태가 반복 전환되지 않게 한다. 사용자가 분할을 명시적으로 닫을 때만 최신 스냅샷을 저장 coordinator에 전달하고 저장 실패 시 닫힘을 강행하지 않는다. 양쪽 편집기는 독립 커서, Undo, 스크롤 상태를 유지한다.

동일 문서 방지, 활성 전환, 상태 복원, 폭 변경, 저장 실패, 빠른 좌우 입력을 테스트하라. iPad Pro 11-inch (M5) 실기기 iPadOS 26.x에서 가로 분할 → 세로 → 가로 자동 복원과 세로 창 최소화·최대화 중 분할 표시·버튼 상태 안정성을 검증하고, 미검증이면 통과로 기록하지 마라.

### 여기까지

## 4-7. 편집기 중심 외장 키보드 명령

### 지금부터

외장 키보드 명령의 공통 command 모델을 구현하라. 이 단계에서는 `Command+S` 저장 요청, `Command+Z` Undo, `Command+Shift+Z` Redo, `Command+B` 바인더 토글, `Command+\\` 듀얼 전환, `Command+Left/Right` 이전·다음 화를 연결한다. 검색 명령은 7단계에서 실제 검색 기능과 연결한다.

메뉴 제목과 discoverability를 제공하고 텍스트 입력 규칙과 단축키가 충돌하지 않게 한다. iPad와 향후 Catalyst가 같은 command 모델을 재사용하게 하라. 실제 Magic Keyboard 또는 Bluetooth 키보드 검증 여부를 보고하라.

### 여기까지

## 4단계 완료 조건

- 한글 조합 중 본문 재주입이나 즉시 문서 전환이 없다.
- 빠른 화 전환과 앱 복귀 후 첫 글자가 유실되거나 영문으로 바뀌지 않는다.
- 스마트 입력 규칙이 각각 한 번의 Undo로 취소된다.
- 듀얼 편집기에서 같은 문서를 동시에 편집하지 않는다.

---

# 5단계: 로컬 자동 저장과 상태 표시

## 5-1. 자동 저장 coordinator

### 지금부터

자동 저장 coordinator를 구현하라. 마지막 입력 후 기본 800ms 디바운스, 다른 화 이동 전, 프로젝트 변경 전, 분할 닫기 전, 앱 inactive/background 전, `Command+S`에 즉시 저장한다.

현재 단계의 저장 순서는 텍스트 스냅샷 → `LocalDocumentStore` 원자 저장 → SHA-256·로컬 메타데이터 갱신 → 필요 시 백업 요청이다. Supabase 업로드와 실제 영구 동기화 큐 등록은 실행하지 마라. 향후 동기화 계층에 저장 완료 사건을 전달할 protocol 지점만 두고 로컬 no-op 구현을 주입한다.

각 문서의 save generation과 dirty generation을 비교해 느린 이전 저장이 최신 상태를 저장됨으로 표시하거나 덮어쓰지 못하게 하라. 양쪽 편집기의 저장은 독립적이되 같은 문서는 actor에서 직렬화한다. 문서 전환은 최신 로컬 저장 성공 후 진행하고, 실패 시 현재 텍스트를 메모리에 보존하면서 재시도와 별도 파일 저장 선택을 제공할 수 있게 한다.

가상 시계로 800ms, 연속 타이핑, 즉시 저장, 느린 저장 역전, 전환 중 입력, 듀얼 편집기, background 제한 시간을 테스트하라. 서버가 없는 상태는 로컬 저장 실패로 보이면 안 된다.

### 여기까지

## 5-2. 로컬 저장 상태 배지

### 지금부터

현재 동작하는 저장 상태를 편집 중, 로컬 저장 중, 로컬 저장됨, 로컬 저장 실패·재시도로 구분하고 상단의 작은 배지로 구현하라. `저장됨` 하나로 모든 의미를 뭉뚱그리지 마라. 상태 우선순위와 문구를 reducer 또는 state machine 한곳에서 결정하고 View에 조건문을 흩뜨리지 마라.

서버 동기화 중, 서버 저장됨, 오프라인 저장됨, 동기화 대기, 다른 기기 편집, 자동 병합, 충돌 상태는 향후 확장 가능한 enum 또는 별도 모델 경계만 마련하되 실제 상태처럼 화면에 표시하지 마라. 설정되지 않은 서버를 오류로 표시하지 않는다.

배지는 색상만으로 상태를 전달하지 않고 VoiceOver 레이블과 아이콘·문구를 함께 제공한다. 입력 → 로컬 저장 중 → 로컬 저장됨 → 다시 입력, 저장 실패 → 재시도 전이를 테스트하고 오래된 비동기 이벤트가 최신 배지를 되돌리지 않는지 확인하라.

### 여기까지

## 5단계 완료 조건

- 800ms 자동 저장과 모든 즉시 저장 조건이 동작한다.
- 로컬 저장 성공 전 문서 전환이나 분할 종료가 원고를 잃지 않는다.
- 로컬 상태와 아직 구현하지 않은 서버 상태가 혼동되지 않는다.

---

# 6단계: 백업·복원·휴지통

## 6-1. 자동 백업과 보존 정책

### 지금부터

자동 백업 저장소와 보존 정책을 구현하라. 일정 시간 이상 편집, 다른 화 이동, 문서 닫기, 과거 버전 복원 전, 구조 변경 전에 백업을 요청할 수 있게 한다. 서버 충돌과 대량 서버 동기화 사건은 향후 호출 가능한 사유 enum만 두고 현재 발생시키지 않는다.

백업 원문은 UTF-8 TXT이며 `백업/자동저장`, `백업/복원전`, `백업/충돌`을 분리한다. `BackupSnapshot`에 문서 ID, 원래 경로, 시각, 해시, 사유, 보관 지정 여부를 기록한다. 같은 해시의 불필요한 연속 백업을 줄이되 중요한 사건 백업은 생략하지 않는다.

최근 24시간 최대 30개, 최근 30일 하루 1개, 보관 지정 영구 유지 정책을 결정론적으로 구현하라. 삭제 실패가 원고 저장을 실패로 만들지 않되 상태와 오류는 남겨라. 경계 시각, 30개 초과, 하루 대표본, 보관 지정, 중복 해시, 삭제 실패, 5,000개 백업 정리 성능을 테스트하라.

### 여기까지

## 6-2. 백업 이력·미리보기·diff·복원

### 지금부터

문서별 백업 목록에 날짜, 시각, 사유, 보관 상태를 표시하고 읽기 전용 미리보기를 제공하라. 큰 텍스트에서도 UI를 막지 않는 줄 또는 문단 기반 diff를 비동기로 계산하고 추가·삭제·변경을 접근성 있게 표현하라.

복원 순서는 현재 문서 즉시 로컬 저장 → 현재본을 `백업/복원전`에 강제 백업 → 선택 백업의 UTF-8·document_id·경로·해시 검증 → 원자적 원고 교체 → 새 해시와 로컬 변경 상태 갱신이다. 실제 서버 revision이나 동기화 큐는 처리하지 말고 향후 복원 완료 사건을 전달할 protocol만 호출한다.

어느 단계든 실패하면 기존 현재본을 보존하고 부분 복원 상태를 남기지 마라. 백업 삭제와 보관 지정도 안전하게 구현한다. 동일본, 큰 차이, 손상 백업, 잘못된 문서, 복원 전 백업 실패, 교체 실패, 성공 후 재시작을 테스트하라.

### 여기까지

## 6-3. 휴지통

### 지금부터

일반 문서 삭제는 즉시 영구 제거하지 않고 `휴지통`으로 원자적으로 이동한다. 원래 상대 경로, 부모 ID, 사용자 순서, 삭제 시각을 기록한다. 열린 문서는 저장 후 닫고 이름 변경·이동 뒤에도 `document_id`를 유지한다. 원고 규칙과 듀얼 편집기 상태를 함께 처리하라.

원래 위치 복원과 사용자가 고른 위치 복원을 제공한다. 이름 충돌 시 덮어쓰지 않고 안전한 새 이름을 제안하며 원고 영역 복원은 `BinderRuleService`를 통과해야 한다. 개별 영구 삭제와 휴지통 비우기는 명시적 확인 후 수행하고 실패한 항목과 성공한 항목을 구분해 보고한다.

향후 서버 tombstone을 전달할 protocol 경계만 두고 현재 서버 삭제 작업은 만들지 않는다. 다른 기기 수정과 삭제 충돌 UI도 구현하지 않는다. 원래 위치 복원, 부모 삭제 상태, 이름 충돌, 원고 규칙 위반, 열린 문서, 영구 삭제 부분 실패, 앱 재시작을 테스트하라.

### 여기까지

## 6-4. 백업 설정 통합

### 지금부터

설정 화면에 자동 백업 사용 여부, 최대 보관 수, 보존 기간을 추가하고 `BackupPolicy`와 연결하라. 손상되거나 범위를 벗어난 설정값은 안전한 기본값으로 복구한다. 설정 변경이 기존 원고와 보관 지정 백업을 삭제하지 않게 한다.

설정 저장, 재실행 복원, 정책 변경 후 정리 대상 계산, 자동 백업 off 상태에서도 복원 전 강제 백업이 유지되는지를 테스트하라.

### 여기까지

## 6단계 완료 조건

- 자동 백업과 보존 정책이 재실행 후에도 일관되게 동작한다.
- 미리보기와 diff가 현재 원고를 바꾸지 않는다.
- 복원 전에 현재본이 별도 보존된다.
- 휴지통 복원은 기존 파일을 덮어쓰지 않는다.

---

# 7단계: 검색·TXT 추출·로컬 마일스톤 검증

## 7-1. 현재 문서 검색

### 지금부터

현재 문서 검색을 TextKit 기반으로 구현하라. 검색어 변경 시 결과를 즉시 갱신하고 결과 수, 현재 결과 번호, 다음·이전 이동, 현재 결과 강조를 제공한다. `Command+F`로 열고 Escape 또는 닫기 후 편집 포커스를 안전하게 복원한다.

Swift String과 TextKit의 UTF-16 범위를 정확히 변환해 한글, 조합 문자, 이모지에서도 잘못된 선택이나 크래시가 없어야 한다. 빈 검색어, 대소문자 정책, 겹치는 결과 정책, 문서 변경, 편집으로 결과 위치가 바뀌는 상황을 명시하라. 강조는 원본 TXT에 저장하지 않고 임시 layout 속성으로만 적용한다.

문서 시작·끝, 여러 줄, 한글·이모지, 0건, 수천 건, 검색 중 편집, 좌우 편집기 독립 검색을 테스트하라.

### 여기까지

## 7-2. 프로젝트 전체 검색

### 지금부터

모든 TXT를 비동기로 읽어 파일별 결과를 그룹화하고 각 일치 주변 문장을 생성하는 프로젝트 전체 검색을 구현하라. 결과를 선택하면 해당 `document_id`를 활성 편집기에 열고 일치 범위로 이동한다. 검색 진행률과 취소를 제공하고 검색 중에도 편집과 자동 저장이 끊기지 않아야 한다.

초기 버전은 파일 직접 검색을 사용하고 1,000화 실측에서 병목이 확인되지 않으면 색인을 추가하지 마라. 디스크 변경 중 파일은 일관된 스냅샷 또는 재시도 정책을 사용한다. 읽기 실패 파일을 전체 검색 실패로 숨기지 말고 부분 결과와 오류 목록으로 보고한다. 휴지통과 백업은 기본 제외하고 일반 보조 문서 영역은 포함한다.

1,000화×약 6,000자와 보조 문서 500개 fixture, 취소, 파일 삭제 경쟁, 비 UTF-8 오류, 결과 이동, 메인 스레드 비차단을 테스트·측정하라. `Command+Shift+F`를 전체 검색에 연결한다.

### 여기까지

## 7-3. TXT 전체·범위 추출

### 지금부터

원고 최상위 컨텍스트 메뉴의 `TXT 추출`을 구현하라. 설정 시트에서 전체 원고 또는 시작 화·종료 화를 선택하고 빈 화 제외, 화 제목 포함, 공백 포함 300자 미만 화에서 추출 중단, 저장 위치 옵션을 제공한다.

300자 미만 중단 옵션은 전체 추출에서 기본 켜짐이며 사용자가 끌 수 있다. 화 번호순으로 검사하다가 공백을 포함한 본문이 300자 미만인 화를 처음 발견하면 그 화를 포함하지 않고 즉시 추출을 끝낸다. 이후 화도 모두 포함하지 않는다. 범위 지정 추출에는 이 중단 옵션을 기본 적용하지 않는다.

권 폴더와 무관하게 파일명에서 화 번호를 추출해 숫자 순으로 정렬한다. 화 제목 옵션 사용 시 `[001화]`, 빈 줄, 본문 형식의 UTF-8 TXT를 만든다. 파일명은 `작품명(1~250화).txt` 또는 `작품명(50~100화).txt` 형식을 사용한다. 출력 파일명에도 Windows 금지 문자와 예약 이름 검사를 적용하라.

완료 보고에는 지정 범위, 실제 포함 화수, 실제 마지막 포함 화, 존재하지 않는 화, 빈 문서 제외, 300자 미만으로 중단된 최초 화, 저장 위치를 포함한다. `FileDocument` 또는 `Transferable`, `fileExporter`, 공유 시트를 연결한다. 초기 TXT 전용 결정은 7-5의 사용자 확정 결정(2026-07-26)에 따라 소장용 TXT·PDF 제공으로 대체한다.

번호 공백, 다른 권에 잘못 배치된 화, 중복, 빈 문서, 정확히 299자·300자·301자, 공백 포함 계산, 중단 옵션 off, 범위 추출, 한글·이모지, 취소, 쓰기 실패를 테스트하고 출력 바이트와 순서를 검증하라.

### 여기까지

## 7-4. 설정·단축키·화면 상태 최종 통합

### 지금부터

설정 화면과 외장 키보드 명령, 화면 상태 복원을 통합하라. 편집 설정은 글꼴, 크기, 줄 간격, 좌우·상하 여백, 타자기 스크롤, 자동 저장 대기시간, 스마트 입력 세부 옵션을 제공한다. 백업 설정은 사용 여부, 최대 보관 수, 기간을 제공한다. 동기화 설정은 구현하지 말고 `Windows v2 확정 후 제공 예정`으로 명확히 표시하거나 이번 로컬 빌드에서 숨긴다.

`Command+S`, `Command+F`, `Command+Shift+F`, `Command+Z`, `Command+Shift+Z`, `Command+B`, `Command+\\`, `Command+Option+Left/Right`를 실제 기능에 연결한다. 마지막 작품, 좌우 문서, 활성 편집기, 분할 여부, 바인더 너비, 펼친 폴더, 커서 위치를 재실행 후 복원한다. 사라진 문서는 안전한 대체를 선택한다.

모든 단축키, 설정 저장, 손상 값 기본화, 상태 복원, 없는 문서, 실제 키보드 검증표를 테스트하고 보고하라.

### 여기까지

## 7-5. 로컬 전용 마일스톤 감사와 작품 전체 내보내기

### 지금부터

새 기능을 넓히기 전에 1~7단계 요구사항 추적표를 실제 코드와 테스트 증거에 대조하라. 미완료, 부분 구현, 가짜 placeholder, 무시된 오류, 메인 스레드 파일 I/O, 500줄 초과 파일, View의 직접 저장 접근을 찾아 수정하라.

네트워크와 Supabase가 전혀 없어도 작품 생성, Windows 작품 가져오기, 8개 고정 바인더 항목, 사용자 생성 폴더, 새 권 25화, 한글 안전 편집, 스마트 입력, 듀얼 편집, 자동 저장, 백업·복원, 휴지통, 검색, TXT 추출이 끝까지 동작하는 통합 테스트를 실행하라. 앱 강제 종료 직전 입력, 저장 실패, 손상 백업, 빠른 화 전환에서도 원문이 보존되어야 한다.

사용자 확정 결정(2026-07-26)에 따라 내보내기는 백업·복원과 분리된 개인 소장용 UTF-8 TXT·PDF로 제공한다. 작품 폴더 구조, UUID·경로 manifest와 재가져오기는 구현하지 않는다. 내보내기 직전에 편집 원고 저장을 완료하고, 실패나 취소 시 원본과 기존 출력본을 그대로 보존한다. 작품 삭제 확인과 마지막 작품 대체 동작도 끝까지 연결하라.

모든 테스트와 실제 iPad 검증 여부를 표로 보고하고 미통과 항목을 해결하라. 외부 조건 때문에 해결할 수 없으면 재현 절차와 차단 여부를 기록한다.

### 여기까지

## 7-6. Supabase 연결 전 로컬 성능 기준선

### 지금부터

Supabase를 연결하기 전 로컬 성능 기준선을 만들고 측정하라. 테스트 데이터 생성기는 권 40개까지, 원고 1,000화×약 6,000자, 일반 보조 문서 500개, 백업 5,000개를 실제 원고와 분리된 임시 영역에 만든다. 릴리스 빌드와 실제 iPad에서 프로젝트 진입, 최상위 바인더 표시, 권 펼침, 화 전환, 연속 타이핑, 원자 저장, 전체 검색, 백업 목록의 시간·메모리·메인 스레드 정지를 계측하라.

목표는 프로젝트 진입 2초 이내, 화 전환 체감 지연 없음, 저장·검색 중 타이핑 유지, 바인더 즉시 반응, 메모리 부족 종료 방지다. 먼저 측정하고 병목이 확인된 경우에만 하위 폴더 지연 로딩을 보완하고 필요한 부모 폴더 캐시만 추가한다. 검색 색인과 전역 캐시는 수치 근거 없이 도입하지 마라.

측정 환경, 데이터 크기, median/p95, 메모리 최고치, Instruments 근거, 변경 전후 수치를 `Docs/PerformanceBaseline.md`에 기록하라. 시뮬레이터 수치를 실제 iPad 수치처럼 보고하지 마라.

### 여기까지

## 7단계 완료 조건

- 현재 문서와 프로젝트 전체 검색이 편집을 막지 않는다.
- TXT 전체·범위 추출이 확정된 300자 중단 규칙을 따른다.
- TXT·PDF 내보내기가 같은 범위·내용 규칙을 따르고 실패·취소 시 원본과 기존 출력본을 보존한다.
- 네트워크와 Supabase 없이 1~7단계 전체 흐름이 동작한다.
- 1,000화 기준 로컬 성능 측정 결과가 기록된다.

---

# 이 문서에서 구현하지 않는 후속 범위

다음 내용은 Windows v2 확정 후 별도 코딩 프롬프트에서 진행한다.

- `supabase-swift` 연결과 인증
- Supabase SQL migration과 RLS
- `commit_document` RPC
- 기기 UUID 등록과 edit lease
- Realtime 알림
- 실제 영구 동기화 큐
- `SyncEngine`
- Base·Local·Remote 3방향 병합
- 충돌 원본 보존과 해결 화면
- metadata revision과 tombstone
- Windows 프로그램 v2 전환
- 교차 기기 계약 및 수용 테스트

# 최종 중단 지점

7단계의 모든 자동 테스트와 가능한 실제 iPad 검증이 끝나면 개발을 중단하고 결과를 보고한다. Windows v2와 Supabase 규약이 확정되기 전에는 후속 서버 단계로 넘어가지 않는다.
