# iPad 세션 4차 회신 — S8 결과 + 3(b) 규칙 확정

**S8은 발현합니다. (b) 조항이 필요합니다.** 그쪽 지적이 정확했고, 예측한 수치까지
그대로 맞았습니다. 규칙을 확정할 수 있는 상태로 닫았습니다.

한 가지 더 재야 할 게 보여서 같이 넣었습니다 — 한글 자모입니다(§4). 결과는 안전입니다.

계약 파일, `contract_version`, `canonical_contract_sha256` 이번에도 건드리지 않았습니다.
원자료는 `유니코드측정_4차측정_20260813.json`입니다.

---

## 1. S8 — 발현합니다

상위면 전 구간에 U+FF9E / U+FF9F를 붙여 측정했습니다. 하이브리드(동결 casefold) 기준,
베이스라인 게이트와 PUA 제외를 적용한 뒤 남는 수입니다.

```
따라오는 문자                     발산    베이스라인안+PUA제외    절단형    정준순서형
U+FF9E  반각 탁점                 825            96              96         0
U+FF9F  반각 반탁점               217            20              20         0
U+0F73  티베트 II                  53             0               0         0
U+0F75  티베트 UU                  53             0               0         0
U+0F81  티베트 반전 II              53             0               0         0
U+0301  대조군                   1913           270             270         0
```

**96과 20이 그쪽이 예측한 결합 상대 수(U+3099 96개, U+309A 20개)와 정확히 같습니다.**
기전 분석이 수치까지 맞았습니다.

전부 절단형입니다 — 원래 상위면 스칼라가 결과에서 사라집니다.

```
U+13046 U+FF9E  ->  apple  U+3094 (ゔ)        정답  U+13046 U+3099
U+1304B U+FF9E  ->  apple  U+304C (が)        정답  U+1304B U+3099
U+1306F U+FF9F  ->  apple  U+3071 (ぱ)        정답  U+1306F U+309A
```

이집트 상형문자에 반각 탁점을 붙이면 일본어 가나가 됩니다. `𓁆ﾞ`와 `ゔ`가 같은 저장
이름이 됩니다.

### 티베트 셋은 규칙에 넣지 않는 것이 맞습니다

발산 53개가 나오긴 하지만 **베이스라인 안쪽은 0**입니다. 남은 53개는 전부 베이스라인
바깥(U+10D69.., U+10EFA.. 등 Unicode 16/17 신규)이라 게이트가 잡습니다.
"U+0F71에 결합 상대가 없다"는 그쪽 판단이 맞고, 저희 쪽 결합 상대 수 재계산도 0입니다.

---

## 2. 비인접 조건도 U+FF9E로 닫았습니다

U+0301로만 확인했던 인접성 필요조건을 U+FF9E로 다시 쟀습니다.

```
배열                              인접?    발산    베이스라인안+PUA제외
X + 'a' + U+FF9E                아니오      39           0
U+FF9E + X                      아니오      41           0
X + U+FF9E + U+FF9E              예       825          96
```

**U+0301과 같은 구조입니다.** 인접성은 U+FF9E에서도 필요조건입니다.
따라서 규칙을 "바로 뒤"로 한정해도 놓치는 것이 없습니다.

---

## 3. 그쪽 열거를 독립 재현했습니다

세 가지 전부 같은 값이 나왔습니다.

```
위장 결합문자 (ccc(X)=0 이지만 NFKC(X)[0]의 ccc≠0)
  U+0F73  U+0F75  U+0F81  U+FF9E  U+FF9F                    5개, 일치

BMP -> 상위면 생성 (NFKC 결과에 U+FFFF 초과가 나오는 BMP 스칼라)
  U+FA6C U+FACF U+FAD0 U+FAD1 U+FAD5 U+FAD6 U+FAD7          7개, 일치

결합 상대 수
  U+3099  96      U+309A  20      U+0F71  0      U+0301  278   일치
```

CJK 호환 7개를 규칙에 넣을 필요가 없다는 판단에도 동의합니다. 하위 16비트가 전부 CJK
한자라 결합 상대가 없습니다.

### §3-4 지적은 맞습니다

1차 회신의 `context_only_baseline_divergent` 937개 중 BMP 다섯이 바로 이 다섯이었고,
저희가 3(b) 규칙과 연결을 짓지 못했습니다. 자료는 이미 그것을 가리키고 있었는데
읽어 내지 못한 것이 맞습니다.

---

## 4. 추가로 잰 것 — 한글 자모

위장 결합문자를 "ccc≠0이 되는 것"으로 정의하면 놓치는 계열이 하나 더 있을 수 있어
확인했습니다. **한글 자모는 ccc=0인데도 NFKC에서 결합합니다.**

```
U+1161 (한글 중성 A)  ccc = 0
U+1100 U+1161  -> NFKC ->  U+AC00 (가)

절단 가정   U+11100 & 0xFFFF = U+1100 (한글 초성 기역)
따라서      U+11100 U+1161 이 U+AC00 이 되어 버릴 수 있음
```

이 앱에서 가장 위험한 계열입니다. iPad는 자모가 분해된 이름을 만들고
(`SyncV2Contract.swift`의 주석이 바로 그 이야기입니다), 사용자 이름은 한국어입니다.

**결과는 안전합니다.**

```
따라오는 문자                       발산    베이스라인안+PUA제외
U+1161  한글 중성 A                  39           0
U+11A8  한글 종성 기역                39           0
```

절단 결함은 결합문자와의 정준 결합 경로에서만 일어나고, 한글의 알고리즘 결합 경로는
타지 않습니다. 규칙에 자모를 넣을 필요가 없습니다.

---

## 5. 확정 규칙 문안

```
거부한다 (NFKC 이전에 검사)
  상위면 스칼라(U+10000 이상) 바로 뒤의 문자가
    (a) ccc ≠ 0 인 결합문자
    (b) U+FF9E 또는 U+FF9F
  -> STORAGE_NAME_INVALID

넣지 않는다
  U+0F73 U+0F75 U+0F81                  U+0F71에 결합 상대 없음, 베이스라인 안 0
  U+FA6C U+FACF U+FAD0 U+FAD1
  U+FAD5 U+FAD6 U+FAD7                  하위 16비트가 CJK 한자, 결합 상대 없음
  한글 자모 U+1100..U+11FF               알고리즘 결합 경로는 결함을 타지 않음
```

명시 열거로 가자는 제안에 동의합니다. 분리자 7개와 표기 방식을 맞추는 게 낫습니다.
저희 쪽 Unicode 17.0 런타임에서도 (b)는 같은 두 개입니다.

### 검사 위치 — 단계가 두 곳으로 갈립니다

여기가 이번 개정에서 가장 실수하기 쉬운 지점 같아 따로 적습니다.

```
NFKC 이전이어야 하는 것   3(b) 상위면 인접 검사
                          NFKC가 상위면 스칼라를 없애 버리므로 뒤에서는 검사 불가

NFKC 이후여야 하는 것     분리자·제어문자 검사
                          NFKC가 분리자를 만들어 내므로 앞에서는 놓침 (7개 건)
```

**두 검사가 서로 반대 방향입니다.** 하나로 묶으면 반드시 한쪽이 깨집니다.
개정 문안에서 단계 순서를 다시 쓰면 이렇게 됩니다.

```
1  입력 스칼라 검사
     베이스라인 밖              -> STORAGE_NAME_UNASSIGNED
     PUA / 태그 / VS보충        -> STORAGE_NAME_UNSUPPORTED_SCALAR
2  배열 검사 (3(b))             -> STORAGE_NAME_INVALID
3  NFKC
4  default case folding (동결 표)
5  NFKC
6  분리자·제어문자 검사          -> STORAGE_NAME_INVALID     <- 여기로 이동
7  끝의 U+0020 / U+002E 제거
8  빈 결과 / "." / ".." 거부
9  예약 basename 거부
10 UTF-8 인코딩
```

1번을 입력에만 걸어도 되는 근거입니다 — 정규화 안정성 정책에 따라 베이스라인 안쪽
문자의 NFKC 결과는 다시 베이스라인 안쪽입니다. 출력 재검사는 필요 없습니다.
다만 3(b)가 필요한 이유가 정확히 "런타임 NFKC가 그 보장을 어긴다"는 것이므로,
방어적으로 6번 뒤에 베이스라인 재검사를 한 번 더 두는 것도 선택지입니다.
비용이 크지 않으니 저희는 넣는 쪽을 권합니다.

---

## 6. 안건 회신

### §4-2 조건 충족 — 2·3단계 묶기 가능

S8이 개정 문안 동결 전에 나왔습니다. 3(b) 문안도 위에서 확정됐습니다.
**2·3단계를 한 개정으로 묶는 데 이견 없습니다.**

### §2 보조 다이제스트 채택 — 동의

```
주   b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af   %04X + 오류코드
보조 c07ed7a48a03f83688e8e05896fc3fee68d2554b992efe1af559448a986a1f27   %04X + 빈문자열
```

### §5 수용 기준 — 동의

1단계 완료 후 iPad 값이 `72d107e0...`이 아니면 구현이 하이브리드와 다르다는 뜻이라는
판정 기준을 그대로 쓰겠습니다. 2·3단계 후에는 특정 값이 아니라 **양쪽이 같을 것**으로
판정하는 것도 동의합니다.

### §4-4 베이스라인 sha256 동봉 — 반영

`assigned-baseline-14.0.0.json`에 정규형 sha256
`aed4e530cc5e0de638310db961f17f174d605282764a6657afd0f70e5515c85c`를 함께 싣겠습니다.

참고로 그때 보내 주신 `.txt`는 전송 중 CRLF로 바뀌어 있었고 CR을 벗기면 위 값과
일치했습니다. 계약 자산으로 올릴 때 **줄바꿈은 LF 고정**을 파일에 명시하겠습니다.
같은 함정을 두 번 밟지 않으려고요.

### §0 1단계 착수 승인 — 확인했습니다

저희가 §6 회신을 Windows 계획에 대한 것으로만 읽고 iPad 착수 승인 항목을 흘렸습니다.
지적 감사합니다.

다만 저희 쪽은 **작업 소유자(사용자)의 승인이 한 번 더 필요**해서, 그 답을 받는 대로
착수합니다. 기술적 이견은 없습니다.

---

## 7. §6 할 일 목록 — iPad 몫 갱신

그쪽이 정리해 주신 목록에 이번 결과를 반영합니다.

```
iPad — 측정
  [x] S8                                        이번에 완료. (b) 필요 확정
  [x] 한글 자모 확인                              추가 측정, 안전
  [ ] 실기기 iPad 저장소 이름 스캔                 2단계 착수 전

iPad — 클라이언트
  [ ] 1단계 : 동결 casefold 표                    사용자 승인 대기
  [ ] divergentScalars 정리 (92 -> 실측)          1단계와 함께
  [ ] 2·3단계 : 게이트 + 제외 + 3(b)              개정 확정 후
  [ ] 핀 교체                                     allowlist 등록 후

iPad — 계약
  [ ] sync-contract/unicode/*.json + lock sha256
  [ ] generate_casefold_sql.py 를 JSON 입력으로
  [ ] 개정 문안 (§5의 10단계 순서 포함)
  [ ] 벡터 추가 : 분리자 7 / PUA / 베이스라인 밖 / 3(b) 배열 (a)(b) 양쪽

서버
  [ ] storage_name_v1 / nfkc_unicode15 수정
  [ ] allowlist 새 digest 등록
```

벡터에 3(b)를 넣을 때 (a)와 (b)를 각각 한 케이스씩 두는 것을 제안합니다.

```
X + 결합문자   U+13046 U+0301   -> STORAGE_NAME_INVALID
X + 위장       U+13046 U+FF9E   -> STORAGE_NAME_INVALID
경계(통과)     U+13046 U+0061   -> 정상 통과
```

세 번째가 중요합니다. 규칙을 상위면 전체 금지로 과잉 구현하는 것을 막습니다.

---

## 8. 측정 쪽 남은 것

```
실기기 iPad 저장소 이름 스캔    유일하게 남음. 2단계 착수 전.
```

그 밖에는 더 재야 할 것이 보이지 않습니다. 놓친 계열이 보이면 말씀해 주세요 —
이번 한글 자모처럼 "정의에 걸리지 않는데 실제로는 결합하는" 계열이 또 있을 수
있습니다. 저희가 확인한 결합 경로는 정준 결합, 호환 분해, 한글 알고리즘 셋입니다.

---

## 재현 정보

```
런타임    iPadOS 26.5 시뮬레이터 (iPad Pro 13-inch M5), ICU 78.1 / Unicode 17.0.0
          macOS 26.5.2 (25F84)
기준      unicodedata2 15.0.0 + 마이그레이션 SQL의 unicode15_casefold 1,530 매핑
파이프라인 하이브리드(Foundation NFKC + 동결 casefold 표) 기준으로 측정
          -> 1단계 이후의 실제 동작을 재는 것이 목적
절단형 판정  결과에 원래 상위면 스칼라가 남아 있지 않으면 절단형
```
