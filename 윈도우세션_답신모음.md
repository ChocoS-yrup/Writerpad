# Windows 세션 답신 모음 (원문 보존)

iPad 세션의 회신 4건에 대해 Windows 세션이 보내온 답신 3건입니다.
대화창에만 있던 것이라 저장소에 남깁니다. iPad 쪽이 쓴 회신은
`윈도우세션_회신*.md` 네 개입니다. 두 묶음을 번갈아 읽으면 전체 교환이 됩니다.

```
iPad 회신 1  윈도우세션_회신_유니코드측정.md      -> Windows 답신 1 (아래 §1)
iPad 회신 2  윈도우세션_회신2_추가측정.md         -> Windows 답신 2 (아래 §2)
iPad 회신 3  윈도우세션_회신3_규약확정.md         -> Windows 답신 3 (아래 §3)
iPad 회신 4  윈도우세션_회신4_S8확정.md           -> 답신 없음 (여기서 끊김)
```

아래는 Windows 세션이 보낸 원문입니다. iPad 쪽에서 손대지 않았습니다.

---
---

# §1. Windows 답신 1 — storage-name-v1 측정 회신 검토

회신 잘 받았습니다. 수치를 전부 재계산했고 **불일치 0건**입니다. 개정안 폐기에 동의합니다.

## 1-1. 회신 수치 독립 검증 — 전부 일치

```
항목                                       결과
276 = 181 + 95                             일치
1,118 = 181 + 937                          일치
inside_baseline_ranges 합계 = 181           일치
outside_baseline_ranges 합계 = 95           일치
missing_from_it 합계 = 184                  일치
목록 길이 181 / 95 / 937 / 1911            전부 선언값과 일치
상위면 결함 inside_baseline = 548           재계산 548, 일치

베이스라인 분류 재계산
  "안쪽"인데 저희 표에 없는 것                0
  "바깥쪽"인데 저희 표에 있는 것              0
  문맥 목록 중 표에 없는 것                   0
```

## 1-2. 기준 구현을 전수 검증했습니다

마이그레이션 SQL에서 매핑을 파싱해 CPython과 전부 대조했습니다.

```
SQL에서 파싱한 매핑                    1,530   (선언 mapping_count 1,530)
CPython 3.11.9 (Unicode 14.0.0)       1,530
SQL에만 있는 것                            0
CPython에만 있는 것                        0
매핑이 다른 것                             0
```

**완전 일치입니다.** 덤으로 하나 확정됐습니다 — **default case folding은 Unicode
14.0.0과 15.0.0 사이에 전혀 바뀌지 않았습니다.**

Cherokee/Cyrillic 지점도 서버 표에서 직접 확인했습니다.

```
U+AB70  sql -> U+13A0     U+13F8  sql -> U+13F0
U+1C80  sql -> U+0432     U+1C88  sql -> U+A64B
```

## 1-3. Windows 실측값 — 분리자

```
a／b (U+0061 U+FF0F U+0062)  -> ACCEPT   키 = U+0061 U+002F U+0062  ("a/b")
a＼b (U+0061 U+FF3C U+0062)  -> ACCEPT   키 = U+0061 U+005C U+0062  ("a\b")
a/b  (평문)                  -> REJECT   STORAGE_NAME_INVALID
```

Windows도 `protocol.json`의 `steps` 순서대로 구현했고, 같은 결과입니다.
**불일치 항목이 아니라 계약 결함이라는 판단에 동의합니다.**

### 다만 2개가 전부가 아닙니다 — 완전 목록은 7개입니다

```
U+2100  ACCOUNT OF                 -> U+0061 U+002F U+0063   (a/c)
U+2101  ADDRESSED TO THE SUBJECT   -> U+0061 U+002F U+0073   (a/s)
U+2105  CARE OF                    -> U+0063 U+002F U+006F   (c/o)
U+2106  CADA UNA                   -> U+0063 U+002F U+0075   (c/u)
U+FE68  SMALL REVERSE SOLIDUS      -> U+005C                 (\)
U+FF0F  FULLWIDTH SOLIDUS          -> U+002F                 (/)
U+FF3C  FULLWIDTH REVERSE SOLIDUS  -> U+005C                 (\)
                                                             총 7개
```

Unicode 14.0.0과 15.0.0에서 **동일한 7개**입니다. 버전 무관이므로 개정안에 그대로
못박을 수 있습니다. `℅`(CARE OF)는 실제 문서에서 쓰이는 문자입니다.

NFKC 결과에 C0 제어문자나 U+007F가 나타나는 스칼라는 없습니다.

## 1-4. 상위면 결함의 기전

```
BMP 스칼라 X 중 NFKC(X + U+0301)이 한 글자로 결합하는 것        278개
보고하신 결함 스칼라 1,911개 중, 하위 16비트가 그 집합에 속하는 것  1,824개
설명되지 않는 잔여                                                87개
```

즉 절단 자체는 전 상위면에서 일어나지만, **잘린 하위 16비트 값이 뒤따르는 결합문자와
결합 가능할 때만** 결과가 달라집니다.

### 부수 소득 — 이모지는 이 결함의 영향을 받지 않습니다

```
결함 목록 1,911개 중 이모지 영역(U+1F000..U+1FBFF) 스칼라 : 0개
```

`U+1F600 & 0xFFFF = 0xF600`은 사설 사용 영역이라 U+0301과 결합하지 않습니다.
**다만 이모지 + U+FE0F 조합은 미측정입니다.** 다음 측정에 넣어 주시길 요청드립니다.

## 1-5. 1,118의 구성 — 저희 표의 설계 결함이 섞여 있습니다

```
문맥에서만 드러나는 것                      937
  ├ Private Use (Co)                       438
  ├ Format (Cf, 태그 문자 등)                40
  └ 나머지                                  459
     └ 그중 BMP는 5개뿐
        U+0F73 U+0F75 U+0F81  (티베트어)
        U+FF9E U+FF9F        (반각 가나 탁점/반탁점)
```

**438개가 사설 사용 영역입니다. 이건 저희가 보낸 베이스라인 표의 문제입니다.**
표를 `category != Cn` 기준으로 생성해서 PUA 137,468개가 통째로 들어갔습니다.

## 1-6. 저희 쪽 전제가 틀렸습니다 — 확인 요청

저희는 "iPad는 버전 검사에 막혀 쓰기가 전면 차단돼 있다"는 전제로 논의해 왔습니다.
클라이언트에 fail-closed 검사가 구현된 적이 없다면 그 전제는 성립하지 않습니다.
**초기 전제와 정면으로 충돌하므로 한 번 더 확인 부탁드립니다.**

## 1-7. 개정 안건 제안

1. **분리자 단계 순서** — 거부 검사를 NFKC 뒤로. 대상 7개는 버전 무관 확정.
2. **PUA·태그문자 허용 여부** — 제외하면 §E 영향 범위가 크게 줄어듦.
3. **기준 표를 계약 자산으로 승격** — 지금 위치(마이그레이션 SQL)는 참조 경로가 아님.
4. **§F 방향 동의** — 다만 (2)를 먼저 정해 규모를 재산정하는 편이 낫다.

## 1-8. Windows 현재 상태

```
정규화 결과      서버와 일치 (기준표 전수 대조로 확인)
분리자 7개 구멍   있음 (계약·서버와 공유, 불일치 아님)
unicodedata2     번들 의존. 빠지면 전면 쓰기 불능. 단일 장애점.
```

개정 전까지 정규화 동작은 **바꾸지 않겠습니다**.

## 1-9. 다음 측정 요청 (우선순위 순)

```
1  §C-1 재확인 — 클라이언트 fail-closed 검사의 실제 유무
2  이모지 + U+FE0F 조합에서 §E 결함이 발현하는지
3  PUA·태그문자를 제외했을 때 1,118과 548이 각각 얼마로 줄어드는지
```

---
---

# §2. Windows 답신 2 — 결정 회신

```
PUA·태그문자·VS보충 금지    그렇게 해   (조건 1건)
1단계 착수                  그렇게 해   (배포 순서 제약 있음)
```

## 2-1. 검증 결과

```
분리자 7개 독립 열거 대조     완전 일치
PUA        claim 137,468   재계산 137,468   OK
태그문자    claim      97   재계산      97   OK
VS 보충     claim     240   재계산     240   OK
제외 후     claim 144,425   재계산 144,425   OK

"181개는 전부 casefold 단계"
  181개 중 NFKC가 항등(분해 없음)인 것 : 181 / 181
```

**181개 전부 NFKC 분해가 없는 문자입니다.** NFKC 단계가 관여할 여지 자체가 없으므로
하이브리드 결과는 실측일 뿐 아니라 기전상 필연입니다.

§7-(4)에서 "(2)를 먼저 정해 규모를 줄이자"고 한 것은 **철회합니다.**

## 2-2. PUA 금지 — 조건 하나

```
제외한다      PUA 전체 / U+E0000..U+E007F 태그 / U+E0100..U+E01EF VS 보충
제외하지 않는다 U+FE00..U+FE0F    VS (BMP)  <- U+FE0F 포함
```

**U+FE0F는 반드시 남아야 합니다.** "변이선택자 전부 제외"로 뭉뚱그려지는 것만 막으면
됩니다.

## 2-3. 1단계 — 동결 표 채택은 Windows에서 동작 무변화입니다

```
CPython casefold vs 동결 표(1,530 매핑), 결과가 다른 스칼라 : 0
```

### 다만 분리자 순서 수정은 배포 순서 제약을 받습니다

```
동결 casefold 표 채택   동작 무변화        계약 개정 없이 지금 가능
분리자 순서 수정        7개 accept→reject  digest 변경 -> 순서 강제
```

```
1  계약 개정 확정 (분리자 순서 + PUA 규칙)
2  서버 private.storage_name_v1 도 같은 규칙으로 수정
3  서버 allowlist 에 새 digest 등록
4  그 다음 클라이언트 핀 교체
```

**2번을 빠뜨리면 안 됩니다.** 서버는 현재 미할당 문자를 거부가 아니라 '분리'해서
허용하는 구조입니다.

## 2-4. 요청하신 다이제스트 — Windows 값

```
규약  '<HEX>:' + 파이프라인 결과 UTF-8 + LF, U+0000 오름차순, 서로게이트 제외

hex 형식   거부 표현        SHA-256
%04X       오류코드 문자열   b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af
%04X       빈 문자열        c07ed7a48a03f83688e8e05896fc3fee68d2554b992efe1af559448a986a1f27
%X         오류코드 문자열   152937f5a1b1bf8a9226e819ad0bbb4cbcfe5815aed5f2df4b4790e672b6d75b
%X         빈 문자열        d14e7acc63be5bccf000de3b31a3fd462b43f6bcf19d9dc1d5cf657ca6b60dc3
```

**위 네 값은 동결 casefold 표를 써도 동일합니다.**

## 2-5. 안건 추가 — 오류 코드 어휘

```
분리자 7개    STORAGE_NAME_INVALID 에 흡수    사용자가 고칠 방법이 자명함
PUA·태그·VS   새 코드                        "왜 안 되는지" 설명이 필요함
베이스라인 밖  새 코드 (2단계에서)             위와 같은 이유
```

## 2-6. Windows 진행 계획

```
지금 착수 (계약 무관, 동작 무변화)
  1  동결 casefold 표 채택
  2  전 코드포인트 회귀 시험 + 다이제스트 고정
  3  분리자 7개 고정 시험
  4  빌드 시 unicodedata2 번들 포함 검사
개정 확정 후
  분리자 순서 수정, PUA 게이트, 핀 4개 갱신, unicodedata2 제거, 벡터 추가
```

---
---

# §3. Windows 답신 3 — 세 결정 회신 + 3(b) 규칙 보완 요청

```
2·3단계를 한 개정으로 묶을지        묶기      조건 1건
상위면 + 결합 기호 조합을 막을지     막기      단 문안에 구멍 있음
1단계(casefold 표)를 지금 착수할지   착수      조건 없음
```

**iPad 1단계 착수에 동의합니다. 조건 없습니다.** 되돌릴 일이 생기지 않는 작업입니다.

## 3-1. 검증 결과

```
하이브리드 잔여 40개 목록 길이        40      선언과 일치
  베이스라인 안쪽                      0      주장과 일치
  구성 U+A7F1(1) / U+16D68..6A(3) / U+1CCD6..F9(36)
거부되는 단독 스칼라                   59      저희 측정값과 일치
```

## 3-2. 다이제스트 규약 — `%04X` + 오류코드, 동의

보조로 빈문자열 값을 함께 두자는 제안도 채택합니다.

```
주   b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af   %04X + 오류코드
보조 c07ed7a48a03f83688e8e05896fc3fee68d2554b992efe1af559448a986a1f27   %04X + 빈문자열
```

## 3-3. 3(b) 규칙에 구멍이 있습니다

**이 검사는 반드시 NFKC 호출 전에 해야 합니다.** 그런데 **입력 시점에 ccc=0 이지만
NFKC가 결합문자로 바꿔 놓는 스칼라**가 있습니다.

```
U+0F73  TIBETAN VOWEL SIGN II             -> U+0F71(ccc=129) U+0F72(ccc=130)
U+0F75  TIBETAN VOWEL SIGN UU             -> U+0F71(ccc=129) U+0F74(ccc=132)
U+0F81  TIBETAN VOWEL SIGN REVERSED II    -> U+0F71(ccc=129) U+0F80(ccc=130)
U+FF9E  HALFWIDTH KATAKANA VOICED         -> U+3099(ccc=8)
U+FF9F  HALFWIDTH KATAKANA SEMI-VOICED    -> U+309A(ccc=8)
                                             5개
```

**분리자 7개와 정확히 같은 구조입니다.** NFKC 앞에 놓인 검사를 NFKC가 무효화합니다.

### 5개 중 실제로 위험한 것은 2개

```
따라오는 문자   NFKC 후    결합하는 BMP 값   위험한 상위면 스칼라
U+FF9E          U+3099            96개              1,536개
U+FF9F          U+309A            20개                320개
U+0F73/75/81    U+0F71             0개                    0개
```

### 같은 계열 — NFKC가 상위면 문자를 만들어 내는 경우

```
U+FA6C -> U+242EE     U+FACF -> U+2284A     U+FAD0 -> U+22844
U+FAD1 -> U+233D5     U+FAD5 -> U+25249     U+FAD6 -> U+25CD0
U+FAD7 -> U+27ED3                            7개 (CJK 호환 한자)
```

**전부 무해합니다** — 하위 16비트가 전부 CJK 한자라 결합 상대가 없습니다.
**이 7개는 규칙에 넣을 필요가 없습니다.**

### 그쪽 1차 자료가 이미 이걸 가리키고 있었습니다

1차 회신의 `context_only_baseline_divergent` 937개 중 BMP는 정확히 다섯 개였습니다.

### 제안하는 규칙 문안

```
거부한다
  상위면 스칼라(U+10000 이상) 바로 뒤의 문자가
    (a) ccc ≠ 0 인 결합문자
    (b) U+FF9E, U+FF9F                     <- 추가
넣지 않는다
  U+0F73 U+0F75 U+0F81      U+0F71에 결합 상대 없음
  U+FA6C 등 CJK 호환 7개     하위 16비트가 CJK 한자, 결합 상대 없음
```

### 측정 요청 — 배열 S8

**저희가 확인한 것은 "규칙 문안이 이 배열을 걸러 내지 못한다"까지입니다.**
Foundation이 실제로 이 배열에서 절단하는지는 재지 못했습니다.

```
S8   X + U+FF9E   /   X + U+FF9F        (X = 상위면 전 구간)
```

## 3-4. 안건 회신

- **3(b) 채택** — 조건부 동의. **S8이 닫히면 동의합니다.**
- **2·3단계 묶기** — 동의. **조건: S8 결과가 개정 문안 동결 전에 나와야 합니다.**
- **오류 코드 이름** — 동의 (`STORAGE_NAME_UNSUPPORTED_SCALAR` / `STORAGE_NAME_UNASSIGNED`)
- **계약 자산 구조** — 동의. `assigned-baseline-14.0.0.json`에 정규형 sha256
  `aed4e530...`을 함께 실어 달라.
- **측정 3 범위 한계** — 이해. 2단계 착수 전 일정에 이견 없음.

## 3-5. 수용 기준 — 단계별 다이제스트

```
현재
  Windows / 계약 기준   b9368cb6...
  iPad 현재 구현        6c0a85ed...
  차이 276개
1단계 완료 시
  iPad                 72d107e0...
  Windows              변화 없음
  차이 40개            <- 정상. 전부 베이스라인 밖.
2·3단계 완료 시
  양쪽 값이 함께 새 값으로 이동. 판정 기준은 **양쪽 값이 같을 것**, 차이 0.
```

## 3-6. 양쪽 할 일 — 소유자별

```
iPad — 측정
  [ ] S8
  [ ] 실기기 iPad 저장소 이름 스캔
iPad — 클라이언트
  [ ] 1단계 : 동결 casefold 표 채택
  [ ] divergentScalars 정리
  [ ] 2단계 : 베이스라인 게이트 + PUA·태그·VS 제외
  [ ] 3단계 : 상위면 + 인접 결합문자 거부 규칙
  [ ] 핀 교체
iPad — 계약 (sync-contract/ 소유)
  [ ] 계약 자산 생성 + contract-lock.json sha256
  [ ] generate_casefold_sql.py 를 JSON 입력으로 뒤집기
  [ ] 개정 문안
  [ ] 적합성 벡터 추가
서버
  [ ] private.storage_name_v1 / nfkc_unicode15 수정
  [ ] allowlist 에 새 digest 등록
Windows
  [진행] 동결 표 채택 / 회귀 시험 / 분리자 고정 시험 / 번들 검사
  [ ]    분리자 순서 수정 / 게이트 / 핀 교체
보류
  [!] iPad 동기화 배선 — 2단계 완료 전 금지. 3단계까지 끝낸 뒤를 권함.
```
