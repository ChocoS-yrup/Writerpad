# iPad 세션 회신 — storage-name-v1 계약 개정 선행 측정

측정일 2026-08-13. 계약 파일은 건드리지 않았고 배포도 하지 않았습니다.
실측 원자료는 `유니코드측정_실측데이터_20260813.json`에 있습니다.

---

## 먼저 결론

**개정안은 지금 형태로는 성립하지 않습니다. 재설계가 필요합니다.**

92라는 숫자를 재현하지 못한 이유는 그것이 실측값이 아니기 때문입니다. `divergentScalars`는
스캔 결과가 아니라 커밋 `a473df4`에서 손으로 적어 넣은 상수이고, 시험은 개수가 92인지만
확인합니다. 저장소에 그것을 만들어 낸 스크립트는 없습니다.

이번에 실제 iPad 런타임으로 전 코드포인트를 다시 훑었습니다. 발산은 92가 아니라 **276**이고,
그중 **181개가 베이스라인 안쪽**입니다. 그쪽이 우려한 두 갈래 중 나쁜 쪽 — **구현 차이** —
가 실재하며, 베이스라인 표는 그것을 걸러내지 못합니다.

문맥(결합문자)까지 넣으면 베이스라인 안쪽 발산은 **1,118개**로 늘어납니다.

그리고 측정 도중 그보다 심각한 것이 하나 더 나왔습니다. Foundation의 NFKC가 상위면 스칼라
뒤에 결합문자가 오면 코드포인트를 16비트로 잘라 처리합니다. `𐁁́`(U+10041 U+0301)의 저장
이름이 `á`가 됩니다. 자세한 것은 아래 §E입니다.

---

## A. 92의 출처

### A-1. 무엇과 무엇을 비교한 것인가

**비교의 산물이 아닙니다.** `WriterPad/Sync/SyncV2Contract.swift:329`의
`divergentScalars`는 하드코딩된 배열이고, `WriterPadTests/SyncV2ContractTests.swift:227`의
시험은 이렇게만 검사합니다.

```swift
XCTAssertEqual(SyncV2StorageName.divergentScalars.reduce(0) { $0 + $1.count }, 92)
```

값을 재도출하지 않고 개수만 셉니다. 커밋 메시지에는 "전 코드포인트 스캔으로 확정"이라고
적혀 있지만 그 스캔의 코드도 산출물도 저장소에 없습니다.

### A-2. 그래서 실측으로 다시 뽑았습니다

- **대상 1 (iPad)** — `SyncV2StorageName.normalize`가 쓰는 것과 같은 Foundation 파이프라인.
  실제 iPadOS 26.5 시뮬레이터와 macOS 26.5.2 양쪽에서 실행.
- **대상 2 (계약 기준)** — 동결된 Unicode 15.0.0 구현을 이 저장소 자체 자료로 재구성:
  NFKC는 `unicodedata2` 15.0.0, default casefold는 서버 마이그레이션에 동결돼 있는
  `private.unicode15_casefold` 1,530개 매핑
  (`supabase/migrations/20260811010000_sync_contract_0_1_0_foundation.sql`).
  이 casefold 표는 CPython 3.9.6 stdlib(Unicode 13.0.0)의 `str.casefold()`와 교차 검증했고
  검사한 표본에서 전부 일치했습니다.

### A-3. 파이프라인 동일 여부

**단계 구성은 동일합니다.** `NFKC -> casefold -> NFKC -> rstrip(" .")`.
**다만 두 번째 단계의 의미가 다릅니다.** iPad는 default case folding이 아니라 Foundation의
`folding(options: [.caseInsensitive])`를 씁니다. 이것이 §B의 구현 차이를 만드는 원인입니다.

### A-4. 스캔 범위

전 코드포인트 **1,112,064개**(U+0000..U+10FFFF, 서로게이트 제외). 부분집합이 아닙니다.

iPadOS와 macOS가 같은 결과를 낸다는 것도 확인했습니다. 전 코드포인트 파이프라인 산출물의
SHA-256이 양쪽에서 동일합니다.

```
f651fbb1ce4dc961eff675023b596491ac96fe45816011d4382ad1636c00194f
```

### A-5. 92개 목록과 그 정체

92개는 실측 발산 276개의 부분집합이고, **전부 Unicode 15.0.0 미할당 스칼라**입니다.
즉 92는 사실상 "버전 차이만 담은 목록"이며, 그것조차 3개가 빠져 있습니다.

```
U+1C89                     (1)
U+A7CB..U+A7CC             (2)
U+A7CE                     (1)
U+A7D2                     (1)
U+A7D4                     (1)
U+A7DA                     (1)
U+A7DC                     (1)
U+A7F1                     (1)
U+10D50..U+10D65          (22)
U+16EA0..U+16EB8          (25)
U+1CCD6..U+1CCF9          (36)
                        ---- 92
```

목록에 잘못 들어간 것은 없습니다(92개 전부 실제로 발산). **빠진 것이 184개입니다.**

```
U+13A0..U+13F5            (86)   Cherokee 대문자        <- 베이스라인 안쪽
U+13F8..U+13FD             (6)   Cherokee 소문자 ye..   <- 베이스라인 안쪽
U+1C80..U+1C88             (9)   Cyrillic 역사적 자형    <- 베이스라인 안쪽
U+AB70..U+ABBF            (80)   Cherokee 소문자        <- 베이스라인 안쪽
U+16D68..U+16D6A           (3)   Kirat Rai              <- 바깥
```

---

## B. 92개와 베이스라인 표의 교집합

베이스라인 표는 먼저 무결성을 확인했습니다. 첨부된 `.txt`는 전송 중 줄바꿈이 CRLF로
바뀌어 있었고, CR을 벗겨 내면 정규형 sha256이 정확히 일치합니다
(`aed4e530cc5e0de638310db961f17f174d605282764a6657afd0f70e5515c85c`, 698범위 / 282,230스칼라).
`.json`의 `ranges`도 같은 내용입니다.

### B-1. 물어보신 그대로의 답

```
92개 중 베이스라인 안쪽 : 0개
92개 중 베이스라인 바깥 : 92개
```

이 숫자만 보면 "성립"입니다. **그런데 이 답은 무의미합니다.** 92라는 목록 자체가 버전
차이만 담고 있으니, 그것을 베이스라인과 교차시키면 당연히 0이 나옵니다. 순환입니다.

### B-2. 실측으로 다시 낸 답

```
                        발산    베이스라인 안쪽    바깥쪽
단독 스칼라 스캔         276          181            95
결합문자 19종 문맥 포함  ----       1,118           ----
```

**안쪽이 181개입니다. 재설계입니다.**

### B-3. 안쪽 181개의 상세

#### (1) Cherokee — 172개, 접는 방향이 반대

`U+13A0..U+13F5`, `U+13F8..U+13FD`, `U+AB70..U+ABBF`

Cherokee는 소문자가 나중에(Unicode 8.0) 추가된 탓에 안정성 정책 때문에 default case folding이
**대문자 쪽으로** 접힙니다. Foundation의 `.caseInsensitive` folding은 **소문자 쪽으로** 접습니다.

```
             계약/서버        iPad
U+13A0  ->   U+13A0          U+AB70
U+AB70  ->   U+13A0          U+AB70
U+13F8  ->   U+13F0          U+13F8
```

짝을 같다고 보는 것은 양쪽 다 맞습니다. 등가류는 같습니다. **하지만 대표 바이트가 다릅니다.**
계약이 "정확한 UTF-8 key"로 충돌을 판정하므로, Windows/서버가 `13A0`으로 만든 키를 iPad는
`AB70`으로 계산합니다. 같은 이름이 서로 다른 키를 갖습니다.

#### (2) Cyrillic 역사적 자형 — 9개, iPad가 아예 접지 않음

`U+1C80..U+1C88`

```
             계약/서버        iPad
U+1C80  ->   U+0432 (в)      U+1C80
U+1C81  ->   U+0434 (д)      U+1C81
U+1C82  ->   U+043E (о)      U+1C82
U+1C83  ->   U+0441 (с)      U+1C83
U+1C84  ->   U+0442 (т)      U+1C84
U+1C85  ->   U+0442 (т)      U+1C85
U+1C86  ->   U+044A (ъ)      U+1C86
U+1C87  ->   U+0463 (ѣ)      U+1C87
U+1C88  ->   U+A64B          U+1C88
```

이쪽은 등가류 자체가 다릅니다. 등가류 비교를 돌려 보면 베이스라인 안에서 **26개 스칼라의
등가류가 iPad에서 축소**됩니다(놓친 충돌). 반대 방향, 즉 iPad가 없는 충돌을 만들어 내는
경우는 0개였습니다.

즉 `ᲀ`라는 이름과 `в`라는 이름을 서버는 충돌로 보고 iPad는 다른 이름으로 봅니다.

### B-4. 문맥까지 넣으면 1,118개

베이스라인 282,230개 스칼라를 각각 19가지 문맥(결합문자 8종 × 앞뒤 + `a`/`A` 문맥 3종)에
놓고 5,362,370건을 비교했습니다.

```
문맥에서 발산하는 베이스라인 스칼라 : 1,118
  단독으로도 이미 발산하던 것        :   181
  문맥에서만 드러나는 것             :   937
```

937개의 대부분은 §E의 상위면 결함입니다.

---

## C. 벡터 15개 — iPad 런타임 실행 결과

### C-1. 우회할 게이트가 없었습니다

전제를 하나 바로잡아야 합니다. **iPad는 버전 검사에 막혀 있지 않습니다.**
`SyncV2Contract.storageNameUnicodeVersion = "15.0.0"`은 선언만 되어 있고 저장소 어디에서도
읽지 않습니다. `containsUnicodeVersionDivergentScalar`도 호출하는 곳이 없습니다.
fail-closed 검사는 클라이언트에 구현된 적이 없습니다. 그래서 우회 없이 그대로 돌렸습니다.

실행 방법은 `WriterPad/Sync/SyncV2Contract.swift`를 **수정 없이 그대로 컴파일**해서
iPadOS 26.5 시뮬레이터(iPad Pro 13-inch M5)에서 실행했습니다.

### C-2. 결과

```
vector  판정   산출 normalized / utf8_hex
SN-001  PASS   "résumé"    72c3a973756dc3a9
SN-002  PASS   "résumé"    72c3a973756dc3a9    (U+0065 U+0301 -> U+00E9 결합 확인)
SN-003  PASS   "file.txt"  66696c652e747874
SN-004  PASS   "file"      66696c65
SN-005  PASS   "폴더"       ed8fb4eb8d94
SN-006  PASS   "i̇"         69cc87              (U+0130 -> U+0069 U+0307)
SN-007  PASS   "strasse"   73747261737365      (U+00DF -> U+0073 U+0073)
SN-008  PASS   " leading"  206c656164696e67
SN-009  PASS   "a b"       612062
SN-010  PASS   "abc"       616263
SN-011  PASS   STORAGE_NAME_RESERVED
SN-012  PASS   STORAGE_NAME_INVALID
SN-013  PASS   STORAGE_NAME_INVALID
SN-014  PASS   STORAGE_NAME_INVALID
SN-015  PASS   STORAGE_NAME_INVALID

15/15 통과, 실패 0
```

**SN-007과 SN-006은 통과합니다.** 걱정하신 지점은 문제가 아니었습니다.
`folding(options: [.caseInsensitive])`는 ß -> ss, U+0130 -> U+0069 U+0307을 제대로 냅니다.
default case folding과 갈리는 곳은 어디까지나 Cherokee와 Cyrillic 1C8x입니다 —
**벡터 15개가 건드리지 않는 영역**입니다. 그래서 벡터로는 이 문제를 잡을 수 없습니다.

이것이 "적합성 벡터 15개는 버전 차이를 검출하지 못한다"는 그쪽 관찰의 더 넓은 판본입니다.
벡터 15개는 **구현 차이도** 검출하지 못합니다.

---

## D. iPad 구현 실태

### D-1. 런타임이 보고하는 Unicode 버전

Swift에는 없지만 **ICU에는 있습니다.** `dlsym`으로 직접 물어봤습니다.

```
u_getUnicodeVersion  ->  17.0.0
u_getVersion         ->  78.1
```

Swift 표준 라이브러리는 자체 Unicode 표를 따로 싣고 있는데 그쪽도 최신 age가 17.0입니다
(할당 스칼라 297,334개).

한 가지 덧붙이면 — 코드 주석의 "Swift에는 판 번호를 알려 주는 API가 없다"는 Swift 한정으로는
맞지만, 위처럼 조회 자체는 가능합니다. 다만 `libicucore`는 Apple 비공개 프레임워크라
App Store 심사에서 걸릴 수 있으니 진단·시험 용도로만 쓸 수 있다고 봅니다.

### D-2. 파이프라인 3단계 API

| 단계 | API | 비고 |
|---|---|---|
| NFKC | `String.precomposedStringWithCompatibilityMapping` | §E 결함 있음 |
| default casefold | `String.folding(options: [.caseInsensitive], locale: nil)` | **default case folding이 아님** |
| 최종 NFKC | 같은 API | |

`.caseInsensitive` folding은 Unicode default case folding이 아니라 Foundation의 자체
대소문자 무시 비교용 folding입니다. 대부분 겹치지만 Cherokee(소문자 방향)와
Cyrillic 1C8x(접지 않음)에서 갈립니다. Foundation에는 default case folding에 대응하는
공개 API가 없습니다.

### D-3. 나머지 처리

- **rstrip(" .")** — `while let last = normalized.last, last == " " || last == "."`.
  스칼라가 아니라 `Character`(자소 묶음) 단위로 떼어 냅니다. 검사한 범위에서 Python의
  `rstrip(" .")`과 결과가 갈리는 사례는 나오지 않았습니다. 마지막 자소가 " "나 "."에
  결합문자가 붙은 형태면 양쪽 다 떼지 않습니다.
- **예약 basename** — `"." `로 1회 분할한 앞부분을 `con/prn/aux/nul/com1..9/lpt1..9`와 대조.
  이미 casefold를 거친 뒤라 대소문자 구분이 없습니다.
- **거부 문자** — `/`, `\`, 스칼라값 ≤ 31, 127. 정규화 **전에** 검사합니다.
  `protocol.json`의 `storage_name_normalization.steps`가 거부를 1단계, NFKC를 2단계로
  적어 두었으므로 **iPad는 계약대로입니다.** 다만 그 순서 때문에 계약 자체에 구멍이
  있습니다. 실측입니다.

  ```
  입력 "a／b" (U+0061 U+FF0F U+0062)  ->  통과, 정규화 결과 U+0061 U+002F U+0062  ("a/b")
  입력 "a＼b" (U+0061 U+FF3C U+0062)  ->  통과, 정규화 결과 U+0061 U+005C U+0062  ("a\b")
  ```

  전각 슬래시는 2단계 NFKC에서 진짜 `/`가 되는데 검사는 이미 끝난 뒤입니다.
  충돌 키에 경로 구분자가 그대로 들어갑니다.
  같은 파일의 `separator_policy`는 "Separators are rejected, not rewritten"이라고
  선언하는데, 단계 순서가 그 선언을 지키지 못합니다.

  이것은 iPad만의 문제가 아닙니다. Windows도 단계대로 구현했다면 같은 결과일 것이고,
  그렇다면 양쪽이 사이좋게 같은 구멍을 갖고 있는 셈입니다(불일치 항목은 아닙니다).
  Windows 쪽 실측값을 알려 주시면 확실해집니다. 개정 때 거부 검사를 2단계 뒤로 옮기는
  것을 함께 안건에 올렸으면 합니다.
- **UTF-8** — `Array(normalized.utf8)`을 `%02x`로. 별도 인코딩 처리 없음.

---

## E. 측정 중 발견 — Foundation NFKC의 상위면 절단 결함

이건 요청받은 항목은 아니지만 개정안 설계에 직접 영향을 주므로 보고합니다.

`precomposedStringWithCompatibilityMapping`은 상위면(U+10000 이상) 스칼라 뒤에 결합문자가
오면 **코드포인트를 16비트로 잘라서** 처리합니다.

```
입력   U+10041 U+0301   ("𐁁" + 결합 acute, UTF-16으로 D800 DC41 0301)
iPad   U+00C1           ("Á")            <- 잘못
정답   U+10041 U+0301
```

casefold까지 거치면 저장 이름이 `á`가 됩니다. 즉 **`𐁁́`와 `á`가 같은 저장 이름을 갖습니다.**

결합문자 하나(U+0301)만으로 재어도 **1,911개** 스칼라가 영향을 받습니다.

```
면별 분포 : 면1 156개, 면2~16 각 117개 (균일)
1,911개 중 1,808개가 정확히 chr(cp & 0xFFFF) 가설과 일치
베이스라인 안쪽 548 / 바깥 1,363
```

면마다 117개씩 균일하게 나오는 것이 절단의 증거입니다. 결합문자를 U+0301 하나만 썼으니
실제 규모는 이보다 큽니다.

macOS 26.5.2와 iPadOS 26.5에서 동일하게 재현되며, 첫 번째 NFKC 단계에서 이미 발생합니다.

같은 계열로 정준 순서(canonical ordering)도 어긋납니다.

```
NFKC(U+0345 U+0F73)
  iPad                    U+0345 U+0F71 U+0F72     <- ccc 240이 129/130 앞에 남음
  CPython 3.9 / ud2 15.0  U+0F71 U+0F72 U+0345
```

**베이스라인 표는 이것도 막지 못합니다.** 문제가 스칼라 하나가 아니라 조합에서 생기고,
해당 스칼라 548개는 베이스라인 안쪽이기 때문입니다.

---

## F. 그래서 어떻게 할 것인가 (제안)

베이스라인 게이트는 방향은 맞지만 **덮는 범위가 부족합니다.** 근본 원인은 버전이 아니라
"클라이언트가 정규화를 런타임에 위임한다"는 점입니다. 런타임을 고정하지 못하는 한
버전이든 구현이든 계속 갈립니다.

서버는 이미 옳은 길을 갔습니다 — 동결 표를 넣고 **직접 계산**합니다.
같은 것을 클라이언트에서도 해야 한다고 봅니다.

```
현재 제안  런타임 버전 요구 삭제 + 베이스라인 할당표 게이트
          -> 버전 차이는 덮음, 구현 차이 181개는 못 덮음, 상위면 결함도 못 덮음

대안      storage-name-v1을 클라이언트에서 자체 구현으로 전환
          동결된 NFKC 표 + 동결된 casefold 표(서버가 쓰는 1,530개 매핑 그대로)
          -> Foundation/ICU/unicodedata2 어느 것에도 의존하지 않음
          -> 버전 차이, 구현 차이, 상위면 결함 전부 소멸
          -> 베이스라인 게이트는 그 위에 얹으면 미할당 문자까지 정리됨
```

Windows 쪽 부담은 오히려 줄어듭니다 — `unicodedata2` 번들이 필요 없어지는 것은 두 안이
같고, 대안 쪽은 CPython 판올림에도 영향받지 않습니다.

비용이 큰 것은 iPad 쪽입니다. Swift로 NFKC를 직접 구현해야 합니다(정준 분해표, 조합 제외,
정준 순서, 호환 분해, 재조합). 서버 마이그레이션에 이미 든 자료를 재사용할 수 있으니
표 자체는 새로 만들 것이 없습니다.

중간 완화책만 원하신다면, 베이스라인 게이트 + Cherokee(172) + Cyrillic 1C8x(9) 거부까지는
스칼라 단위로 가능합니다. 다만 §E는 조합 문제라 스칼라 거부로는 정리되지 않습니다.

어느 쪽이든 결정은 양쪽 합의 후에. 이 회신에서는 계약 파일, `contract_version`,
`canonical_contract_sha256` 어느 것도 건드리지 않았습니다.

---

## G. 부수적으로 알려 드릴 것

`SyncV2StorageName.divergentScalars`(92개)와 그것을 확인하는 시험, 그리고
`SyncV2Contract.swift:268`의 주석("실제로 갈릴 수 있는 문자는 92개뿐이다")은 이번 측정으로
사실이 아님이 확인됐습니다. 이번 작업은 측정만 하기로 했으므로 코드는 고치지 않았습니다.
개정 방향이 정해지면 함께 정리해야 합니다.

---

## 재현 방법

측정에 쓴 것은 전부 이 저장소와 첨부 파일뿐입니다.

```
런타임 A   iPadOS 26.5 시뮬레이터 (iPad Pro 13-inch M5), ICU 78.1 / Unicode 17.0.0
런타임 B   macOS 26.5.2 (25F84), 동일 ICU
기준 구현  unicodedata2 15.0.0 (NFKC)
          + supabase/migrations/20260811010000_..._foundation.sql 의
            private.unicode15_casefold 1,530개 매핑 (default casefold)
교차 검증  CPython 3.9.6 stdlib unicodedata 13.0.0
피시험 코드 WriterPad/Sync/SyncV2Contract.swift (무수정)
```

수치와 전체 스칼라 목록은 `유니코드측정_실측데이터_20260813.json`에 있습니다.
