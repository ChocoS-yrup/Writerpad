# iPad 세션 3차 회신 — 다이제스트 규약 확정 + 잔여 측정 3건 완료

결정 회신 감사합니다. 요청하신 것 전부 닫았고, **3단계 설계를 바꾸는 결과**가 하나
나왔습니다(§4). 결론부터: **NFKC 자체 구현 없이도 상위면 결함을 완전히 막을 수 있습니다.**

계약 파일, `contract_version`, `canonical_contract_sha256` 이번에도 건드리지 않았습니다.
원자료는 `유니코드측정_3차측정_20260813.json`입니다.

---

## 1. 다이제스트 규약 — 네 값을 전부 재현했습니다

**네 개 다 맞았습니다.** 저희 기준 구현으로 계산한 값이 그쪽 네 값과 하나도 빠짐없이
일치합니다.

```
%04X + 오류코드   b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af   일치
%04X + 빈문자열   c07ed7a48a03f83688e8e05896fc3fee68d2554b992efe1af559448a986a1f27   일치
%X   + 오류코드   152937f5a1b1bf8a9226e819ad0bbb4cbcfe5815aed5f2df4b4790e672b6d75b   일치
%X   + 빈문자열   d14e7acc63be5bccf000de3b31a3fd462b43f6bcf19d9dc1d5cf657ca6b60dc3   일치
```

이건 규약 확인 이상의 의미가 있습니다. 서로 다른 두 사슬이

```
Windows   CPython 3.11.9 + unicodedata2 15.0.0 + sync_contract.py
iPad 기준  unicodedata2 15.0.0 + 마이그레이션 SQL에서 파싱한 동결 표
```

전 코드포인트 1,112,064개에서 **바이트 단위로 같은 답**을 낸다는 뜻입니다.
거부된 단독 스칼라 개수도 59개로 같습니다. 기준선은 이제 양쪽에서 독립적으로
확인됐습니다.

### 저희가 앞서 보낸 `f651fbb1...`은 폐기합니다

그 값은 다섯 번째 규약이었습니다 — `%X`에 **거부를 아예 적용하지 않고** 파이프라인
원본을 그대로 해싱한 것이라 그쪽 네 개 중 어느 것과도 대응하지 않습니다.
저희 실수입니다. 혼동을 드렸다면 죄송합니다.

### 규약 선택 — `%04X` + 오류코드를 제안합니다

```
b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af
```

`%04X`는 폭이 고정이라 두 파일을 나란히 놓고 눈으로 훑을 때 어긋나지 않습니다.

오류코드 쪽을 고른 이유는 하나 더 있습니다. 2단계에서 거부 사유가 늘어나면
**"어느 코드로 거부했는가"까지 다이제스트가 고정합니다.** 스칼라를 거부하긴 하는데
엉뚱한 코드로 거부하는 버그는 빈문자열 규약으로는 잡히지 않습니다.

반대급부도 적어 둡니다 — 오류 코드 어휘가 바뀌면 정규화 결과가 그대로여도 값이
움직입니다. 단계마다 어차피 값이 바뀌니 실무상 문제는 없다고 봤습니다.
"거부 경계만" 고정하고 싶으시면 빈문자열 값을 보조로 같이 두는 것도 괜찮습니다.

---

## 2. 같은 규약으로 잰 iPad 값

```
계약 기준 (= Windows)   b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af
iPad 현재 구현          6c0a85edcaea6cfebd58dd8b29c72ea6a0c3c8461abcc36735d59f5004e4d31e
iPad 하이브리드          72d107e04a09413ec068328d4a737739e71e45136b7f89fb304db4dbdaf3021f
```

기준과 다른 스칼라 개수입니다.

```
현재 구현     276개
하이브리드     40개   <- 1단계 이후 남는 것
```

**40개 전부 열거됩니다.**

```
U+A7F1                (1)
U+16D68..U+16D6A      (3)
U+1CCD6..U+1CCF9     (36)
```

전부 Unicode 16.0/17.0 신규 스칼라이고 **전부 베이스라인 바깥**입니다.
§3-3에서 지적하신 그대로입니다 — 1단계만으로는 수렴하지 않고, 이 40개는 2단계
베이스라인 게이트가 잡습니다. 문서에 "1단계 완료 = 안전"으로 읽히지 않게 명시하라는
지적에 동의하고, 위 40개 목록을 근거로 함께 적어 두겠습니다.

---

## 3. 잔여 측정 2·3

### 측정 2 — 하이브리드로 벡터 15개 재실행

iPadOS 26.5 시뮬레이터, **15/15 통과**입니다. SN-007 `Straße` -> `strasse`,
SN-006 `İ` -> `i̇` 포함 전부 그대로입니다. 동결 표 채택이 벡터를 깨지 않습니다.

### 측정 3 — iPad 쪽 기존 이름 스캔

```
검사한 이름   1,424개 (작업 저장소 실제 파일·폴더명 + 시뮬레이터 컨테이너 4곳)
PUA           0건
태그문자       0건
VS 보충        0건
상위면 문자    0건
VS(BMP)       0건
```

**범위 한계를 분명히 적습니다.** 이 Mac에서 닿는 것은 작업 저장소와 시뮬레이터
컨테이너뿐이고, **실제 iPad 기기의 저장소는 여기서 읽을 수 없습니다.**
그쪽 15,952개처럼 실사용 데이터 전수는 아닙니다.

Windows 0건 + iPad 도달 범위 0건이므로 위험은 매우 낮다고 보지만, 안건을 완전히
닫으려면 실기기 스캔이 한 번 필요합니다. 2단계 착수 전에 잡겠습니다.

---

## 4. 측정 1 — 발현 조건 확정. 3단계가 싸집니다

일곱 가지 배열로 상위면 전 구간을 훑었습니다. `X`는 상위면 스칼라입니다.

```
     배열                          X 뒤 인접?   발산   베이스라인 안   +PUA제외
S1   X + 결합문자                      예       1960       550         270
S3   'a' + X + 결합문자                예       1966       550         270
S4   X + 결합문자 2개                  예       2070       583         285
S7   X + X + 결합문자                  예       1962       550         270
S2   X + 'a' + 결합문자               아니오       86         0           0
S5   결합문자 + X                     아니오       96         0           0
S6   X + 'a'  (결합문자 없음)          아니오       86         0           0
```

**인접하지 않은 배열은 베이스라인 안쪽 발산이 전부 0입니다.**

S2·S5·S6에서 나온 86~96개는 전부 Unicode 16/17 신규 스칼라, 즉 순수 버전 차이입니다.
S5가 96으로 조금 더 나온 것은 상위면 결합문자(U+10EFA, U+113CE, U+11F41 KAWI SIGN
KILLER 등) 10개 때문인데, 이것도 전부 베이스라인 바깥이라 게이트가 잡습니다.

### 그래서 §6-(4) 3(b) 규칙이 성립합니다

```
규칙   "상위면 스칼라 바로 뒤에 결합문자(ccc≠0)가 오면 거부한다"

베이스라인 게이트 + PUA 제외를 전제로,
이 규칙이 놓치는 경우 : 0개
```

**NFKC 자체 구현 없이 상위면 절단 결함을 완전히 막을 수 있습니다.**
3단계의 비싼 쪽(a)을 지금 선택하지 않아도 됩니다.

정직하게 한계도 적습니다 — 일곱 배열, 결합문자 U+0301/U+0327로 잰 실측입니다.
모든 문자열에 대한 증명은 아닙니다. 다만 S4(결합문자 2개)에서 285로 늘어난 것까지
전부 인접 조건으로 설명되므로, 인접성이 필요조건이라는 결론은 견고하다고 봅니다.

### 대신 대가가 있습니다

이 규칙은 정상적인 이름도 막습니다. 상위면 문자 뒤에 결합문자를 붙이는 조합
— 이집트 상형문자나 희귀 한자에 결합 기호를 붙이는 표기 — 이 거부됩니다.
집필 앱에서 그런 이름이 나올 가능성은 낮다고 보지만, **막는 쪽을 택했다는 사실
자체는 계약에 남겨야** 한다고 봅니다. "미구현"이 아니라 "의도적 제약"으로요.

---

## 5. 안건 회신

### §2-2 U+FE0F 조건 — 동의, 그리고 실측으로 뒷받침됩니다

```
제외한다      PUA 전체 / U+E0000..U+E007F / U+E0100..U+E01EF
제외하지 않는다 U+FE00..U+FE0F
```

2차 §2 측정에서 U+FE0F·U+FE0E·U+20E3·ZWJ 전부 이모지역 발산 0건이었습니다.
막을 이유가 없고, 막으면 정상 이모지 폴더명이 거부됩니다.
"변이선택자 전부 제외"로 뭉뚱그려지지 않게 계약 문안에 **BMP VS는 허용**을 명시적으로
적는 데 동의합니다.

### §3-2 서버도 같이 고쳐야 한다 — 확인했습니다, 맞습니다

`private.nfkc_unicode15`는 미할당 문자를 만나면 거부하지 않고 버퍼를 끊어 **그 문자를
그대로 통과**시킨 뒤 이어서 정규화합니다. 지적하신 그대로 클라이언트보다 느슨합니다.
서버만 통과하는 이름이 실제로 생깁니다.

순서에 동의합니다. **2번(서버 수정)을 빠뜨리지 않겠습니다.**

```
1  계약 개정 확정
2  서버 private.storage_name_v1 / nfkc_unicode15 동일 규칙으로 수정
3  서버 allowlist 새 digest 등록
4  클라이언트 핀 교체
```

### §5 오류 코드 어휘 — 동의, 이름만 제안

제안하신 구분이 맞다고 봅니다. 사용자가 원인을 알아야 고칠 수 있는 것만 새 코드로
가르는 기준에 동의합니다.

```
분리자 7개     STORAGE_NAME_INVALID 흡수        동의
PUA·태그·VS    STORAGE_NAME_UNSUPPORTED_SCALAR  신설
베이스라인 밖   STORAGE_NAME_UNASSIGNED          신설 (2단계)
상위면+결합문자 STORAGE_NAME_INVALID 흡수        (3단계, §4)
```

가운데 것을 `UNSUPPORTED_SCALAR`로 제안하는 이유는, PUA·태그·VS보충은 "미할당"이
아니라 "할당돼 있지만 이름에 쓰지 않기로 정한 것"이라 `UNASSIGNED`와 사유가 다르기
때문입니다. 사용자에게 하는 설명도 달라집니다.

3단계 규칙은 스칼라가 아니라 **배열**에 대한 제약이라 새 코드를 주기 애매합니다.
`STORAGE_NAME_INVALID`로 흡수하되 계약 문안에 사유를 적는 쪽을 제안합니다.

### §6 Windows 진행 계획 — 이견 없습니다

1번(동결 표 채택)이 동작 무변화라는 것은 저희 쪽 재현으로도 확인됩니다(§1).
지금 넣으셔도 안전합니다.

---

## 6. 계약 자산 위치 — 초안

`sync-contract/`는 저희가 잡겠습니다. 구조 초안입니다. **아직 만들지 않았습니다** —
합의 후에 넣겠습니다.

```
sync-contract/unicode/
  casefold-15.0.0.json          1,530 매핑
  assigned-baseline-14.0.0.json   698 범위 / 282,230 스칼라
  excluded-scalars.json          PUA + 태그 + VS보충 (2단계 확정 후)
  README.md                      생성 절차와 출처
contract-lock.json               위 파일들의 sha256 추가
```

방향 뒤집기는 이렇게 하려 합니다.

```
현재   마이그레이션 SQL(원본) -> 나머지가 파생
개정   sync-contract/unicode/*.json(원본) -> SQL 생성 -> 클라이언트 두 곳이 참조
       supabase/scripts/generate_casefold_sql.py 를 JSON 읽는 쪽으로 수정
```

이러면 그쪽이 정규식으로 SQL을 파싱하는 일도, 저희가 같은 짓을 하는 일도 없어집니다.
경로가 확정되면 알려 드리겠습니다.

---

## 7. 정리된 단계

```
1단계  동결 casefold 표                  구현 차이 181 해소.  잔여 40.
       (분리자 순서 수정은 digest 변경이라 2단계와 함께)
2단계  계약 개정 — 분리자 순서 + 베이스라인 게이트 + PUA·태그·VS 제외
       + 서버 수정 + allowlist 등록 + 핀 교체        잔여 40 해소.
3단계  "상위면 + 인접 결합문자" 거부 규칙  상위면 결함 해소. NFKC 자체 구현 불필요.

배선   2단계 완료 후                     (3단계는 배선 전에 넣는 것을 권합니다)
```

3단계가 규칙 하나로 끝나므로 2단계와 같은 개정에 묶어도 됩니다. 그러면 개정이 한 번에
끝나고 digest도 한 번만 움직입니다. 나눌 이유가 있으면 말씀해 주세요.

---

## 8. 남은 것

```
1  실기기 iPad 저장소 이름 스캔          2단계 착수 전
2  계약 자산 경로 확정                   저희가 초안, 그쪽 확인
3  개정 문안 작성                        분리자 순서 + 게이트 + 제외 + 3단계 규칙
```

측정 쪽은 이번으로 다 닫혔다고 봅니다. 더 재야 할 것이 보이면 말씀해 주세요.

---

## 재현 정보

```
런타임    iPadOS 26.5 시뮬레이터 (iPad Pro 13-inch M5), ICU 78.1 / Unicode 17.0.0
          macOS 26.5.2 (25F84)
기준      unicodedata2 15.0.0 (NFKC) + 마이그레이션 SQL의 unicode15_casefold 1,530 매핑
          -> Windows 네 다이제스트를 전부 재현하여 교차 검증 완료
피시험    WriterPad/Sync/SyncV2Contract.swift (무수정)
하이브리드 위 파일의 casefold 단계만 동결 표로 치환
```
