# Windows v2 확정 후 재검토 메모

- 작성일: 2026-07-15
- 관련 프로젝트: iPadOS 집필프로그램
- 목적: Windows v2 구현이 확정된 뒤 iPad 개발 기획과 다시 대조할 항목을 보관한다.

## 1. Supabase 데이터 구조

Windows v2의 실제 스키마를 받은 뒤 다음을 확인한다.

- 프로젝트 테이블 또는 이에 대응하는 프로젝트 저장 구조
- 프로젝트 소유권, 표시 순서, 이름 변경, 삭제 상태
- 폴더와 문서 계층을 나타내는 `parent_id`
- 파일과 폴더를 구분하는 노드 종류
- 사용자 지정 정렬 순서
- `project_id`와 상대 경로의 중복 방지 방식
- 이름이나 경로가 바뀌어도 `document_id`가 유지되는지
- iPad SwiftData 메타데이터와 서버 필드의 대응 관계

상태: Windows v2 전환 진행 중. 확정 자료를 받은 뒤 검토한다.

## 2. 이름 변경·이동·삭제·일괄 생성의 원자적 처리

Windows 환경의 구현을 먼저 확인한 뒤 iPad에서 동일한 규약을 사용한다.

- 문서와 폴더 이름 변경
- 부모 폴더 또는 상대 경로 이동
- 휴지통 이동, 복원, 영구 삭제
- 새 권과 25개 화의 일괄 생성
- 각 작업의 `operation_id` 기반 중복 처리 방지
- 서버 성공 후 응답이 유실된 경우의 안전한 재시도
- 일괄 작업 일부만 반영된 경우의 재시도 또는 롤백
- 본문 `revision`과 메타데이터 `metadata_revision`의 분리 방식
- 본문 저장 RPC 이외의 메타데이터 전용 RPC 또는 동등한 트랜잭션 처리

상태: Windows v2에서 확인 후 iPad 규약과 대조한다.

## 3. 인증과 RLS

Windows v2에서 진행 중인 다음 내용을 확정 후 전달받는다.

- Supabase 로그인 방식
- Auth 세션 저장과 자동 갱신
- 로그아웃, 세션 만료, 인증 실패 처리
- 네트워크 단절 중 인증 상태 처리
- 프로젝트 및 문서별 소유권 검사
- RPC 내부의 `owner_id` 재검증
- Realtime 비공개 채널 접근 권한
- 클라이언트에 publishable/anon 키만 포함되는지
- service role 키가 클라이언트에 포함되지 않는지

상태: Windows v2에서 진행 중. 확정 후 iPad 인증 흐름을 설계한다.

## 4. 3방향 병합용 Base 원고 보존

Base는 Local과 Remote의 변경점을 판단하는 공통 기준 원고다. Windows v2의 버전 보관 방식을 확인한 뒤 다음을 결정한다.

- Base 본문을 각 기기에 로컬 보존할지
- 서버 `document_versions`에서 Base revision을 다시 가져올지
- 영구 동기화 큐에 Base 본문을 넣을지, Base revision만 넣을지
- 장기 오프라인 상태에서도 필요한 Base 버전이 삭제되지 않게 하는 정책
- 같은 문서의 로컬 작업이 여러 개 누적될 때 사용할 Base
- 충돌 해결 전까지 Base·Local·Remote를 모두 보존하는 위치
- 충돌 해결과 재커밋이 끝난 뒤 Base를 정리하는 시점

상태: 별도 결정 필요. Windows v2의 revision 및 버전 보존 방식 확정 후 재검토한다.

## 5. iPad 백그라운드 전환과 동기화 기준

공통 동기화 규약은 Windows v2 확정 후 대조하고, iPadOS의 제한은 iPad 구현 단계에서 별도로 반영한다.

- background 진입 전에 로컬 TXT 저장을 어디까지 완료할지
- 저장 중인 텍스트 스냅샷과 save generation 처리
- 서버 업로드가 완료되지 않으면 영구 큐에 남기는 기준
- 제한된 백그라운드 실행 시간이 끝난 경우의 중단 처리
- 앱이 종료되어도 임시 파일과 동기화 작업을 복구하는 방법
- 다음 실행에서 미완료 작업을 재개하는 순서
- 상태 배지에서 `로컬 저장 완료`와 `서버 동기화 대기`를 구분하는 방법

상태: Windows v2 확정 후 공통 규약을 전달받아 재검토한다.

## 6. 확정된 TXT 전체 추출 중단 규칙

이 항목은 Windows v2와 무관하게 현재 확정된 요구사항이다.

- 화 번호순으로 원고를 검사한다.
- 글자 수에는 공백을 포함한다.
- 본문이 300자 미만인 화를 처음 발견하면 추출을 중단한다.
- 300자 미만인 해당 화와 그 이후 화는 추출 결과에 넣지 않는다.
- 300자 이상인 직전 화까지만 포함한다.
- 전체 원고 추출에서 이 옵션을 기본으로 켠다.
- 사용자가 옵션을 끌 수 있다.
- 범위 지정 추출에는 기본 적용하지 않는다.
- 완료 결과에 중단된 화와 실제 마지막 포함 화를 표시한다.

## 7. 추후 전달받을 자료

Windows v2가 확정되면 가능하면 다음 자료를 전달받아 iPad 기획과 비교한다.

- 최종 Supabase SQL migration
- 테이블과 필드 명세
- RPC 함수명, 입력값, 반환값, 충돌 응답 형식
- RLS 정책
- 인증 및 세션 흐름
- 동기화 큐 구조
- revision과 metadata revision 갱신 규칙
- document version 보존 정책
- 백그라운드 또는 종료 직전 저장 규칙
- Windows에서 사용하는 파일명·경로 검증 규칙
