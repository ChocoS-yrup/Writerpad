# 인수인계 — storage-name 유니코드 계약 개정

작성 2026-08-13. 앞선 세션이 토큰을 다 써서 넘깁니다.
**이 문서를 읽는 당신은 이 저장소의 파일 말고는 아무 맥락이 없습니다.** 그래서
배경부터 적습니다. 길지만 순서대로 읽으면 지금 상태가 그대로 재구성됩니다.

---

## 0. 30초 요약

이 앱의 폴더·파일 이름은 서버에 올라갈 때 "충돌 키"로 정규화됩니다. 계약은 그 정규화를
Unicode 15.0.0으로 하라고 요구하는데, **iPad 구현이 그걸 못 지키고 있었습니다.**
전 코드포인트를 실측해 원인을 밝히고, 절반을 고쳤고, 나머지는 계약 개정안으로 정리해
두었습니다.

```
[x] 원인 규명       Foundation 접기가 default case folding이 아님 + NFKC 절단 결함
[x] 1단계 구현      접기를 동결 표로 교체. 커밋 4개. 수용 기준 통과.
[x] 계약 자산       sync-contract/unicode/ 로 표 4개 올림
[x] 개정안 초안     계약개정안_storage-name-v2_초안.md
[ ] 실기기 스캔     개정 동결 전 선행 조건. 남은 유일한 측정.
[ ] 2·3단계         계약 개정 -> 서버 -> allowlist -> 클라이언트 핀 (순서 엄수)
[!] 절대 금지       iPad 동기화 배선 시작
```

---

## 1. 이 프로젝트가 무엇인가

**WriterPad** — 소설 집필용 앱입니다. 사용자는 작가이고 한국어로 씁니다.

같은 작품을 **두 클라이언트**가 씁니다.

```
iPad   Swift/SwiftUI 앱.  이 저장소의 WriterPad/
Windows 파이썬 데스크톱 앱. 별도 코드베이스. 이 저장소에 없음.
서버   Supabase(Postgres). 이 저장소의 supabase/
계약   두 클라이언트가 지켜야 하는 규약. 이 저장소의 sync-contract/
```

### 저장소 지형 — 여기가 가장 헷갈리는 지점

**계약 원본과 서버가 둘 다 이 저장소(iPad 쪽)에 있습니다.** Windows는 계약을 받아
쓰는 쪽이고, 자기 코드는 다른 곳에 있습니다.

```
/Users/chocos/Documents/작가님 힘내세요/     <- 여기
├─ sync-contract/     계약 원본 (protocol.json, 벡터, 스키마, lock)
├─ supabase/          서버 (마이그레이션, RPC)
├─ WriterPad/         iPad 앱
└─ WriterPadTests/

Windows 쪽 (여기 없음)
   sync_contract.py, Antigravity_AI_Writer.spec, requirements-stage8.txt
```

따라서 **계약을 고치는 것도, 서버를 고치는 것도 이쪽 일**입니다.

### 두 AI 세션이 문서를 주고받으며 협업 중입니다

사용자가 Windows 세션과 iPad 세션(=여기)을 각각 띄워 두고, 한쪽 결과를 다른 쪽에
복사해 붙이는 방식으로 진행했습니다. 그 교환 기록이 저장소에 파일로 남아 있습니다.

```
iPad -> Windows   윈도우세션_회신_유니코드측정.md      (1차)
                  윈도우세션_회신2_추가측정.md
                  윈도우세션_회신3_규약확정.md
                  윈도우세션_회신4_S8확정.md           (마지막, 답신 아직 없음)
Windows -> iPad   윈도우세션_답신모음.md               (3건 원문)
근거 자료         유니코드측정_실측데이터_20260813.json
                  유니코드측정_추가측정_20260813.json
                  유니코드측정_3차측정_20260813.json
                  유니코드측정_4차측정_20260813.json
결과물            계약개정안_storage-name-v2_초안.md
```

**새 세션에서 이 일을 이어받는다면 위 파일들을 먼저 읽으십시오.** 특히
`윈도우세션_답신모음.md`에 Windows 쪽이 합의한 조건들이 들어 있습니다.

브랜치는 `feat/sync-v2-structure-recovery`입니다.

---

## 2. 문제가 무엇이었나

### 2-1. storage-name-v1 이 무엇인가

같은 폴더 안에 이름이 겹치는 항목을 못 만들게 해야 합니다. 그런데 iPad는 한글 자모가
분해된 이름(`ᄀ ᅡ`)을, Windows는 결합된 이름(`가`)을 만들 수 있습니다. 눈에는 같은데
바이트가 다릅니다. 그래서 이름을 하나의 키로 수렴시킨 뒤 그 키의 **정확한 UTF-8
바이트**로 충돌을 판정합니다.

계약이 정한 파이프라인입니다.

```
1  거부: U+0000..U+001F, U+007F, '/', '\'
2  NFKC
3  Unicode 15.0.0 default case folding (로케일 없이)
4  NFKC
5  끝의 U+0020 / U+002E 제거
6  빈 결과 / "." / ".." 거부
7  첫 '.' 앞부분이 Windows 예약어면 거부 (CON, PRN, ... LPT9)
8  UTF-8 인코딩 -> 이 바이트가 충돌 키
```

정의는 `sync-contract/protocol.json`의 `storage_name_normalization`에 있습니다.
적합성 벡터 15개가 `sync-contract/conformance_vectors/storage-name-v1.json`에 있습니다.

### 2-2. 발단 — Windows 세션이 던진 질문

Windows 세션이 자기 쪽 실측을 끝내고 개정 방향을 제안하면서, iPad 쪽 자료 하나가
재현되지 않는다고 물어왔습니다(그 요청서는 `~/Downloads/ipad_prompt.md`).

핵심은 이것이었습니다. iPad 코드에 `SyncV2StorageName.divergentScalars`라는 **92개**
목록이 있고 "판이 달라도 갈릴 수 있는 문자는 이 92개뿐"이라고 적혀 있었는데,
Windows에서는 어떤 버전 쌍으로도 92가 안 나온다는 것이었습니다.

```
92가 버전 차이라면  -> 베이스라인 표(미할당 거부)로 덮을 수 있다
92가 구현 차이라면  -> 못 덮는다. 개정안을 다시 설계해야 한다
```

### 2-3. 답 — 92는 실측값이 아니었습니다

`divergentScalars`는 스캔 결과가 아니라 **손으로 적어 넣은 상수**였습니다. 시험은
개수가 92인지만 셌기 때문에 값이 틀려도 통과했습니다. 생성 스크립트는 저장소에
없었습니다.

실제로 전 코드포인트(1,112,064개)를 훑었더니:

```
실제 발산            276개  (92가 아님)
  베이스라인 안쪽    181개  <- 구현 차이. 미할당 거부로 못 잡는다.
  베이스라인 바깥     95개  <- 버전 차이.
```

**우려하던 나쁜 쪽이 실재했고, 개정안은 그대로는 성립하지 않았습니다.**

---

## 3. 실측으로 밝혀진 것 전부

여기가 이 인수인계의 핵심입니다. 숫자는 전부 재현된 것입니다.

### 3-1. iPad 런타임

```
ICU u_getUnicodeVersion  ->  17.0.0
ICU u_getVersion         ->  78.1
Swift stdlib 최신 age     ->  17.0
iPadOS 26.5 / macOS 26.5.2 결과 완전 동일 (전 코드포인트 다이제스트 일치)
```

Swift에는 판 번호 API가 없지만 `dlsym`으로 ICU에 직접 물으면 나옵니다. 다만
`libicucore`는 Apple 비공개 프레임워크라 App Store 심사 위험이 있어 진단용으로만
쓸 수 있습니다.

### 3-2. 결함 1 — 접기가 default case folding이 아니다

`String.folding(options: [.caseInsensitive])`는 Foundation 자체의 비교용 접기입니다.
Unicode default case folding과 다릅니다.

```
             계약/서버      Foundation
U+13A0  ->   U+13A0        U+AB70      Cherokee. 접는 방향이 반대.
U+AB70  ->   U+13A0        U+AB70
U+1C80  ->   U+0432 (в)    U+1C80      Cyrillic. 아예 안 접음 -> 충돌을 놓침.
```

Cherokee는 소문자가 나중에(Unicode 8.0) 추가된 탓에 안정성 정책 때문에 default case
folding이 **대문자 쪽으로** 접힙니다. Foundation은 소문자 쪽으로 접습니다.
등가류는 같지만 대표 바이트가 달라 서버와 다른 키가 나옵니다.

Cyrillic U+1C80..U+1C88은 등가류 자체가 달라집니다. 서버는 `ᲀ`와 `в`를 충돌로 보는데
iPad는 다른 이름으로 봅니다. **놓치는 충돌**입니다.

이 결함이 베이스라인 안쪽 181개의 정체입니다(Cherokee 172 + Cyrillic 9).
**전부 접기 단계이고 NFKC는 무죄입니다** — 181개 모두 NFKC 분해가 없는 문자입니다.

### 3-3. 결함 2 — NFKC의 상위면 절단

`precomposedStringWithCompatibilityMapping`이 상위면 스칼라 **바로 뒤에** 결합문자가
오면 코드포인트를 16비트로 잘라 처리합니다.

```
U+10041 U+0301  ->  U+00C1   ("Á")     정답: U+10041 U+0301
U+13046 U+FF9E  ->  U+3094   ("ゔ")    정답: U+13046 U+3099
```

이집트 상형문자에 반각 탁점을 붙이면 일본어 가나가 됩니다. `𐁁́`와 `á`가 **같은 저장
이름**이 됩니다.

규모(결합문자 U+0301 하나 기준):

```
영향받는 상위면 스칼라   1,911개  (면마다 117개씩 균일 = 절단의 증거)
  베이스라인 안쪽          548개
  PUA 등 제외 후           268개
```

**발현 조건을 일곱 가지 배열로 확정했습니다.**

```
배열                          X 뒤 인접?   베이스라인안+PUA제외
X + 결합문자                     예              270
'a' + X + 결합문자               예              270
X + 결합문자 2개                 예              285
X + X + 결합문자                 예              270
X + 'a' + 결합문자              아니오             0
결합문자 + X                    아니오             0
X + 'a'                        아니오             0
```

**인접이 필요조건입니다.** 그래서 NFKC를 직접 구현하지 않고 규칙 하나로 막을 수
있습니다. 이것이 개정안 3번 단계입니다.

### 3-4. 위장 결합문자 5개 (Windows가 찾음, iPad가 확인)

입력 시점엔 ccc=0인데 NFKC가 결합문자로 바꿔 놓는 스칼라입니다. 규칙을 통과한 뒤
위험해집니다.

```
U+0F73 U+0F75 U+0F81  -> U+0F71...   결합 상대 0개  -> 규칙에 넣을 필요 없음
U+FF9E  반각 탁점      -> U+3099      결합 상대 96개 -> 넣어야 함
U+FF9F  반각 반탁점    -> U+309A      결합 상대 20개 -> 넣어야 함
```

실측 결과 U+FF9E는 96개, U+FF9F는 20개가 실제로 발산했습니다(전부 절단형).
Windows가 예측한 결합 상대 수와 정확히 일치합니다.

### 3-5. 안전이 확인된 것

```
이모지            9종 조합(U+FE0F, U+FE0E, U+20E3, ZWJ 포함) 전부 발산 0
한글 자모         U+1161, U+11A8 등 ccc=0인데 결합하는 계열 -> 0
                  (U+11100 & 0xFFFF = U+1100 이라 위험할 뻔했으나 안전)
CJK 호환 7개      U+FA6C 등, NFKC가 상위면을 만들지만 결합 상대 없음
```

한글 자모는 이 앱에서 가장 위험한 계열이라(iPad가 분해된 자모를 만들고 이름이
한국어) 따로 쟀습니다. 절단은 정준 결합 경로에서만 일어나고 한글의 알고리즘 결합
경로는 타지 않습니다.

### 3-6. 계약 자체의 결함 — 분리자 7개 (양쪽 공통)

거부 검사가 NFKC **앞**에 있어서, NFKC가 만들어 내는 분리자를 놓칩니다.

```
U+2100 -> "a/c"   U+2101 -> "a/s"   U+2105 -> "c/o"   U+2106 -> "c/u"
U+FE68 -> "\"     U+FF0F -> "/"     U+FF3C -> "\"
```

iPad와 Windows에서 각각 독립 열거해 **같은 7개**가 나왔습니다. iPad 잘못이 아니라
계약대로 구현한 결과이고, `separator_policy`("Separators are rejected, not rewritten")와
`steps`의 순서가 서로 모순입니다.

### 3-7. 베이스라인 표의 설계 결함 (Windows가 인정)

Windows가 보낸 Unicode 14.0.0 할당표가 `category != Cn` 기준이라 사설 사용 영역이
통째로 들어가 있었습니다.

```
전체     282,230
PUA      137,468   <- 표의 절반
태그          97
VS 보충      240
제외 후  144,425
```

그래서 PUA·태그·VS보충을 이름에서 금지하기로 했습니다. **U+FE00..U+FE0F(BMP 변이
선택자)는 제외하지 않습니다** — IME가 이모지에 자동 삽입하는 문자이고 발산 0건이라,
막으면 정상 이모지 폴더명이 거부됩니다.

### 3-8. 게이트는 처음부터 없었습니다

Windows는 "iPad가 버전 검사에 막혀 있다"고 전제하고 논의했는데, 사실이 아니었습니다.

```
UNICODE_VERSION_MISMATCH   저장소 0건, 전 브랜치 이력 0건
                           protocol.json 오류 코드 어휘에도 없음
storageNameUnicodeVersion  선언만 있고 참조 0건
SyncV2StorageName          계약 파일 밖에서 호출 0건
서버 RPC의 unicode 검사     없음
```

`UNICODE_VERSION_MISMATCH`는 계약 어휘가 아니라 **Windows가 자기 `unicodedata2`
의존을 지키려고 세운 로컬 방어 장치**였고, 그것이 계약 공통 게이트로 일반화된 것이
착오의 원인이었습니다.

**중요 — 그래서 실피해는 0입니다.** 잘못된 키를 계산하는 코드는 있지만 아무 데서도
호출되지 않습니다. 배선하는 순간 위험해집니다.

---

## 4. 다이제스트 — 판정에 쓰는 값

전 코드포인트를 한 값으로 압축해 양쪽 구현이 같은지 확인합니다.

```
규약 (양쪽 합의)
  각 코드포인트마다  "%04X" + ":" + (정규화 결과 | 오류 코드) + LF
  U+0000 오름차순, 서로게이트 제외
  주 다이제스트는 오류 코드 문자열, 보조는 빈 문자열을 쓴다
```

```
계약 기준 (= Windows)     b9368cb675858781cc42633c87b5501aa784b28a7aba858124aef5e2861dc1af
  보조                    c07ed7a48a03f83688e8e05896fc3fee68d2554b992efe1af559448a986a1f27
iPad 1단계 이전            6c0a85edcaea6cfebd58dd8b29c72ea6a0c3c8461abcc36735d59f5004e4d31e
iPad 1단계 이후 (현재)     72d107e04a09413ec068328d4a737739e71e45136b7f89fb304db4dbdaf3021f  <- 달성
  보조                    baea29956b7ad40b8f033c1502d54e33b75787f6283bf13c86ae8109e205f549
거부되는 단독 스칼라        59개 (양쪽 일치)
```

**iPad 세션의 기준 구현이 Windows의 네 다이제스트를 전부 재현했습니다.** 서로 다른
두 사슬이 1,112,064개 코드포인트에서 바이트 단위로 일치한다는 뜻이고, 기준선은 양쪽
독립 확인됐습니다.

현재 계약 기준과 다른 스칼라는 **40개**입니다.

```
U+A7F1(1) / U+16D68..U+16D6A(3) / U+1CCD6..U+1CCF9(36)
전부 베이스라인 바깥 -> 2단계 게이트가 잡는다
```

`72d107e0...`이 아닌 값이 나오면 1단계 구현이 합의된 것과 다르다는 뜻입니다.

---

## 5. 저장소에 실제로 바뀐 것

커밋 4개입니다(전부 `feat/sync-v2-structure-recovery` 브랜치, 푸시 안 함).

```
1a5938a  feat: 대소문자 접기를 런타임 대신 동결 표로 한다
76d494b  test: 접기 교체가 베이스라인 안쪽 발산을 닫은 것을 못 박는다
8753cde  refactor: 유니코드 표를 계약 자산으로 올리고 생성 방향을 뒤집는다
596bf5b  docs: storage-name 개정안 초안을 적는다
```

### 5-1. `WriterPad/Sync/SyncV2UnicodeCasefold.swift` (신규)

Unicode 15.0.0 full default case folding 표 1,530개를 담은 Swift 파일입니다.
문자열로 압축해 싣고 처음 쓸 때 한 번 풉니다(딕셔너리 리터럴 1,530개는 타입 검사가
느려서).

```
정규형 sha256  eac289d0d721c58867acb07af38d9a8e8ee374d328b33d93251ae6348e258439
```

**이 파일은 `WriterPad.xcodeproj/project.pbxproj`에 4곳 등록돼 있습니다.**
(PBXBuildFile / PBXFileReference / 그룹 children / Sources 빌드 단계)
이 프로젝트는 파일시스템 동기화 그룹을 안 쓰는 구식 pbxproj(objectVersion 56)라,
**새 파일을 만들면 반드시 수동 등록해야 합니다. 안 하면 조용히 컴파일에서 빠집니다.**

### 5-2. `WriterPad/Sync/SyncV2Contract.swift` (수정)

한 줄이 핵심입니다.

```swift
// 이전
normalized = normalized.folding(options: [.caseInsensitive], locale: nil)
// 이후
normalized = SyncV2UnicodeCasefold.apply(normalized)
```

그 외에 `divergentScalars`를 실측값 40개로 바꾸고 주석을 사실에 맞게 고쳤습니다.
(예전 주석은 "갈릴 수 있는 문자는 92개뿐"이라고 단언하고 있었습니다.)

### 5-3. `WriterPadTests/SyncV2ContractTests.swift` (수정)

시험 3개를 새로 넣었습니다.

```
testFrozenCasefoldTableIsIntact              표 정규형 sha256 + 1,530개 + 다중 매핑
testFrozenCasefoldClosesInsideBaselineDivergence
                                             Cherokee/Cyrillic이 계약대로 나오는지
testDivergentScalarsMatchMeasurement         남은 40개 구성
```

기존 시험 중 `testSupplementaryPlaneNFKCTruncates_CONTRACT_VIOLATION_PINNED`는
**일부러 계약 위반 동작을 고정해 둔 것**입니다. 3단계에서 규칙이 들어가면 이 시험을
고치지 말고 **삭제**하십시오(파일 주석에도 그렇게 적혀 있습니다).

### 5-4. `sync-contract/unicode/` (신규 5개 파일)

```
casefold-15.0.0.json            1,530 매핑   eac289d0...
assigned-baseline-14.0.0.json   698 범위     aed4e530...   (Windows가 보낸 표)
excluded-scalars.json           PUA·태그·VS  f56ee73b...
assigned-15.0.0.json            707 범위     5a354149...   (서버가 쓰는 것)
README.md                       출처·정규형·소비자
```

`assigned-baseline-14.0.0.json`(14.0.0)과 `assigned-15.0.0.json`(15.0.0)은 **다른
표이고 서로 바꿔 쓰면 안 됩니다.** 앞은 개정이 클라이언트를 옮겨 갈 표, 뒤는 지금
배포된 서버가 내부적으로 쓰는 표입니다.

### 5-5. `supabase/scripts/generate_casefold_sql.py` (수정)

호스트 CPython의 `unicodedata`를 읽던 것을 **JSON 자산을 읽도록 뒤집었습니다.**
이제 아무것도 유도하지 않고 표를 SQL로 옮기기만 합니다.

예전에는 인터프리터 버전(3.12.13)과 Unicode 15.0.0을 못 박아 놓고 다른 기계에서는
실행 자체를 거부했습니다 — 클라이언트에서 없앤 것과 똑같은 취약점이었습니다.

**검증 명령이 중요합니다.**

```bash
python3 supabase/scripts/generate_casefold_sql.py --check supabase/migrations/20260811010000_sync_contract_0_1_0_foundation.sql
```

이미 적용된 마이그레이션이 한 바이트도 안 바뀌었음을 확인합니다.
**그 마이그레이션은 이미 적용됐으므로 절대 다시 쓰지 마십시오.**

CR이 섞이면 스크립트가 실행을 거부합니다. 베이스라인 표가 Windows 전송을 거치며
CRLF로 바뀌어 와서 다이제스트가 어긋났던 적이 있어서 넣은 방어입니다.

### 5-6. 건드리지 않은 것

```
sync-contract/protocol.json          그대로
sync-contract/contract-lock.json     그대로
canonical_contract_sha256            416c1b99edb9bda694731dee4b25688d9d82d1f32610aa23ddfda571ec3c7670
supabase/migrations/*                그대로
배포                                  안 함
```

계약 다이제스트가 서버 allowlist보다 앞서 나가면 `CONTRACT_DIGEST_MISMATCH`로 모든
쓰기가 막힙니다. 그래서 순서가 강제됩니다(§7).

---

## 6. 개정안 요약

전문은 `계약개정안_storage-name-v2_초안.md`입니다. 핵심만 옮깁니다.

`algorithm_id`를 `storage-name-v2`로 올리고 `unicode_version: "15.0.0"`을 삭제합니다.
단계를 11개로 다시 씁니다.

```
1  베이스라인 표에 없는 스칼라 거부        STORAGE_NAME_UNASSIGNED
2  제외 표에 있는 스칼라 거부              STORAGE_NAME_UNSUPPORTED_SCALAR
3  상위면 스칼라 바로 뒤가 ccc≠0 또는
   U+FF9E/U+FF9F 면 거부                  STORAGE_NAME_INVALID
4  NFKC
5  동결 표로 default case folding
6  NFKC
7  제어문자·'/'·'\' 거부                   STORAGE_NAME_INVALID   <- 여기로 이동
8  끝의 공백/마침표 제거
9  빈 결과 / "." / ".." 거부
10 예약 basename 거부                     STORAGE_NAME_RESERVED
11 UTF-8 인코딩
```

### 이 개정에서 가장 실수하기 쉬운 지점

**3번과 7번은 서로 반대 방향입니다. 하나로 묶으면 반드시 한쪽이 깨집니다.**

```
3번  NFKC '이전'이어야 한다.  NFKC가 상위면 스칼라를 없앤다(U+10041 U+0301 -> U+00C1).
                            뒤에서 보면 검사할 대상이 남아 있지 않다.
7번  NFKC '이후'여야 한다.    NFKC가 분리자를 만들어 낸다(U+FF0F -> '/').
                            앞에서 보면 7개를 놓친다.
```

### 벡터 추가 (SN-016..SN-029)

기대값은 전부 동결 표 기준 구현으로 계산해 확인했습니다. 그중 둘이 특히 중요합니다.

```
SN-026  U+13046 U+0061   통과, f093818661       <- 3번을 "상위면 전체 금지"로
                                                   과잉 구현하는 것을 막는다
SN-027  U+1F642 U+FE0F   통과, f09f9982efb88f   <- 제외 목록을 "변이선택자 전부"로
                                                   쓸어 담는 것을 막는다
```

기존 SN-001..015는 **한 개도 안 바뀝니다**(실측 확인).

### 서버도 반드시 같이 고쳐야 합니다

`private.nfkc_unicode15`는 미할당 문자를 거부하지 않고 **버퍼를 끊어 그대로 통과**시킨
뒤 이어서 정규화합니다. 개정 후 클라이언트는 그런 이름을 거부하므로, 빠뜨리면
**서버만 받아 주는 이름**이 생깁니다.

### 재키잉은 필요 없습니다

받아들이는 이름의 키는 하나도 안 바뀌고, 받아들이던 이름 일부가 거부로 바뀔 뿐입니다.
기존 데이터 영향은 0건으로 확인됐습니다(단 §8-1 참고).

---

## 7. 배포 순서 — 어기면 전면 차단

```
1  iPad 실기기 이름 스캔 (0건 확인)          <- 아직 안 함
2  개정 문안 동결 (protocol.json, lock, 벡터, 스키마)
3  서버 수정 + 새 digest allowlist 등록 + 배포
4  클라이언트 핀 교체 (Windows / iPad 각각)
5  그 다음에야 iPad 동기화 배선
```

3번 전에 4번을 하면 클라이언트가 `CONTRACT_DIGEST_MISMATCH`로 막히고,
4번 전에 5번을 하면 잘못된 키가 서버에 올라갑니다.

---

## 8. 다음에 할 일

### 8-1. 실기기 이름 스캔 (개정 동결 전 유일한 선행 조건)

실제 iPad에 있는 작품 폴더·파일 이름에 PUA·태그·VS보충·상위면 문자가 있는지
확인해야 합니다. 있으면 이관 문제가 됩니다.

```
Windows 실사용 15,952개    0건 (완료)
iPad 이 Mac에서 닿는 1,424개  0건 (완료, 단 실기기 데이터 아님)
iPad 실기기                  미확인   <- 남은 것
```

방법은 두 가지입니다. 앱에 일회성 검사 코드를 넣어 실기기에서 돌리거나, 아이패드를
Mac에 연결해 백업으로 확인하거나. 사용자에게 어느 쪽이 편한지 물어보십시오.

### 8-2. Windows 답신 대기 (회신 4에 대한)

`윈도우세션_회신4_S8확정.md`를 보냈고 아직 답이 없습니다. 물어본 것 세 가지:

```
1  방어적 재검사(7번 뒤 베이스라인 재확인)를 넣을지   -> iPad는 넣는 쪽
2  storage_name_v1 능력을 서버가 언제까지 받아줄지
3  verify_contract.py 도 동결 표를 읽도록 뒤집을지
```

### 8-3. 2·3단계 구현 (개정 확정 후)

```
iPad     1~3번 검사 추가 / 7번 검사 이동 / 능력 선언 storage_name_v2 / 핀 4개
서버     storage_name_v2 로 규칙 교체 / 베이스라인 범위 테이블 신설 / allowlist
Windows  같은 변경 + unicodedata2 제거
```

---

## 9. 절대 하지 말 것

```
[!] iPad 동기화 배선을 시작하지 말 것
      SyncV2StorageName 이 아직 아무 데서도 호출되지 않는다는 것이
      잘못된 키가 서버에 올라간 적이 없는 유일한 이유다.
      배선이 곧 마감 시한이다. 2단계, 되도록 3단계까지 끝난 뒤에.

[!] protocol.json / contract-lock.json / canonical_contract_sha256 을
      서버 allowlist 등록보다 먼저 바꾸지 말 것

[!] supabase/migrations/* 를 다시 쓰지 말 것 (이미 적용됨)

[!] 개수만 세는 시험을 다시 만들지 말 것
      "92개인지 센다"가 정확히 이번 사고의 원인이다.
      값이 틀려도 통과했다.
```

---

## 10. 측정 환경 재현

앞선 세션의 작업 폴더는 세션 전용이라 사라집니다. 다시 만드는 법입니다.

### 10-1. 기준 구현 (Unicode 15.0.0)

```bash
python3 -m venv venv
./venv/bin/pip install "unicodedata2==15.0.0" rfc8785 jsonschema
```

이 Mac의 시스템 파이썬은 3.9.6 / Unicode 13.0.0이라 그대로는 못 씁니다.
`unicodedata2` 15.0.0이 Windows가 쓴 것과 같은 자료입니다.

기준 파이프라인은 이렇게 만듭니다.

```python
import unicodedata2 as u15, json
CF = {int(s,16): "".join(chr(int(d,16)) for d in v)
      for s, v in json.load(open("sync-contract/unicode/casefold-15.0.0.json"))["mappings"].items()}
def key(s):
    n = u15.normalize("NFKC", s)
    n = "".join(CF.get(ord(c), c) for c in n)
    return u15.normalize("NFKC", n).rstrip(" .")
```

### 10-2. iPad 런타임에서 실제로 돌리기

앱 전체를 빌드하지 않고 계약 파일만 따로 컴파일해 시뮬레이터에서 돌릴 수 있습니다.
훨씬 빠릅니다.

```bash
SDK=$(xcrun --sdk iphonesimulator --show-sdk-path)
swiftc -O -target arm64-apple-ios26.5-simulator -sdk "$SDK" \
  WriterPad/Sync/SyncV2Contract.swift \
  WriterPad/Sync/SyncV2UnicodeCasefold.swift \
  stubs.swift main.swift -o probe
xcrun simctl boot 39ADD620-67C9-4F0A-8047-C32AC64D448A   # iPad Pro 13-inch (M5)
xcrun simctl spawn 39ADD620-67C9-4F0A-8047-C32AC64D448A ./probe
```

`stubs.swift`가 필요합니다. `SyncV2Contract.swift`가 다른 파일의 enum 두 개를
참조하기 때문입니다. 원본과 같게 적으십시오.

```swift
enum SyncV2OperationStatus: String, Codable, Equatable, Sendable {
    case pending, inflight
    case retryWait = "retry_wait"
    case conflict, completed, cancelled, blocked
}
enum SyncV2CommitStatus: String, Decodable, Equatable, Sendable {
    case committed, replayed
}
```

시뮬레이터가 잠들면 `spawn`이 "device is not booted"로 실패합니다. 다시 `boot`
하십시오.

### 10-3. 계약 검증기

```bash
python3 sync-contract/scripts/verify_contract.py
```

**이 검증기는 호스트 CPython의 `unicodedata`가 15.0.0일 것을 요구해서 이 Mac에서는
그냥 돌지 않습니다.** ("Python Unicode data version mismatch: expected 15.0.0, got
13.0.0") 클라이언트에서 없앤 취약점이 검증기에 그대로 남아 있습니다(개정안 §8의
열린 항목).

우회해서 돌리는 법입니다. 저장소는 안 건드립니다.

```python
# run_verify.py — 검증 전용 실행기
import sys, runpy, unicodedata2
sys.modules['unicodedata'] = unicodedata2
sys.argv = ['verify_contract.py']
runpy.run_path('sync-contract/scripts/verify_contract.py', run_name='__main__')
```

정상이면 마지막 줄이 이렇게 나옵니다.

```
Canonical SHA-256: 416c1b99edb9bda694731dee4b25688d9d82d1f32610aa23ddfda571ec3c7670
```

### 10-4. 앱 시험

```bash
xcodebuild test -project WriterPad.xcodeproj -scheme WriterPad \
  -destination 'platform=iOS Simulator,id=39ADD620-67C9-4F0A-8047-C32AC64D448A' \
  -only-testing:WriterPadTests/SyncV2ContractTests
```

현재 46개 통과, 실패 0입니다.

**xcodebuild가 성공했다고 곧이 믿지 마십시오.** 로그에서 파일이 실제로 컴파일됐는지
확인하십시오. 새 파일이 pbxproj에 없으면 아무 경고 없이 빌드가 성공하면서 그 파일만
빠집니다.

```bash
grep "Compiling.*SyncV2UnicodeCasefold" build.log
```

---

## 11. 사용자에 대해

- 개발자가 아니라 **작가**입니다. 자기 집필 앱을 AI와 함께 만들고 있습니다.
- 한국어로 소통합니다. 코드 주석과 커밋 메시지도 전부 한국어이고, **"무엇을"보다
  "왜"를 적는 문체**입니다. 기존 커밋을 몇 개 읽고 맞추십시오.
- **작업이 끝날 때마다 "사용자가 직접 해야 할 일"을 따로 간추려 달라고 요청했습니다.**
  결정해야 할 것 / 다른 세션에 전달할 것 / 하지 말아야 할 것으로 나눠서, 보고 끝에
  짧게 붙이십시오. 대신 할 수 있는 일은 그 목록에 넣지 말고 그냥 하십시오.
  결정 항목은 선택지를 늘어놓지 말고 권고안을 먼저 적으십시오.
- 두 AI 세션 사이를 오가며 조율하는 위치라, 자기 차례에 뭘 해야 하는지가 기술 요약에
  묻히면 막힙니다.

---

## 12. 한 줄로 다시

**iPad가 계약이 요구하는 충돌 키를 못 내고 있었고, 원인은 판 차이가 아니라 구현
차이였습니다. 접기는 고쳤고(다이제스트로 확인), NFKC 결함과 계약 자체의 구멍은
개정안으로 정리해 두었습니다. 실기기 스캔 하나만 끝나면 개정을 동결할 수 있습니다.
그때까지 동기화 배선은 시작하면 안 됩니다.**
