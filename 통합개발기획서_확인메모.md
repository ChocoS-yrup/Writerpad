# 통합개발기획서 확인 메모

- 작성일: 2026-07-15
- 대상 문서: `아팯_집필프로그램_통합개발기획서.md`
- 용도: 기획서 원본과 분리하여, 추후 확인하거나 반영할 결정 사항을 기록한다.

## 1. 확정: `플롯`과 `메인 스토리 틀`은 전용 항목으로 구현하지 않음

기획서를 합치는 과정에서 AI 집필도우미 프로그램의 항목과 일반 집필프로그램의 항목이 혼동된 것으로 확인했다.

- iPad에서는 일반 집필프로그램만 구현한다.
- 시스템이 제공하는 `플롯` 전용 항목은 구현하지 않는다.
- `메인 스토리 틀`도 플롯 계열의 전용 항목으로 보아 구현 대상에서 제외한다.
- 실제 원고는 `원고/N권/NNN화.txt` 구조로 저장한다.
- 플롯이나 작품 구성 자료가 필요한 사용자는 일반 문서 영역에서 직접 폴더와 TXT 문서를 만들어 관리한다.
- 기획서에서 `플롯`, `메인 스토리 틀`, `메인/플롯/`을 고정 항목으로 적은 부분은 추후 원본 정리 시 제거하거나 사용자 생성 폴더 설명으로 교체한다.

## 2. Windows v2 확정 후 재검토

### 2-1. Supabase 데이터 구조

- `projects` 구조와 프로젝트 소유권·순서·삭제 처리
- 폴더와 문서 계층을 나타내는 `parent_id`, 노드 종류, 사용자 지정 순서
- 경로 중복 방지 제약과 문서·폴더의 식별 방식
- 실제 Windows v2 스키마가 확정되면 iPad 구조와 다시 대조한다.

상태: Windows v2 전환 진행 중. 확정 자료를 받은 뒤 검토한다.

### 2-2. 메타데이터 작업의 원자적 처리

- 이름 변경
- 폴더 또는 문서 이동
- 삭제와 복원
- 새 권과 25개 화의 일괄 생성
- 각 작업의 `operation_id` 기반 중복 방지
- 일부만 서버에 반영된 경우의 재시도 및 롤백 방식

상태: Windows 환경에서 먼저 확인한다. Windows v2의 RPC 또는 동등한 처리 방식이 확정되면 iPad와 대조한다.

### 2-3. 인증과 RLS

- 로그인 방식
- Supabase Auth 세션 저장 및 갱신
- 로그아웃·세션 만료·네트워크 단절 처리
- RPC와 Realtime 채널의 사용자 소유권 확인
- 클라이언트에는 publishable/anon 키만 보관하는지 확인

상태: Windows v2에서 진행 중. 확정 후 iPad 구현 요구사항을 대조한다.

### 2-4. 3방향 병합용 Base 원고 보존

- Base를 로컬에 보존할지, 서버 `document_versions`에서 가져올지
- 동기화 큐에 Base 본문 또는 Base revision만 저장할지
- 장기 오프라인 편집 중 Base 버전이 삭제되지 않도록 하는 보존 정책
- 여러 로컬 작업이 누적되었을 때 어느 버전을 Base로 사용할지
- 충돌 해결이 끝날 때까지 Base·Local·Remote를 모두 보존하는 방식

상태: 별도 결정 필요. Windows v2의 버전 보관 방식이 확정되면 다시 검토한다.

### 2-5. iPad 백그라운드 전환 기준

- background 진입 전에 반드시 완료할 로컬 저장 범위
- 제한된 실행 시간 안에 서버 업로드가 끝나지 않을 경우 영구 큐로 넘기는 기준
- 저장 중 앱이 종료되어도 복구할 수 있는 임시 파일·큐 처리
- 다음 실행 시 미완료 저장과 동기화 작업을 재개하는 순서
- 상태 배지에서 로컬 저장 완료와 서버 대기를 구분하는 방식

상태: Windows v2 확정 후 공통 동기화 규약과 함께 다시 검토한다. iPadOS의 백그라운드 실행 제약은 iPad 구현 단계에서 별도로 반영한다.

## 3. 확정된 TXT 추출 규칙

전체 원고 추출 중 화 번호 순서대로 검사한다.

- 글자 수 계산에는 공백을 포함한다.
- 본문이 300자 미만인 화를 처음 발견하면 해당 지점에서 추출을 중단한다.
- 300자 미만인 해당 화와 그 이후 화는 추출 결과에 포함하지 않는다.
- 이 동작은 전체 추출의 기본 켜짐 옵션이며 사용자가 끌 수 있다.
- 범위 지정 추출에는 이 중단 옵션을 기본 적용하지 않는다.
- 완료 결과에는 중단된 화 번호와 실제 마지막 포함 화를 표시하는 것이 좋다.

## 4. Windows·iPad 공통 파일명 규칙 초안

다음 규칙은 Windows와 iPad 사이에서 같은 폴더를 안전하게 가져오고 내보내기 위한 기준이다.

상태: 금지 문자와 Windows 비호환 이름을 앱에서 차단하는 방향으로 확정했다. 정확한 길이 제한은 Windows v2 환경 확인 후 확정한다.

### 4-1. Windows에서 사용할 수 없는 문자

사용자 지정 프로젝트명, 폴더명, 문서명에 다음 문자를 허용하지 않는다.

`< > : " / \\ | ? *`

경로 구분자로 사용되는 `/`, `\\`도 이름에 포함할 수 없다. 탭, 줄바꿈, NUL을 포함한 제어 문자도 허용하지 않는다.

### 4-2. Windows 예약 이름

다음 이름은 확장자가 붙어도 사용할 수 없다.

- `CON`, `PRN`, `AUX`, `NUL`
- `COM1`부터 `COM9`
- `LPT1`부터 `LPT9`

예: `CON.txt`, `aux.TXT`, `LPT1.txt`도 금지한다. 대소문자는 구분하지 않는다.

### 4-3. 마침표와 공백

- 이름 끝의 마침표와 공백을 허용하지 않는다.
- 입력값 앞뒤 공백은 저장 전에 제거하거나 사용자에게 수정을 요청한다.
- `.`과 `..`만으로 된 이름은 허용하지 않는다.
- Windows에서 이름 끝의 마침표나 공백이 사라져 다른 파일과 충돌할 수 있으므로 자동 저장 전에 검사한다.

### 4-4. 대소문자와 Unicode 중복

- Windows는 일반적으로 파일명의 대소문자를 구분하지 않으므로 `Plot.txt`와 `plot.txt`를 같은 이름으로 취급한다.
- 한글과 조합 문자는 화면상 같아도 Unicode 구성 방식이 다를 수 있으므로 비교용 이름을 정규화한다.
- 비교 시 Unicode NFC 정규화와 대소문자 비구분을 적용한 키를 사용한다.
- 실제 문서 정체성은 파일명이 아니라 `document_id`로 유지한다.

### 4-5. 길이 제한

- 파일명 한 구성 요소가 지나치게 길지 않도록 앱 자체 제한을 둔다.
- 프로젝트 폴더부터 문서까지의 전체 상대 경로 길이도 검사한다.
- 구형 Windows 환경과 압축·백업 도구까지 고려하면 운영상 짧은 이름을 권장한다.
- 정확한 앱 제한 글자 수는 Windows v2가 사용하는 저장 위치와 경로 길이를 확인한 뒤 확정한다.

### 4-6. 중복 검사

새 이름을 저장하기 전에 같은 부모 폴더에서 다음을 모두 검사한다.

- 완전히 같은 이름
- 대소문자만 다른 이름
- Unicode 정규화 후 같아지는 이름
- Windows가 끝의 공백·마침표를 제거했을 때 같아지는 이름
- 예약 이름 또는 금지 문자가 포함된 이름

충돌 시 기존 파일을 덮어쓰지 않고 이름 수정을 요청하거나 안전한 새 이름을 제안한다.

### 4-7. 시스템이 생성하는 원고 이름

- 권 폴더는 `N권` 규칙으로 앱이 생성한다.
- 화 파일은 `001화.txt` 형식으로 앱이 생성한다.
- 사용자가 숫자와 `화` 접두사를 변경하지 못하게 하므로 일반 사용자 이름보다 충돌 위험이 낮다.
- 프로젝트명과 캐릭터·설정집·메모장 등에서 만드는 사용자 지정 파일·폴더에는 위 검사를 모두 적용한다.
