# iPadOS Swift 집필 프로그램 바이브코딩 단계별 프롬프트

기준 문서: `iPadOS_Swift_집필프로그램_통합개발기획서.txt` (2026-07-14)

## 사용 방법

- 아래 프롬프트를 0단계부터 순서대로 한 번에 하나씩 입력한다.
- 각 단계가 끝나면 AI가 보고한 테스트 결과와 미완료 항목을 확인한 뒤 다음 단계로 간다.
- 실패한 테스트가 있으면 다음 단계로 넘어가지 말고 같은 단계에서 원인을 해결한다.
- 가능하면 같은 대화와 같은 Git 작업 브랜치를 계속 사용한다.
- 실제 작가 원고가 아닌 테스트용 임시 프로젝트로 검증한다.
- Xcode가 필요한 단계는 macOS에서 수행한다. Windows에서는 소스 작성·검토는 가능해도 iPad 빌드와 실제 기기 검증을 완료 처리하지 않는다.
- 각 프롬프트는 구현뿐 아니라 검증과 결과 보고까지 포함한다. AI가 임의로 범위를 넓히면 안 된다.
- 각 단계가 통과하면 그 단계에서 바꾼 파일만 별도 Git 커밋으로 남긴다. 기존 사용자 변경이나 다른 단계 파일을 함께 stage하지 않는다.

---

### 지금부터 ###
0단계 프롬프트 내용

이제부터 `아팯_집필프로그램_통합개발기획서.txt`를 단일 제품 요구사항 기준으로 삼아 iPadOS 집필 앱을 단계적으로 개발한다. 먼저 저장소 전체와 Git 상태, 적용되는 AGENTS.md 등의 작업 지침, 기존 Windows 앱 구조, 테스트, 동기화 코드, 비밀정보 관리 방식을 읽기 전용으로 조사하라. 아직 제품 코드는 구현하지 마라.

조사 결과를 바탕으로 `Docs/` 아래에 다음 문서를 작성하라: 요구사항 추적표, 목표 아키텍처, 모듈 의존성, 단계별 완료 조건, 위험 목록, 결정 기록(ADR) 색인. 기획서의 모든 요구사항을 고유 ID로 나누고 어느 개발 단계와 테스트가 검증하는지 연결하라. 본문 원본은 UTF-8 TXT, 메타데이터는 SwiftData, 로컬 우선, 원자적 저장, UUID 기반 문서 정체성, revision 기반 동기화, IME 존중, 향후 Mac Catalyst 재사용이라는 원칙을 고정하라. AI 관련 코드와 화면은 iPad 앱 범위에서 제외하라.

기존 사용자 변경과 실제 원고를 수정하지 말고 비밀키를 출력하지 마라. 불명확한 사항은 합리적인 기본값과 근거를 ADR에 기록하되 제품 방향을 바꿀 정도의 선택만 질문하라. 마지막에 생성한 문서, 발견한 위험, 다음 단계 진입 조건, Git 변경 요약을 보고하라.
### 여기까지 ###

### 지금부터 ###
1단계 프롬프트 내용

0단계 문서와 기획서를 다시 확인하고 Xcode 기반을 구성하라. iPadOS 17 이상, SwiftUI 앱, SwiftData, UIKit/TextKit 편집기 연동을 전제로 `WriterPad` 앱과 테스트 타깃을 만든다. 기존 저장소에 Xcode 프로젝트가 있다면 새로 만들지 말고 현재 구성을 안전하게 보완하라. 지원 화면 방향, Split View와 Stage Manager, 고정 Bundle ID를 위한 설정 위치, 향후 Mac Catalyst를 켤 수 있는 빌드 설정을 준비하되 iPhone 전용 UI는 만들지 마라.

권장 소스 구조인 App, Domain/Models, Domain/Rules, Domain/Protocols, Data/Local, Data/Backup, Data/Trash, Data/Migration, Sync, Features, Platform, Resources, Tests를 실제 그룹과 폴더에 반영하라. AppEnvironment를 통해 의존성을 주입할 수 있는 최소 골격만 만들고 View가 파일 시스템이나 Supabase를 직접 호출하지 못하게 경계를 세워라. 사용자 데이터, 백업, `.env`, xcconfig 비밀값이 Git에 들어가지 않도록 `.gitignore`와 샘플 설정을 정리하라.

빈 앱을 시뮬레이터와 가능하면 실제 iPad에서 빌드하고 테스트 타깃의 기본 테스트를 실행하라. Mac이 아닌 환경이면 프로젝트 파일을 억지로 생성하지 말고 필요한 작업과 검증 불가 항목을 정확히 보고하라. 완료 시 변경 파일, 빌드·테스트 결과, 수동 Xcode 설정을 보고하라.
### 여기까지 ###

### 지금부터 ###
2단계 프롬프트 내용

도메인 모델과 저장 경계를 구현하라. Project, DocumentNode, BackupSnapshot, SyncOperation, SaveState 및 필요한 열거형과 값 객체를 만들고, 화면 프레임워크와 저장 구현에 의존하지 않는 순수 Swift 모델로 유지하라. `document_id`와 `project_id`는 UUID이며 이름 변경·이동·휴지통 이동 후에도 변하지 않아야 한다. 상대 경로는 정체성이 아니라 위치 정보임을 타입과 주석으로 명확히 하라.

문서 종류, 부모 ID, 상대 경로, 사용자 순서, 수정 시각, SHA-256, 서버 revision, metadata revision, 삭제 상태, 커서 위치, 펼침 상태, 좌우 편집기 상태, 동기화 작업 필드를 기획서와 추적표에 맞춰 모델링하라. 본문 문자열은 SwiftData 모델에 저장하지 않는 구조로 설계하라. 저장소, 파일 저장기, 백업, 검색, 추출, 동기화, 시계, UUID 생성기, 해시 계산기의 protocol을 정의해 테스트 대역을 넣을 수 있게 하라.

Codable 또는 SwiftData 변환이 필요한 값은 왕복 테스트를 작성하고, UUID 유지, 경로 변경, 상태 전이, 본문 비중복을 검증하라. 한 파일이 500줄을 넘지 않게 분리하고 공개 API를 문서화하라. 완료 시 모델 관계와 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
3단계 프롬프트 내용

SwiftData 메타데이터 계층을 구현하라. 기획서에서 지정한 프로젝트, 문서, 앱 화면 상태, 영구 동기화 작업 메타데이터만 저장하고 원고 본문은 절대로 SwiftData에 중복 저장하지 마라. 스키마 버전과 마이그레이션 계획을 처음부터 포함하고, UUID·상대 경로·부모 관계·사용자 순서·해시·revision·metadata revision·deleted 상태에 필요한 유일성 및 조회 전략을 설계하라.

ProjectRepository의 기본 CRUD, 마지막 프로젝트, 마지막 열린 좌우 문서, 활성 편집기, 바인더 너비, 펼쳐진 폴더, 문서별 커서 위치 저장 API를 actor 또는 명확한 격리 경계로 구현하라. 잘못된 부모 ID, 중복 document_id, 프로젝트를 넘는 부모 관계가 저장되지 않게 검증하라. 앱 최초 실행, 스키마 재오픈, 손상된 메타데이터 오류 처리, 인메모리 테스트 컨테이너를 지원하라.

테스트에서는 앱 재시작을 모사해 상태 복원, 문서 ID 보존, 순서 보존, 본문 미저장, 마이그레이션 기본값을 확인하라. 실패를 숨기지 말고 사용자 원고에 손대지 않는 복구 경로를 정의하라. 완료 시 스키마 표, 마이그레이션 버전, 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
4단계 프롬프트 내용

Application Support 아래의 로컬 작품 경로 계층을 구현하라. `Projects/프로젝트명/집필모드/메인/{원고,캐릭터,설정집,메모장,플롯 또는 메인 스토리 틀,흐름정리,복선,장소}`, `휴지통`, `백업/{자동저장,복원전,충돌}`, `설정.json` 구조를 기획서와 기존 Windows 폴더 명칭의 실제 호환성을 확인해 하나의 경로 정책으로 고정하라. 명칭 차이가 있다면 ADR과 호환 매핑을 작성하라.

PathPolicy/ProjectPathResolver를 만들어 모든 경로 생성을 한곳에서 처리하고, 프로젝트 이름과 파일 이름에서 경로 탈출, 절대 경로 주입, 예약 문자, 빈 이름, 지나치게 긴 이름, 대소문자·유니코드 정규화 충돌을 방지하라. `.txt` 확장자는 저장 시 유지하고 바인더 표시에서만 숨길 수 있게 하라. 외부 입력 경로가 프로젝트 루트 밖으로 나가지 않는지 표준화 후 재검증하라.

실제 원고가 아닌 임시 디렉터리에서 표준 구조 생성, 한글 이름, 잘못된 경로 거부, 동일 이름 충돌, 재실행 멱등성을 자동 테스트하라. 테스트가 만든 폴더는 정리하고 실제 Application Support는 테스트에서 사용하지 마라. 결과와 결정한 Windows 호환 매핑을 보고하라.
### 여기까지 ###

### 지금부터 ###
5단계 프롬프트 내용

`LocalDocumentStore` actor를 구현해 UTF-8 TXT 읽기·쓰기와 원자적 저장을 완성하라. 저장은 동일 디렉터리의 고유 임시 파일에 전체 내용을 쓴 뒤 flush 가능한 범위까지 완료하고 안전하게 교체하며, 어느 단계에서 실패해도 기존 정상 원고가 유지되어야 한다. UTF-8만 허용하고 디코딩 실패, 접근 거부, 저장 공간 부족, 중간 교체 실패를 의미 있는 오류로 반환하라.

동일 문서의 저장 요청을 직렬화하고 각 요청에 save generation을 부여해 오래된 비동기 완료 결과가 최신 입력 상태나 해시를 덮어쓰지 못하게 하라. 저장 성공 후 SHA-256과 수정 시각을 계산하고 메타데이터 갱신이 실패했을 때 파일과 색인을 다시 일치시키는 복구 전략을 구현하라. 임시 파일 잔재는 원본과 구분해 다음 실행에서 안전하게 정리 또는 복구 판단하라.

정상 왕복, 빈 문서, 한글·이모지, 큰 문서, 동시 저장 순서, 오래된 generation, 각 실패 지점 주입, 앱 재시작 후 임시 파일 처리를 자동 테스트하라. 실제 작품 파일을 테스트에 사용하지 마라. 공개 API, 원자성 보장 범위, 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
6단계 프롬프트 내용

작품 관리 기능의 도메인·데이터 계층을 구현하라. 새 작품 생성, 목록 조회, 마지막 작품 자동 열기, 이름 변경, 사용자 지정 순서 변경, 삭제 전 확인에 필요한 서비스 API, 작품 전체 내보내기의 기반을 만든다. UI는 최소 연결만 하고 먼저 트랜잭션 안전성을 완성하라.

작품 생성은 임시 디렉터리에 전체 기본 구조와 메타데이터를 만든 뒤 모두 성공했을 때 정식 경로로 승격하라. 동일 이름·정규화 충돌을 덮어쓰지 마라. 이름 변경 중 파일 이동과 SwiftData 갱신 중 하나가 실패하면 이전 상태로 복구하거나 재시작 시 완료할 수 있는 저널을 남겨라. 삭제는 이 단계에서 즉시 제거하지 말고 후속 휴지통 정책과 연결 가능한 명시적 상태 전이를 사용하라. 마지막 프로젝트가 없어졌거나 접근 불가할 때 안전한 기본 화면으로 돌아가라.

프로젝트 생성·재실행·이름 변경·순서 변경·실패 롤백·마지막 작품 복원을 임시 폴더와 인메모리 SwiftData로 테스트하라. 동일 작업을 두 번 요청해도 중복 프로젝트가 생기지 않는지 확인하라. 완료 시 UI에서 사용할 API, 실패 복구 방식, 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
7단계 프롬프트 내용

기존 Windows 작품 폴더 가져오기를 검사 단계와 실행 단계로 분리해 구현하라. 원본 폴더는 절대로 수정하지 않는다. 먼저 전체 트리를 읽어 UTF-8 여부, 예상 디렉터리 구조, 중복 프로젝트, 중복 화수, 잘못된 `N권`·`NNN화.txt` 이름, 숨김·알 수 없는 파일, 접근 불가 파일, 이름 정규화 충돌을 수집한 ImportReport를 사용자에게 보여줄 수 있게 하라.

치명 오류와 경고를 구분하고 사용자가 확인한 뒤에만 가져오기를 시작한다. 가져오기는 앱 내부 임시 프로젝트로 복사하고 각 문서에 안정적인 UUID와 해시를 부여한 뒤 전부 성공할 때만 정식 프로젝트와 SwiftData 메타데이터를 등록하라. 중간 실패 시 복사본과 메타데이터를 전부 롤백하되 원본에는 손대지 마라. 예상 카테고리 명칭 차이는 4단계 호환 매핑을 적용하고 보고서에 남겨라.

정상 작품, 비 UTF-8, 중복 화, 잘못된 권, 읽기 실패, 대용량 트리, 중간 복사 실패를 fixture로 자동 테스트하라. 파일 선택 UI는 얇게 연결하고 security-scoped resource 수명도 안전하게 관리하라. 결과 예시와 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
8단계 프롬프트 내용

바인더 데이터 제공 계층과 9개 최상위 항목을 구현하라. 순서는 원고, 캐릭터, 설정집, 메모장, 메인 스토리 틀, 흐름 정리, 복선, 장소, 휴지통이며 원고는 항상 첫 번째다. 실제 디스크 구조와 표시 이름이 다르면 4단계 매핑을 사용한다. 파일 시스템 탐색 결과와 SwiftData의 UUID·부모·사용자 순서를 일관되게 결합하라.

최상위 항목은 즉시 표시하고 하위 폴더는 펼칠 때 비동기로 지연 로딩하라. `.txt` 확장자는 UI에서만 숨기고 빈 문서와 작성된 문서를 해시 또는 파일 크기 기준으로 구분할 수 있게 하라. 펼침 상태와 사용자 순서를 저장·복원하고, 파일 시스템이 외부에서 바뀌었을 때 전체 UUID를 무조건 새로 만들지 않는 재조정 규칙을 정의하라. View가 직접 디렉터리를 열거하지 못하게 BinderRepository/ViewModel 경계를 둬라.

1,000화와 보조 문서 fixture에서 초기에는 최상위만 읽는지, 자연 정렬, 사용자 정렬, 펼침 복원, 확장자 숨김, 외부 변경 재조정을 테스트하라. 메인 스레드 파일 I/O가 없는지 확인하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
9단계 프롬프트 내용

순수 Swift `BinderRuleService`를 구현하라. 원고 바로 아래에는 `N권` 폴더만, 권 안에는 세 자리 숫자 `NNN화.txt`만 허용한다. 권 이름은 변경할 수 없고 화 이름의 숫자+`화` 접두사는 변경할 수 없으며 같은 화 번호를 중복 생성할 수 없다. 원고 문서를 다른 최상위 카테고리로 이동하거나 외부 문서를 원고로 이동할 수 없고 원고 최상위 순서는 변경할 수 없다. 원고 내부 기본 정렬은 숫자 자연 정렬이다.

규칙 판정은 UI나 파일 I/O와 분리하고 생성·이름 변경·이동·드롭 각각에 대해 허용/거부 결과와 사용자용 이유를 반환하라. 숫자 앞자리 0, `1권`과 `01권`, `001화`, 1,000화 이상, 유니코드 유사 문자, 확장자 대소문자, 중복 정규화 같은 경계를 명시적으로 처리하라. 일반 카테고리의 자유 이름과 사용자 순서는 불필요하게 제한하지 마라.

허용·거부 행렬을 표 기반 테스트로 작성하고 기획서의 모든 원고 규칙이 최소 하나의 테스트에 매핑되게 하라. View에서 규칙을 중복 구현한 코드가 있다면 서비스 호출로 통합하라. 완료 시 규칙표와 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
10단계 프롬프트 내용

바인더의 일반 문서·폴더 작업을 구현하라. 생성, 이름 변경, 사용자 순서, 드래그 앤 드롭 이동, 휴지통 이동, 우클릭·길게 누르기용 명령 모델을 제공한다. 모든 변경은 먼저 9단계 `BinderRuleService`로 검사하고, 파일 시스템과 SwiftData가 어긋나지 않도록 트랜잭션 또는 복구 저널을 사용하라.

이름 충돌은 덮어쓰지 말고 안전한 대안을 제시한다. 이동과 이름 변경 후에도 `document_id`와 하위 문서 ID가 유지되어야 하며 상대 경로와 부모 ID만 갱신한다. 실패 시 사용자에게 원인을 보존 가능한 상태로 알리고 바인더를 실제 디스크 상태와 재조정하라. 드롭 대상 미확정, 자기 하위로 폴더 이동, 프로젝트 루트 밖 이동, 열려 있는 문서 이동을 처리하라. 컨텍스트 메뉴 항목은 기능 가능 여부에 따라 활성화 상태와 거부 이유를 표시할 수 있게 하라.

정상 CRUD, ID 보존, 중간 실패, 중복 이름, 잘못된 드롭, 대규모 하위 트리 이동을 자동 테스트하라. 실제 원고를 사용하지 말고 변경 중 UI가 멈추지 않는지 확인하라. 완료 시 명령별 원자성·복구 방식과 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
11단계 프롬프트 내용

원고 최상위 컨텍스트 메뉴의 `새 권 추가`를 트랜잭션 방식으로 구현하라. 현재 존재하는 유효한 `N권` 중 가장 높은 번호+1을 새 권으로 하고, 시작 화 `(권-1)*25+1`, 종료 화 `권*25`, 파일명은 세 자리 숫자+`화.txt`로 계산한다. 권이 없으면 1권과 001~025화를 만든다.

고유 임시 폴더에 빈 UTF-8 TXT 25개와 각 문서 UUID·메타데이터를 준비하고 전부 성공한 경우에만 정식 `N권`으로 승격하라. 기존 권이나 화를 절대로 덮어쓰지 않고 일부 화만 남기지 마라. 생성 중 명령을 재진입하지 못하게 하고 실패 시 임시 파일과 미완료 메타데이터를 정리한다. 성공하면 바인더 새로고침, 새 권 펼침, 첫 화 열기 의도를 결과로 반환하고, 아직 서버가 없어도 후속 동기화 큐에 등록 가능한 일괄 작업 도메인 모델을 만든다.

0권 상태, 1권·2권 상태, 번호 공백, 잘못된 권 폴더 혼재, 기존 화 충돌, 25개 각 지점 실패, 동시 두 번 요청을 테스트하라. 1,000화 이후 파일명 정책도 명시하라. 결과와 테스트를 보고하라.
### 여기까지 ###

### 지금부터 ###
12단계 프롬프트 내용

iPad 화면의 기본 셸을 SwiftUI로 구현하라. `NavigationSplitView`를 사용해 가로에서는 바인더와 편집기를 함께, 세로에서는 편집기를 최대한 넓게 표시하고 바인더를 버튼으로 열고 닫게 하라. 상단에는 작품 전환, 검색, 좌우 분할, 작은 저장·동기화 상태 배지 영역을 두되 큰 하단 상태바는 만들지 마라.

8~11단계 Binder ViewModel을 연결해 펼치기, 선택, 컨텍스트 메뉴, 길게 누르기, 가능한 환경의 우클릭, 드래그 앤 드롭을 제공한다. sheet는 추출·백업·충돌 화면을 나중에 연결할 자리만 타입 안전하게 마련하고 가짜 기능을 완성처럼 보이게 하지 마라. compact/regular width, 가로·세로, Split View, Stage Manager에서 레이아웃이 무너지지 않게 하고 화면 폭이 부족한 듀얼 편집기는 후속 정책을 표시할 수 있게 하라.

접근성 레이블, 키보드 포커스 이동, Dynamic Type의 합리적 범위를 적용하라. Preview와 UI 테스트에서 9개 최상위 순서, 바인더 열기·닫기, 문서 선택, 메뉴 노출, 상태 배지 위치를 확인하라. 실제 iPad에서 검증 가능한 체크리스트와 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
13단계 프롬프트 내용

SwiftUI `TextEditor`에 의존하지 않고 `UIViewRepresentable`로 `UITextView`를 감싼 `iPadTextEditor`와 `UITextView` 하위 클래스 `SmartTextView`의 최소 기반을 구현하라. TextKit을 통해 UTF-8 일반 텍스트, 커서·선택 범위, 스크롤, 현재 문서 검색 강조 확장 지점, 실행 취소·다시 실행, 복사·잘라내기·붙여넣기를 지원하라.

SwiftUI 상태와 UITextView 본문을 양방향 연결하되 매 update마다 전체 text를 재설정하지 마라. 외부 모델 버전과 실제 편집 변경을 구분하고, 문서 전환 시 기존 view 인스턴스를 불필요하게 폐기하지 않으며 first responder와 선택 범위를 안정적으로 관리하라. 문서 없음·빈 문서 안내는 placeholder 계층으로 구현해 원문에 섞이지 않게 한다. 파일 저장과 스마트 입력 규칙은 editor 클래스에서 호출할 수 있는 경계만 두고 아직 한글 변환 규칙을 넣지 마라.

UIKit 단위 테스트가 가능한 coordinator 로직을 분리하고, 한글·이모지 텍스트 왕복, 선택 유지, 동일 본문 재주입 방지, 문서 전환 포커스, Undo 기본 동작을 검증하라. 실제 iPad 수동 테스트 항목도 작성하고 결과를 보고하라.
### 여기까지 ###


### 지금부터 ###
14단계 프롬프트 내용

13단계 편집기에 한글 조합 입력 안전장치를 구현하라. `markedTextRange`가 존재하는 동안은 조합 중으로 판단하고 SwiftUI에서 UITextView 전체 본문을 다시 설정하거나 문서를 즉시 교체하지 마라. 문서 전환 요청은 조합 완료 후 실행되는 명시적 pending transition으로 관리하고, 화 전환마다 SwiftUI `id`를 바꾸거나 UITextView 인스턴스를 새로 만들지 마라.

iPad 키보드 언어를 강제로 바꾸거나 Windows식 IME 강제 전환·키 재전송을 사용하지 않는다. 조합 중에도 로컬 저장용 텍스트 스냅샷은 얻을 수 있지만 저장 완료가 화면 본문을 재주입해서는 안 된다. 소프트웨어 키보드, Magic Keyboard, 일반 Bluetooth 키보드, 빠른 문서 전환, background/foreground에서 첫 글자가 영문으로 바뀌거나 사라지지 않게 포커스 전이 상태 머신을 구현하라.

가능한 coordinator 단위 테스트와 UI 테스트를 작성하고, 실제 iPad에서 `한`, 겹받침, 연속 입력, 조합 중 전환, 빠른 이전·다음 화, 앱 복귀 직후 첫 글자를 반복 검증하라. 시뮬레이터만 통과했다면 완료로 과장하지 말고 실제 기기 미검증을 표시하라. 상태 전이 도식과 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
15단계 프롬프트 내용

스마트 텍스트 입력을 UIKit과 분리된 순수 Swift `TextRuleEngine`으로 구현하라. 입력 문자열, 선택 범위, 주변 문맥, 설정, 조합 상태를 받아 새 문자열 또는 최소 편집 연산, 새 선택 범위, 처리 여부를 반환하게 한다. UTF-16 기반 NSRange와 Swift String 인덱스 변환에서 한글·이모지 경계를 안전하게 처리하라.

입력 전 규칙으로 `'`, `"`, `[`, `(`, `{` 자동 쌍과 가운데 커서, 선택 영역 감싸기, 동일 닫는 기호 건너뛰기(`'`, `"`, `]`, `)`, `}`, `」`, `』`), 빈 쌍 Backspace 삭제(`''`, `""`, `[]`, `()`, `{}`, `「」`, `『』`), 닫는 기호 앞 Enter 시 기호를 넘은 뒤 줄바꿈을 구현하라. 각 규칙은 설정으로 끌 수 있고 서로 중복 적용되지 않아야 한다.

빈 선택, 다중 유니코드, 문서 시작·끝, 줄바꿈, 잘못된 NSRange, 조합 중 입력, Undo 단위에 필요한 결과 메타데이터를 표 기반 테스트로 철저히 검증하라. SmartTextView에는 아직 최소 어댑터만 연결하고 규칙 판정 코드를 복제하지 마라. 규칙 우선순위와 테스트 표를 보고하라.
### 여기까지 ###

### 지금부터 ###
16단계 프롬프트 내용

`TextRuleEngine`에 조합 완료 후 규칙과 장면 전환선을 추가하라. `...`는 `…` 한 글자로, `ㄴㄴ`은 `「」`, `ㄱㄱ`은 `『』`로 바꾸고 커서를 인용부호 가운데 둔다. 이 세 규칙은 `markedTextRange`가 완전히 사라진 뒤에만 실행하며 기존 더 긴 텍스트를 잘못 재변환하지 않도록 최근 확정 입력 범위를 기준으로 판단하라.

별표 한 번 입력 시 정확히 `\n\n * * *\n\n`을 삽입하고, 전환선 바로 뒤 Backspace 또는 바로 앞 Delete는 전체 문자열을 삭제한다. 삽입과 삭제, 말줄임표·특수 인용부호 변환은 각각 한 번의 Undo로 되돌아가야 한다. 자동 쌍 규칙과 별표 규칙이 선택 영역, 목록 문맥, 이미 존재하는 별표에 오작동하지 않도록 명시된 단일 `*` 입력 사건만 처리하라. 네 설정 그룹은 기본 켜짐이며 개별 비활성화를 존중한다.

IME 조합 이벤트 시퀀스, 붙여넣기, 빠른 입력, 전환선 양쪽 삭제, Undo/Redo, 설정 off를 자동 테스트하고 SmartTextView에 연결하라. 실제 iPad 한글 키보드 수동 검증 결과도 보고하라.
### 여기까지 ###

### 지금부터 ###
17단계 프롬프트 내용

편집기 사용성을 완성하라. 실시간 글자 수, 문서별 커서·선택·스크롤 위치 기억, 문서 전환 후 안전한 자동 포커스, 타자기 스크롤, 글꼴·글자 크기·줄 간격·좌우 및 상하 여백을 구현한다. 원고는 TXT이므로 굵게·색상·밑줄 같은 부분 서식을 원고에 저장하지 말고 모든 시각 설정은 앱 설정으로만 보존하라.

TextKit 레이아웃과 textContainerInset/paragraphStyle 적용 시 본문 문자열이 바뀌거나 Undo 스택이 오염되지 않게 한다. 글자 수의 기준(공백 포함 또는 표시 옵션)을 명시하고 큰 문서에서도 매 키 입력마다 전체 고비용 재계산을 하지 않도록 측정하라. 타자기 스크롤은 사용자가 선택 범위를 이동하거나 검색 결과로 이동할 때도 화면을 예측 가능하게 유지하며 접근성 Reduce Motion을 존중한다.

설정 기본값·영속화·재실행 복원, 한글·이모지 글자 수, 커서 복원 범위 보정, 6,000자 이상 입력 성능을 테스트하라. 설정 변경 전후 TXT 바이트가 동일함을 확인하고 실제 iPad 체크리스트와 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
18단계 프롬프트 내용

좌우 듀얼 에디터를 구현하라. 기본은 단일 편집기이고 상단 버튼으로 두 문서를 나란히 연다. 왼쪽 문서, 오른쪽 문서, 활성 편집기, 분할 여부를 저장·복원하며 활성 쪽에만 과하지 않은 강조 테두리를 표시한다. 바인더에서 문서를 선택하면 활성 편집기에 열리게 하라.

동일한 `document_id`를 양쪽에서 동시에 편집하지 못하게 하고 이미 반대쪽에 열린 문서를 선택하면 그쪽을 활성화하거나 명확한 안내를 보여라. 폭이 부족한 compact 환경에서는 단일 모드 전환을 제안하고 데이터 손실 없이 한쪽을 닫는다. 분할을 닫기 전에 해당 편집기의 최신 스냅샷을 즉시 저장하도록 후속 저장 coordinator 인터페이스를 호출하며, 저장 실패 시 닫힘을 강행하지 않고 복구 선택을 제공할 수 있게 하라. 각 편집기는 독립 커서·Undo·스크롤 상태를 유지한다.

동일 문서 방지, 활성 전환, 상태 복원, 폭 변경, 분할 닫기 저장 실패, 빠른 좌우 입력을 테스트하라. 실제 iPad 가로·세로·Split View 결과와 미검증 항목을 보고하라.
### 여기까지 ###

### 지금부터 ###
19단계 프롬프트 내용

자동 저장 coordinator를 구현하라. 마지막 입력 후 기본 800ms 디바운스, 다른 화 이동 전, 프로젝트 변경 전, 분할 닫기 전, 앱 inactive/background 전, Command+S에 즉시 저장한다. 저장 순서는 텍스트 스냅샷 → `LocalDocumentStore` 원자 저장 → 해시·로컬 메타데이터 갱신 → 필요 시 백업 요청 → 영구 동기화 작업 등록 → 서버 업로드 시도이며, 서버가 아직 없어도 앞 단계가 완전히 동작해야 한다.

각 문서의 save generation과 dirty generation을 비교해 느린 이전 저장이 최신 상태를 저장됨으로 표시하거나 덮어쓰지 못하게 하라. 두 편집기의 저장은 문서별로 독립적이되 같은 문서 저장은 actor에서 직렬화한다. 문서 전환은 최신 로컬 저장 성공 전 새 문서를 편집 가능 상태로 바꾸지 않는 안전한 순서를 사용하고, 실패 시 현재 텍스트를 메모리에 보존하면서 재시도와 별도 파일 구조를 제공할 수 있게 하라.

가상 시계로 800ms, 연속 타이핑, 즉시 저장, 느린 저장 역전, 전환 중 입력, 두 편집기, background 제한 시간을 테스트하라. 서버 없음이 로컬 저장 실패로 보이지 않게 하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
20단계 프롬프트 내용

저장·동기화 상태 모델과 상단 배지를 구현하라. 상태는 편집 중, 로컬 저장 중, 로컬 저장됨, 서버 동기화 중, 서버 저장됨, 오프라인 저장됨, 동기화 대기 N건, 다른 기기 편집 중, 자동 병합 완료, 충돌 확인 필요, 동기화 실패·재시도를 구분한다. `저장됨` 하나로 로컬과 서버를 혼동시키지 마라.

복합 상태의 우선순위와 표시 문구를 하나의 reducer/state machine에서 결정하고 View에 조건문을 흩뜨리지 마라. 예를 들어 `로컬 저장됨 · 서버 동기화 중`, `오프라인 · 서버 대기 3건`, `로컬·서버 저장 완료`를 정확히 표현한다. 배지는 작지만 색상만으로 상태를 전달하지 않고 VoiceOver 레이블과 아이콘·문구를 함께 제공한다. 누르면 세부 상태, 마지막 오류, 마지막 서버 시각, 대기 건수, 수동 재시도 자리를 보여준다.

입력→로컬 저장→큐 등록→서버 성공, 오프라인, 실패, 충돌, 다시 입력되는 모든 전이를 테스트하라. 오래된 비동기 이벤트가 최신 배지를 되돌리지 않는지 검증하고 UI 스냅샷 또는 접근성 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
21단계 프롬프트 내용

자동 백업 저장소와 보존 정책을 구현하라. 일정 시간 이상 편집, 다른 화 이동, 문서 닫기, 서버 충돌, 과거 버전 복원 전, 구조 변경·대량 동기화 전에 백업을 요청할 수 있게 한다. 백업 원문은 UTF-8 TXT이며 `백업/자동저장`, `백업/복원전`, `백업/충돌`을 분리하고 BackupSnapshot 메타데이터에 문서 ID, 원래 경로, 시각, 해시, 사유, 보관 지정 여부를 기록한다.

같은 해시의 불필요한 연속 백업을 줄이되 중요한 사건 백업은 생략하지 않는다. 권장 정책인 최근 24시간 최대 30개, 최근 30일 하루 1개, 보관 지정 영구 유지가 결정론적으로 작동하게 구현한다. 삭제 후보를 고른 뒤 실제 삭제 실패가 원고 저장을 실패로 만들지 않되 상태와 오류는 남겨라. 앱 재시작과 기기 시각 변경에도 가능한 한 안전하도록 주입 가능한 시계와 정렬 규칙을 사용한다.

경계 시각, 30개 초과, 하루 대표본, 보관 지정, 중복 해시, 삭제 실패, 5,000개 백업 정리 성능을 임시 폴더에서 테스트하라. 정책 예시와 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
22단계 프롬프트 내용

백업 이력, 미리보기, 차이 비교, 복원을 구현하라. 문서별 백업 목록에 날짜·시각·사유·보관 상태를 표시하고 선택 시 현재본을 바꾸지 않는 읽기 전용 미리보기를 제공한다. 큰 텍스트에서도 UI를 막지 않는 줄 또는 문단 기반 diff를 비동기로 계산하고 추가·삭제·변경을 접근성 있게 표현하라.

복원 순서는 현재 문서 즉시 로컬 저장 → 현재본을 `백업/복원전`에 강제 백업 → 선택 백업의 UTF-8·document_id·경로·해시 검증 → 원자적 원고 교체 → 새 해시와 revision 파생 상태 갱신 → 동기화 큐 등록이다. 어느 단계든 실패하면 기존 현재본을 보존하고 부분 복원 상태를 남기지 마라. 복원은 새 로컬 변경으로 취급하고 과거 서버 revision을 그대로 최신이라고 표시하지 않는다. 백업 삭제와 보관 지정도 안전하게 구현한다.

동일본, 큰 차이, 손상 백업, 잘못된 문서 백업, 복원 전 백업 실패, 교체 실패, 성공 후 재시작을 테스트하라. UI 흐름과 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
23단계 프롬프트 내용

휴지통을 구현하라. 일반 삭제와 프로젝트 내 문서 삭제는 즉시 영구 제거하지 않고 `휴지통`으로 원자적으로 이동하며 원래 상대 경로, 부모 ID, 사용자 순서, 삭제 시각을 기록한다. 열린 문서는 저장 후 닫고, 이름 변경·이동 뒤에도 document_id는 유지한다. 원고 규칙과 열린 좌우 편집기 상태를 함께 처리하라.

원래 위치 복원과 사용자가 고른 위치 복원을 제공한다. 대상 이름이 충돌하면 덮어쓰지 않고 안전한 새 이름을 제안하며 원고 영역 복원은 BinderRuleService 규칙을 반드시 통과해야 한다. 개별 영구 삭제와 휴지통 비우기는 명시적 확인 후 수행하고 실패한 항목을 보고하며 성공한 항목과 구분한다. 후속 서버 tombstone을 위해 deleted 상태와 동기화 작업을 생성할 수 있게 한다. 다른 기기 수정과 삭제 충돌 UI는 후속 단계 경계로 남긴다.

원래 위치 복원, 부모 삭제 상태, 이름 충돌, 원고 규칙 위반, 열린 문서, 영구 삭제 부분 실패, 앱 재시작을 테스트하라. 실제 원고나 백업을 건드리지 말고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
24단계 프롬프트 내용

현재 문서 검색을 TextKit 기반으로 구현하라. 검색어 변경 시 결과를 즉시 갱신하고 결과 수, 현재 결과 번호, 다음·이전 이동, 현재 결과의 구별되는 강조를 제공한다. 검색 UI는 Command+F로 열리고 Escape 또는 닫기 동작 후 편집 포커스를 안전하게 복원한다.

Swift String과 TextKit의 UTF-16 범위를 정확히 변환해 한글, 조합 문자, 이모지에서도 잘못된 선택이나 크래시가 없어야 한다. 빈 검색어, 대소문자 정책, 겹치는 결과 정책, 문서 변경, 편집으로 결과 위치가 바뀌는 상황을 명시한다. 강조는 원본 TXT의 속성으로 저장하지 않고 임시 layout 속성으로만 적용하며 기존 선택·타자기 스크롤과 충돌하지 않게 한다. 결과 이동은 해당 범위를 보이게 스크롤하되 사용자의 텍스트를 바꾸지 않는다.

문서 시작·끝, 여러 줄, 한글·이모지, 0건, 수천 건, 검색 중 편집, 좌우 편집기 독립 검색을 테스트하라. 6,000자와 더 큰 문서의 반응 시간을 측정하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
25단계 프롬프트 내용

프로젝트 전체 검색을 구현하라. 모든 TXT를 비동기로 읽고 파일별 결과를 그룹화하며 각 일치 주변 문장을 안전하게 생성한다. 결과를 선택하면 해당 document_id를 올바른 편집기에 열고 일치 범위로 이동한다. 검색 진행률과 취소를 제공하고 검색 중에도 편집과 자동 저장이 끊기지 않아야 한다.

초기 버전은 파일 직접 검색을 사용하고 1,000화 실측에서 병목이 확인되지 않으면 검색 색인을 추가하지 마라. 디스크 변경 중인 파일은 일관된 스냅샷 또는 재시도 정책을 사용하고 읽기 실패 파일을 전체 검색 실패로 숨기지 말고 부분 결과와 오류 목록으로 보고한다. 휴지통·백업 포함 여부는 기본 제외로 명시하고 사용자가 기대하는 보조 카테고리는 포함한다. UTF-16 위치와 문서 전환 후 TextKit 범위를 재검증한다.

1,000화×약 6,000자와 보조 문서 500개 fixture, 취소, 파일 삭제 경쟁, 비 UTF-8 오류, 결과 이동, 메인 스레드 비차단을 테스트·측정하라. 색인을 넣지 않았거나 넣은 근거를 수치로 보고하라.
### 여기까지 ###

### 지금부터 ###
26단계 프롬프트 내용

원고 최상위 컨텍스트 메뉴의 `TXT 추출`을 구현하라. 설정 시트에서 전체 원고 또는 시작 화·종료 화를 선택하고, 빈 화 제외, 화 제목 포함, 공백 제외 300자 미만 화부터 제외, 저장 위치 옵션을 제공한다. 300자 미만 중단 옵션은 전체 추출에서 기본 켜짐이지만 사용자가 끌 수 있어야 한다.

권 폴더와 무관하게 파일명에서 화 번호를 추출해 숫자 순으로 정렬한다. 결과 형식은 화 제목 옵션 사용 시 `[001화]`, 빈 줄, 본문으로 구성하고 UTF-8 TXT를 만든다. 파일명은 `작품명(1~250화).txt` 또는 `작품명(50~100화).txt` 형식을 사용하되 실제 포함 범위가 없을 때 안전하게 처리한다. 완료 보고에는 지정 범위, 실제 포함 화수, 존재하지 않는 화, 빈 문서 제외, 300자 규칙 제외·중단, 저장 위치를 포함한다.

FileDocument 또는 Transferable, fileExporter, 공유 시트를 연결하고 PDF는 만들지 마라. 번호 공백, 다른 권에 잘못 배치된 화, 중복, 빈 문서, 한글·이모지, 취소, 쓰기 실패를 테스트하고 출력 바이트와 순서를 검증하라.
### 여기까지 ###

### 지금부터 ###
27단계 프롬프트 내용

설정 화면과 외장 키보드 명령, 화면 상태 복원을 통합하라. 편집 설정은 글꼴, 크기, 줄 간격, 좌우·상하 여백, 타자기 스크롤, 자동 저장 대기시간, 스마트 기호 세부 옵션을 제공한다. 백업 설정은 사용 여부, 최대 보관 수, 기간을 제공하고, 동기화 설정에는 아직 연결 전임을 명확히 표시하면서 기기 이름·대기 작업·재시도 API 자리를 둔다.

Command+S 즉시 저장, Command+F 현재 검색, Command+Shift+F 전체 검색, Command+Z/Shift+Z, Command+B 바인더 토글, Command+\\ 듀얼 전환, Command+Option+Left/Right 이전·다음 화를 구현한다. 메뉴 제목과 discoverability를 제공하고 텍스트 입력 규칙과 단축키가 충돌하지 않게 하라. 마지막 작품, 좌우 문서, 활성 편집기, 분할 여부, 바인더 너비, 펼친 폴더, 커서 위치를 앱 재실행 후 복원한다. 사라진 문서는 안전한 대체를 선택한다.

모든 단축키, 설정 저장, 손상된 값 기본화, 상태 복원, 없는 문서, iPad와 Catalyst 명령 재사용을 테스트하라. 실제 키보드 검증표와 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
28단계 프롬프트 내용

여기서 로컬 전용 1차 마일스톤을 감사하라. 새 기능을 넓히기 전에 0~27단계 요구사항 추적표의 상태를 실제 코드와 테스트 증거로 다시 확인하고, 미완료·부분 구현·가짜 placeholder·무시된 오류·메인 스레드 파일 I/O·500줄 초과 파일·View의 직접 저장 접근을 찾아 수정하라. 기획서와 구현이 충돌하면 임의로 요구사항을 삭제하지 말고 ADR과 추적표를 갱신하라.

네트워크와 Supabase가 전혀 없어도 작품 생성, Windows 작품 가져오기, 바인더, 새 권 25화, 한글 안전 편집, 스마트 입력, 듀얼 편집, 자동 저장, 백업·복원, 휴지통, 검색, TXT 추출이 끝까지 동작하는 통합 테스트 시나리오를 실행하라. 앱 강제 종료 직전 입력, 저장 실패, 손상 백업, 빠른 화 전환에서도 원문이 보존되는지 확인한다. 실제 사용자 원고가 아닌 복제 fixture만 사용한다.

작품 전체 내보내기도 이 마일스톤에서 완성하라. 프로젝트의 UTF-8 TXT와 호환 가능한 폴더 구조, UUID·경로 복원에 필요한 안전한 manifest를 사용자가 선택한 위치에 원자적으로 내보내고, 내보낸 사본을 새 임시 환경에 다시 가져와 원고 수와 해시가 일치하는지 검증한다. 작품 삭제 확인과 마지막 작품 대체 동작도 끝까지 연결하되 내보내기 실패나 취소 시 원본은 그대로 보존하라.

모든 테스트 결과와 실제 iPad 검증 여부를 표로 보고하고, 미통과 항목이 있으면 이번 단계에서 해결하라. 해결 불가능한 외부 조건은 정확한 재현 절차와 함께 차단 항목으로 남겨라. 테스트 통과 상태에서만 로컬 마일스톤 완료로 표시하라.
### 여기까지 ###

### 지금부터 ###
29단계 프롬프트 내용

Supabase 연결 전 로컬 성능 기준선을 만들고 측정하라. 테스트 데이터 생성기는 권 40개까지, 원고 1,000화×약 6,000자, 캐릭터·설정·메모 500개, 백업 5,000개를 실제 원고와 분리된 임시 영역에 결정론적으로 만든다. 릴리스 빌드와 실제 iPad에서 프로젝트 진입, 최상위 바인더 표시, 권 펼침, 화 전환, 연속 타이핑, 원자 저장, 전체 검색, 백업 목록의 시간·메모리·메인 스레드 정지를 계측하라.

목표는 프로젝트 진입 2초 이내, 화 전환 체감 지연 없음, 저장·검색 중 타이핑 유지, 바인더 즉시 반응, 메모리 부족 종료 방지다. 먼저 측정하고 병목이 확인된 경우에만 하위 폴더 지연 로딩을 보완하고, 필요한 부모 폴더 캐시만 추가·무효화하라. 검색 색인과 복잡한 전역 캐시는 수치 근거 없이 도입하지 마라.

측정 환경, 데이터 크기, median/p95, 메모리 최고치, Instruments 근거, 변경 전후 수치를 `Docs/PerformanceBaseline.md`에 기록하라. 시뮬레이터 수치를 실제 iPad 수치처럼 보고하지 말고 남은 병목을 명시하라.
### 여기까지 ###

### 지금부터 ###
30단계 프롬프트 내용

iPad 동기화의 연결·인증·기기 정체성 기반을 구현하라. `supabase-swift`의 현재 프로젝트 고정 버전을 공식 문서와 호환성에 맞춰 추가하고 AppEnvironment에 `SupabaseService` protocol과 구현을 주입한다. URL과 publishable/anon 키는 안전한 xcconfig 또는 런타임 설정에서 읽으며 Git에 실제 값이 들어가지 않게 한다. service_role 키는 어떤 클라이언트 코드·로그·테스트 fixture에도 넣지 마라.

사용자 인증 세션의 복원·만료·로그아웃을 명확히 처리하고, 네트워크 미설정 상태에서도 로컬 기능은 그대로 동작해야 한다. 기기마다 UUID `device_id`를 Keychain에 저장하고 `device_name`, platform, last_seen을 등록한다. 앱 재설치와 Keychain 잔존 정책, 테스트 시 안정적인 ID 주입을 ADR에 기록한다. 네트워크 오류, 인증 오류, 서버 오류를 구분해 상위 상태 모델로 전달하고 원고 본문이나 토큰을 로그에 남기지 않는다.

설정 없음, 잘못된 설정, 세션 만료, 오프라인, Keychain 재실행, 기기 이름 변경을 테스트하라. 아직 documents 직접 쓰기나 동기화 엔진을 구현하지 말고 연결 계층의 테스트 결과와 보안 점검을 보고하라.
### 여기까지 ###

### 지금부터 ###
31단계 프롬프트 내용

Supabase v2 스키마를 재실행 가능한 SQL migration으로 구현하라. 적용 전 전체 백업 절차를 문서화하고 `documents`, `document_versions`, `edit_leases`, `devices` 및 프로젝트 소유권에 필요한 테이블·인덱스·외래키·제약을 만든다. 기획서 필드인 UUID, owner_id, relative_path, title, content, content_hash, revision BIGINT, metadata_revision, updated_at, updated_by_device, deleted_at, version의 UNIQUE operation_id를 정확히 반영하라.

공개 스키마의 모든 테이블에 RLS를 활성화하고 인증된 본인 프로젝트 행만 읽고 쓸 수 있게 정책을 작성한다. lease와 devices도 타 사용자 정보가 노출되지 않아야 한다. 클라이언트가 보낸 owner_id나 updated_at을 신뢰하지 않고 서버에서 auth.uid와 소유권을 재검증한다. 필요한 함수 권한은 최소화하고 클라이언트의 documents 직접 update 권한은 전환 기간 전략에 맞춰 제한할 준비를 한다.

로컬 Supabase 또는 격리된 테스트 프로젝트에서 두 사용자로 읽기·쓰기·교차 접근 거부, 익명 미인증 거부, tombstone 조회, 제약 위반을 자동 검증하라. 운영 DB에 임의 적용하지 말고 migration, rollback/forward-only 전략, 백업 확인 절차와 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
32단계 프롬프트 내용

원자적 서버 저장 RPC `commit_document`를 SQL migration과 통합 테스트로 구현하라. 입력은 document_id, base_revision, content, content_hash, operation_id, device_id이며 인증 사용자의 owner_id와 문서 소유권을 서버에서 검증한다. 트랜잭션 안에서 operation_id 기존 처리 여부 확인, 대상 행 잠금, 서버 revision 비교, 불변 document_versions 삽입, documents 본문·해시·revision·서버 updated_at·device 갱신, 결과 반환을 원자적으로 수행한다.

base와 서버 revision이 같을 때만 정확히 +1 revision으로 성공한다. 다르면 어떤 본문도 덮어쓰지 않고 conflict와 서버 revision을 반환한다. 서버 저장은 성공했지만 응답이 유실되어 같은 operation_id가 다시 오면 새 버전을 만들지 않고 최초 성공과 동등한 결과를 반환해야 한다. 같은 operation_id를 다른 payload로 재사용하는 경우는 거부하거나 명시적으로 안전 처리한다. content_hash는 서버에서 검증 가능한 범위를 정의한다.

base 15/server 15, base 15/server 17, 동시 두 커밋, 응답 유실 재시도, operation ID payload 불일치, 타 사용자, 삭제 문서, 잘못된 해시를 테스트하라. documents 직접 update 없이 RPC만으로 성공하는지 증명하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
33단계 프롬프트 내용

편집 lease와 Realtime 알림의 서버·iPad 계층을 구현하라. 문서를 열면 약 90초 lease를 등록하고 편집 중 30초마다 heartbeat, 정상 문서 이동·종료 시 해제하며 앱 중단 시 자연 만료되게 한다. session_id로 같은 기기의 오래된 세션과 새 세션을 구분하고 서버 시각을 사용한다. lease는 충돌 예방 안내일 뿐 오프라인까지 막는 영구 락으로 만들지 마라.

다른 기기 lease가 있으면 기기 이름과 함께 `읽기 전용으로 열기`, `다른 화 열기`, `그래도 편집하기` 선택을 제공할 수 있는 상태를 반환한다. Realtime은 저장, 이름 변경, 이동, 삭제, 편집 시작·종료 알림을 비공개 Broadcast 채널 우선으로 전달하되 메시지 본문을 최종 원고로 신뢰하지 않는다. 알림 수신 후 권한 있는 서버 revision을 다시 조회해 판단한다. 앱 background에서 heartbeat를 무리하게 유지하지 않는다.

만료, heartbeat 유실, 강제 종료, 두 세션, 오프라인 강행 편집, 위조 채널 접근, 중복·순서 뒤바뀐 알림을 테스트하라. 배터리와 개인정보 영향을 점검하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
34단계 프롬프트 내용

SwiftData 또는 별도 SQLite를 사용해 iPad 영구 동기화 큐를 구현하라. 기존 스키마 결정과 성능을 검토해 하나를 선택하고 ADR에 근거를 남긴다. 작업에는 operation_id, document_id, operation_type, base_revision, content_hash, 필요한 payload 또는 안전한 payload 참조, created_at, retry_count, next_attempt_at, last_error를 보존한다. 원고 본문 중복 저장 원칙과 재시작 후 전송 요구가 충돌하지 않도록 큐 payload의 스냅샷 저장 위치·보호 방식을 명확히 설계하라.

같은 문서 작업은 생성 순서대로 보내고 다른 문서는 제한된 병렬 처리가 가능하다. 앱 강제 종료 후 큐가 남고 네트워크 복구 시 지수 백오프와 최대 대기시간으로 자동 재시도하며 사용자가 즉시 재시도할 수 있게 한다. 성공 응답을 받으면 먼저 로컬 revision을 내구성 있게 갱신한 뒤 큐에서 제거한다. 중간 종료 시 operation_id로 안전하게 재개한다. 실패 작업을 조용히 버리지 마라.

재실행, 같은 문서 순서, 다른 문서 병렬, 서버 성공 후 응답 유실, revision 갱신 직후 종료, backoff, 수동 재시도를 가상 시계와 가짜 서버로 테스트하라. 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
35단계 프롬프트 내용

iPad `SyncEngine` actor를 구현해 자동 저장과 영구 큐, commit RPC를 연결하라. 로컬 TXT 저장은 800ms 정책을 유지하고 서버 업로드는 마지막 입력 후 2~3초 무입력, 문서·프로젝트 이동, background, Command+S에서 시도한다. 키 입력마다 서버에 업로드하지 않는다. 네트워크가 없어도 로컬 저장 성공과 큐 등록이 완료되면 계속 집필할 수 있어야 한다.

문서를 열 때 로컬 TXT를 즉시 표시하고, 로컬 미처리 작업 확인, 서버 최신 revision 조회, 로컬 변경이 없고 서버가 최신일 때 다운로드, 로컬 변경이 있으면 기준 revision으로 병합 판단, lease 등록, 상태 배지 갱신 순서를 구현한다. 서버 확인 때문에 빈 로딩 화면을 보여주지 말고 `로컬 원고 표시 중 · 서버 확인 중`에서도 입력을 로컬에 보존한다. 다운로드도 원자 저장과 백업을 거쳐야 하며 최신 편집을 덮지 않도록 generation을 확인한다.

가짜 서버로 정상 업로드, 오프라인, 지연 응답, 빠른 전환, background 시간 제한, 서버 최신 다운로드, 로컬 dirty 동시 입력, 큐 재시작을 테스트하라. 상태 전이와 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
36단계 프롬프트 내용

순수 Swift `ThreeWayMergeService`를 구현하라. Base는 편집 시작 당시 공통 원고, Local은 현재 기기 수정본, Remote는 서버 최신본이다. 줄·문단 경계를 고려해 서로 다른 범위의 수정은 결정론적으로 자동 병합하고, 같은 줄 또는 겹치는 범위의 다른 수정은 자동 선택하지 않고 구조화된 conflict hunk로 반환한다. 줄바꿈 형식과 최종 개행, 한글·이모지를 보존하라.

병합에 필요한 Base revision/version을 서버 `document_versions`와 로컬 메타데이터에서 안전하게 찾는 API를 구현한다. 자동 병합 성공 후에는 Remote revision을 새 base로 같은 로컬 변경 의도를 새 operation_id로 commit하고, 다시 충돌하면 무한 반복하지 말고 사용자 확인으로 전환한다. 어떤 경우에도 Local, Remote, Base 원문을 버리지 않는다. 외부 diff 라이브러리를 사용한다면 라이선스·유지보수·성능 근거를 ADR에 기록한다.

서로 다른 문단, 같은 문장, 삽입/삭제 교차, 빈 파일, 줄바꿈만 변경, 큰 6,000자 문서, 재충돌을 fixture 기반 테스트하라. 속성 기반 또는 fuzz 테스트로 병합 결과에 양쪽 비충돌 변경이 남는지 검증하고 보고하라.
### 여기까지 ###

### 지금부터 ###
37단계 프롬프트 내용

충돌 원본 보존과 해결 화면을 구현하라. 충돌이 감지되는 즉시 `백업/충돌`에 `001화_Windows_날짜.txt`, `001화_iPad_날짜.txt`, `001화_Base_날짜.txt`와 충돌 메타데이터를 원자적으로 저장한다. 기기 이름이 다르거나 파일명이 충돌해도 덮어쓰지 않는 고유 이름을 사용하고, 백업 성공 전에는 해결을 완료 처리하지 마라.

화면에는 기준본, Windows 수정본, iPad 수정본, 자동 병합 가능한 부분과 충돌 범위를 비교 가능하게 표시한다. 선택은 자동 병합본 사용, Windows본 사용, iPad본 사용, 직접 편집, 양쪽 별도 파일 보관이다. 어느 선택에서도 반대쪽 원문과 충돌 백업을 삭제하지 않는다. 선택 결과는 현재 Remote revision 기준의 새 commit으로 저장하며 그 사이 서버가 바뀌면 다시 검증한다. VoiceOver와 큰 원고에서 사용할 수 있는 UI를 만든다.

각 선택, 직접 편집, 백업 쓰기 실패, 해결 중 새 remote revision, 앱 종료·재개, 같은 이름 충돌을 테스트하라. 충돌을 단순 마지막 저장 우선으로 숨기는 코드가 없는지 감사하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
38단계 프롬프트 내용

이름 변경·이동·삭제와 새 권 생성의 동기화를 구현하라. 본문 revision과 별도로 metadata_revision을 비교하며 document_id는 모든 경로 변경 뒤에도 유지한다. 같은 문서를 두 기기에서 다른 이름 또는 위치로 바꾸면 한쪽을 조용히 덮지 말고 선택 가능한 metadata conflict를 만든다. 서버 행 삭제 대신 deleted_at tombstone을 사용한다.

다른 기기에서 삭제한 문서를 로컬에서 수정했다면 버리거나 자동 삭제하지 말고 복원 여부를 묻는다. 복원 선택은 tombstone을 해제하고 새 revision으로 되살리며 반대쪽 원문을 보존한다. 새 권의 25개 문서는 각각 UUID를 가지며 로컬 전체 성공 뒤 서버 일괄 작업으로 보낸다. 서버에 일부만 반영된 응답 유실 상황에서도 batch/operation idempotency로 안전하게 재시도하고 중복 문서를 만들지 않는다.

본문 수정+이름 변경, 양쪽 다른 이름, 이동+삭제, 삭제+오프라인 수정, 복원, batch 중간 실패·재시도, Realtime 순서 역전을 통합 테스트하라. RLS와 RPC가 직접 테이블 덮어쓰기를 허용하지 않는지 확인하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
39단계 프롬프트 내용

Windows 앱의 v2 동기화 전환을 구현하기 전에 읽기 전용 감사를 수행하라. 기존 `sync_manager.py`와 프로젝트·집필 저장, 백업, UUID, Supabase 직접 update, 메모리 큐, 충돌 처리, 스레딩, 종료 저장 경로를 추적한다. 사용자 변경과 실제 원고, 운영 Supabase를 수정하지 마라.

현재 v1 데이터 흐름을 코드 위치와 함께 문서화하고 기획서 요구와의 차이를 목록화하라: 상대 경로 문서 UUID, base/revision, commit RPC, SQLite 영구 큐, operation_id, lease/heartbeat, Realtime, 3방향 병합, 충돌 백업, metadata revision, tombstone. iPad와 공유해야 할 프로토콜 계약을 언어 독립 JSON fixture와 SQL 함수 결과 형태로 정의한다. 구형 앱이 직접 덮어쓰는 위험과 단계적 cutover/rollback, 전체 Supabase 백업, 사용자 원고 백업 계획을 작성한다.

아직 Windows 동기화 동작을 바꾸지 말고, 수정 대상 파일, 테스트 추가 지점, 데이터 마이그레이션 위험, 운영 전환 순서, 사용자 결정이 필요한 항목을 `Docs/WindowsSyncV2Plan.md`에 보고하라. 비밀값과 원고 본문은 문서에 넣지 마라.
### 여기까지 ###

### 지금부터 ###
40단계 프롬프트 내용

39단계 계획에 따라 Windows 기존 문서에 안정적인 UUID, revision, metadata_revision 메타데이터를 도입하고 SQLite 영구 동기화 큐를 구현하라. 기존 상대 경로 기반 문서를 처음 스캔할 때 UUID를 한 번 부여하고 이름 변경·이동·휴지통 후에도 유지한다. 기존 사용자가 업그레이드해도 원고 파일 형식과 경로 호환성을 깨지 마라.

큐 필드와 의미는 iPad 34단계와 동일하게 하고 같은 문서 순서, operation_id 멱등성, 지수 backoff, 앱 강제 종료 후 재개, 성공 후 revision 저장 뒤 제거를 보장한다. Python UI 스레드나 편집 입력을 네트워크·SQLite 작업으로 막지 않는다. 기존 메모리 큐는 안전한 전환 후 제거하거나 영구 큐의 얇은 인터페이스로 대체한다. 마이그레이션 전 자동 백업과 실패 rollback을 구현한다.

복제한 테스트 작품에서 최초 UUID 부여, 재실행 ID 유지, 경로 변경, 큐 재시작, DB 손상·잠금, 기존 설정 마이그레이션을 테스트하라. 운영 원고와 운영 DB는 사용하지 말고 변경 파일·호환성·테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
41단계 프롬프트 내용

Windows 저장을 v2 `commit_document` RPC로 전환하고 edit lease·Realtime 알림을 구현하라. documents 직접 update 코드를 제거하거나 전환 플래그 아래 완전히 차단하고, 로컬 TXT 저장을 항상 서버보다 먼저 완료한 뒤 영구 큐로 업로드한다. base_revision과 operation_id를 보존해 응답 유실 재시도가 새 버전을 두 번 만들지 않게 한다.

문서를 열면 90초 lease, 편집 중 30초 heartbeat, 이동·정상 종료 해제를 적용하고 오프라인에서는 안내 후 로컬 편집을 허용한다. 다른 기기 편집 안내와 읽기 전용/다른 화/강행 선택을 기존 UI에 자연스럽게 연결한다. Realtime 알림은 서버 revision 재조회 트리거로만 쓰고 메시지 본문을 최종 원고로 저장하지 않는다. 네트워크 작업은 UI와 자동 로컬 저장을 방해하지 않아야 한다.

RPC 성공·conflict, 응답 유실, 순서 역전, 오프라인, 서버 중단·복구, lease 만료, 위조 알림, 강제 종료를 가짜 또는 격리 서버로 테스트하라. 기존 Windows 저장 기능 회귀 테스트와 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
42단계 프롬프트 내용

Windows에 iPad와 동일한 3방향 병합, 충돌 백업, metadata revision, tombstone 처리를 구현하라. Base·Local Windows·Remote iPad의 프로토콜 의미와 병합 fixture가 Swift 구현과 완전히 같아야 한다. 서로 다른 문단 변경은 자동 병합 후 Remote revision 기준으로 재커밋하고, 겹친 변경은 어느 쪽도 선택하지 않은 채 사용자 충돌 화면으로 보낸다.

충돌 순간 Windows·iPad·Base UTF-8 TXT를 `백업/충돌`에 덮어쓰기 없이 저장한다. 자동 병합본, Windows본, iPad본, 직접 편집, 양쪽 보관 선택을 제공하고 반대 원본을 지우지 않는다. 이름 변경·이동은 document_id 유지와 metadata_revision 충돌을 사용하며, 삭제는 deleted_at tombstone으로 처리한다. 삭제된 문서를 로컬에서 수정한 경우 복원 여부를 묻고 새 revision으로 되살린다. UI 스레드에서 큰 diff를 계산하지 마라.

iPad와 공유하는 JSON fixture로 모든 병합·metadata·삭제 시나리오를 양쪽 테스트 스위트에서 실행하라. Windows 기존 백업·휴지통 회귀와 강제 종료 후 충돌 화면 재개도 검증하고 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
43단계 프롬프트 내용

기존 Supabase 데이터를 v2로 옮기는 migration과 전환 도구를 구현하라. 먼저 운영 데이터 전체 백업과 복원 가능성 확인을 필수 preflight로 만들고, 기존 documents를 새 스키마로 복사하면서 프로젝트 소유권, UUID, revision 초기값, metadata_revision, content_hash, 첫 document_version을 결정론적으로 부여한다. 원문과 행 수, 해시가 일치하지 않으면 전환을 중단한다.

dry-run 보고서, 실제 실행, 재실행 멱등성, 중간 실패 재개 또는 안전 rollback, 구형 Windows 클라이언트 탐지 전략을 제공한다. 전환 순서는 v2 테이블·RPC → 데이터 복사 → Windows v2 전환 → Windows 단독 안정성 확인 → iPad 연결 → 모든 기기 확인 → documents 직접 수정 권한 차단이다. 운영 DB 적용은 사용자의 명시적 승인 없이 실행하지 마라. 구형 앱이 새 revision을 우회해 덮어쓸 수 있는 기간을 최소화하고 유지보수 창 안내를 작성하라.

익명화된 복제 DB에서 정상·중복 경로·잘못된 UTF-8·중간 실패·재실행·rollback을 연습하고 전후 행 수·해시·RLS·RPC 결과를 비교하라. 실행 runbook과 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
44단계 프롬프트 내용

Windows와 iPad의 동기화 계약 적합성 테스트를 완성하라. 언어 독립 fixture를 기준으로 UUID, SHA-256 인코딩, UTF-8·줄바꿈, revision, metadata_revision, operation_id, RPC 성공·conflict 응답, tombstone, lease, Realtime 알림, 3방향 병합 결과가 양쪽에서 동일한지 검증한다. 기기 로컬 시각은 최신 판단에 사용하지 않고 서버 revision과 서버 시각만 사용한다.

계약 테스트는 격리 Supabase를 시작해 두 클라이언트 또는 테스트 하네스로 실행할 수 있어야 하며 운영 계정·원고를 사용하지 않는다. 서로 다른 줄바꿈, 한글 정규화, 이모지, 빈 본문, 6,000자 본문, 1,000화 초기 동기화를 포함한다. 한쪽 변경이 계약을 깨면 CI가 실패하도록 한다. 서버 migration 버전과 최소 Windows/iPad 앱 버전 호환 표를 유지한다.

설정 화면의 마지막 서버 시각, 대기 작업 수, 수동 재시도, 전체 새로고침, 충돌 폴더 열기를 실제 SyncEngine 상태에 연결한다. 전체 새로고침도 dirty 로컬 문서를 덮지 않게 한다. 계약 차이와 테스트 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
45단계 프롬프트 내용

실제 교차기기 동기화 수용 시나리오를 자동화하고 가능한 범위에서 Windows PC와 실제 iPad로 실행하라. 최소 시나리오는 동시 저장, 양쪽 오프라인에서 같은 문서 편집, 서로 다른 문장 자동 병합, 같은 문장 충돌 화면, 서버 성공 후 응답 유실, 같은 operation_id 두 번, 작업 도착 순서 역전, 기기 시간 큰 불일치, 이름 변경+본문 수정, 삭제+본문 수정, iPad background, Windows 강제 종료, 서버 중단·복구, 1,000화 초기 동기화다.

각 시나리오는 시작 데이터, 작업 타임라인, 예상 revision·queue·백업·상태 배지, 최종 세 원문 해시를 선언하고 반복 실행 가능하게 한다. 네트워크 차단과 응답 유실은 통제된 프록시 또는 가짜 transport로 재현하고 운영 서버를 교란하지 않는다. 실패해도 Local·Remote·Base 중 어떤 원문도 사라지지 않았는지를 최우선 assertion으로 둔다.

자동화 불가능한 실제 기기 단계는 사람이 그대로 따라 할 체크리스트와 증거 캡처 위치를 제공한다. 실패를 재현·수정·재시험하고, 미검증 시나리오를 통과로 표시하지 마라. 결과 행렬을 보고하라.
### 여기까지 ###

### 지금부터 ###
46단계 프롬프트 내용

저장·백업·동기화에 대한 장애 주입과 복구 훈련을 수행하라. 저장 공간 부족, 권한 상실, 원자 교체 실패, SwiftData/SQLite 잠금·손상, 앱 강제 종료, iPad background 제한, 서버 401/403/409/429/500, 타임아웃, DNS 불가, Realtime 중복·유실·역전, 기기 시각 변경을 재현한다. 실제 사용자 원고나 운영 DB를 대상으로 하지 않는다.

모든 경우에 마지막 확인된 로컬 TXT, 최신 편집 메모리 스냅샷, 백업, 동기화 큐, 서버 버전 중 복구 가능한 원본이 최소 하나 이상 남고 조용한 덮어쓰기가 없어야 한다. 오류 메시지는 사용자가 할 수 있는 행동을 제시하되 기술 세부나 비밀정보·본문을 로그로 노출하지 않는다. 앱 재시작 시 임시 파일, 미완료 저널, 큐, 충돌 세션을 검사해 안전하게 재개한다.

발견한 취약점을 수정하고 재시험하라. 각 장애의 예상 상태 배지, 자동 복구 여부, 사용자 조치, 보존된 원본 위치를 `Docs/RecoveryMatrix.md`에 기록하고 잔여 위험을 보고하라.
### 여기까지 ###

### 지금부터 ###
47단계 프롬프트 내용

실제 iPad 사용 환경 UI·입력 검증을 수행하고 발견한 문제를 수정하라. iPadOS 17 지원 기기에서 가로·세로, 1/3·1/2·2/3 Split View, Stage Manager, 화면 잠금·해제, 앱 전환, 소프트웨어 한글 키보드, Magic Keyboard 또는 Bluetooth 키보드, 길게 누르기, 가능한 포인터 우클릭을 점검한다.

빠른 화 전환 직후 첫 한글, 조합 중 전환, 듀얼 편집기 양쪽 입력, Command 단축키, 검색 강조, 타자기 스크롤, 바인더 드래그, sheet, fileImporter/fileExporter, 공유 시트를 실제 작업 흐름으로 반복한다. compact 폭에서는 편집 영역을 지키고 듀얼 모드 제한 안내가 자연스러워야 한다. VoiceOver, 충분한 명암, 색상 외 상태 표시, Reduce Motion, 큰 글자에서 핵심 기능을 점검한다.

실제 기기가 없으면 검증 스크립트와 미완료 표시만 만들고 완료로 선언하지 마라. 기기 모델·OS·키보드·화면 모드별 결과, 재현 영상 또는 스크린샷 경로, 수정 내용을 보고하라.
### 여기까지 ###

### 지금부터 ###
48단계 프롬프트 내용

최종 성능과 메모리 최적화를 실제 iPad 릴리스 빌드에서 수행하라. 29단계와 동일한 1,000화·보조 문서 500개·백업 5,000개 데이터로 비교해 프로젝트 진입, 화 전환, 검색, 추출, 초기 동기화, 충돌 diff, 장시간 입력, background 복귀를 측정한다. 30분 이상 연속 집필하면서 자동 저장과 서버 업로드 중 프레임 정지·키 유실·메모리 증가를 확인하라.

Instruments로 Main Thread, Time Profiler, Allocations, Leaks, 파일 I/O를 조사하고 수치로 확인된 병목만 고친다. 최상위 즉시 표시와 하위 지연 로딩, 변경 부모 캐시만 무효화 원칙을 유지한다. 대용량 본문·검색 결과·백업 원문을 불필요하게 동시에 메모리에 보유하지 말고 actor hop과 과도한 해시 재계산을 점검한다. 최적화가 데이터 안전성이나 IME를 약화시키면 되돌린다.

변경 전후 median/p95, 메모리 최고치, 목표 달성 여부, 미달 이유를 문서화하고 성능 회귀 테스트 임계치를 현실적으로 설정하라. 결과를 보고하라.
### 여기까지 ###

### 지금부터 ###
49단계 프롬프트 내용

출시 전 보안·개인정보·데이터 무결성 감사를 수행하라. Git 기록과 현재 파일에서 Supabase service_role, 토큰, 개인 원고, 백업, `.env`, 프로비저닝 비밀이 추적되지 않는지 검사하되 발견한 비밀값 자체를 출력하지 마라. iPad에는 publishable/anon 키만 존재하고 RLS가 모든 공개 테이블에 적용되며 RPC가 auth.uid와 owner_id를 재확인하는지 테스트한다.

경로 탈출, 악성 파일명, security-scoped URL 수명, 로그의 원고 본문·토큰 노출, Keychain 접근성, 네트워크 오류 메시지, Realtime 채널 권한, SQL 함수 search_path와 실행 권한, operation_id 재사용 공격을 점검한다. 클라이언트 updated_at이나 device clock으로 최신을 판단하는 코드, documents 직접 update, 영구 강제 락, 메모리 전용 큐가 남아 있지 않은지 정적 검색과 실행 테스트로 확인한다.

발견 사항을 심각도와 재현 증거로 분류하고 이번 범위의 문제는 수정·재시험하라. 비밀이 이미 Git에 들어간 경우 임의로 기록을 파괴하지 말고 폐기·재발급과 사용자 승인 필요한 정리 절차를 보고하라.
### 여기까지 ###

### 지금부터 ###
50단계 프롬프트 내용

Mac Catalyst 확장 가능성을 검증하라. 1차 제품은 iPadOS이므로 macOS 전용 기능을 새로 만들지 말고 Catalyst 빌드 타깃 또는 빌드 설정을 켜 기존 코드가 컴파일되는지 확인한다. UIKit/TextKit 래퍼, 파일 선택, 공유, contextMenu, 키보드 명령, SwiftData, Supabase, Keychain에서 플랫폼 조건부 코드가 최소화되어 있는지 점검한다.

플랫폼 차이는 `Platform` 계층과 작은 adapter로 격리하고 Domain, Data, Sync, Feature 로직을 Catalyst와 공유한다. iPad 동작을 깨뜨리는 대규모 추상화는 하지 마라. 우클릭과 Command 단축키가 같은 명령 모델을 쓰며 앱 샌드박스 경로와 보안 접근이 Catalyst에서 명확한지 확인한다. Catalyst 전용 미지원 기능은 컴파일 가능한 명시적 안내로 처리한다.

iPadOS 전체 테스트를 다시 실행해 회귀가 없음을 확인하고 Catalyst 빌드 결과, 경고, 조건부 코드 목록, 향후 실제 Mac 검증 항목을 보고하라. Catalyst 실행 미검증을 완료로 과장하지 마라.
### 여기까지 ###

### 지금부터 ###
51단계 프롬프트 내용

개인 iPad 직접 설치와 갱신 절차를 완성하라. App Store 등록 없이 Xcode로 본인 iPad에 개발 서명 설치하는 runbook을 작성하고 고정 Bundle ID, Team, 인증서, 프로비저닝 프로파일을 확인한다. 실제 embedded provisioning profile의 `ExpirationDate`를 읽어 기록하고 만료 전 캘린더 알림을 만들 수 있는 체크리스트를 제공한다. 무료 Personal Team의 짧은 만료와 유료 프로그램 프로파일의 실제 만료일을 추측하지 말고 설치 산출물에서 확인한다.

갱신 전 전체 작품 TXT 내보내기와 Supabase 상태·대기 큐 확인을 필수로 하고, 앱을 삭제하지 않은 채 같은 Bundle ID로 기존 앱 위에 설치해 Application Support·SwiftData·Keychain·설정이 보존되는지 테스트 작품으로 확인한다. Bundle ID 변경, 앱 삭제, iPad 초기화의 데이터 위험을 명확히 경고한다. 설치 실패 시 원고가 어디에 남는지 복구 절차를 작성한다.

실제 iPad 설치·재서명·덮어 설치 결과와 프로파일 만료일 기록 위치를 보고하되 인증서나 개인 식별 비밀은 노출하지 마라.
### 여기까지 ###

### 지금부터 ###
52단계 프롬프트 내용

릴리스 후보에 대해 전체 자동 테스트와 정적 검사를 깨끗한 환경에서 실행하라. Swift 단위·통합·UI 테스트, Windows 테스트, SQL migration/RLS/RPC 테스트, 양쪽 계약 테스트, 교차기기 시나리오, 성능 회귀를 포함한다. 테스트가 실제 작품 대신 임시 폴더·격리 DB만 사용하는지 확인하고 테스트 실행 후 잔재와 비밀이 남지 않게 한다.

컴파일 경고, concurrency 경고, 크래시 가능 강제 unwrap, 무시된 오류, TODO/FIXME, 500줄 초과 파일, 중복 본문 저장, 메인 스레드 I/O, 불안정한 시간 기반 테스트를 감사한다. AI 관련 코드와 화면이 iPad 타깃에 들어 있지 않고 PDF 추출이나 iPhone 전용 기능처럼 범위 밖 기능이 섞이지 않았는지 확인한다. 사용자 변경을 임의로 되돌리지 말고 이번 단계 변경만 분리한다.

실패는 원인을 수정한 뒤 전체 관련 테스트를 재실행한다. 빌드 환경상 실행하지 못한 테스트를 통과로 쓰지 말고 명령·예상 결과·필요 장비를 남긴다. 릴리스 후보 해시와 변경 요약, 테스트 증거를 보고하라.
### 여기까지 ###

### 지금부터 ###
53단계 프롬프트 내용

실제 운영 전 최종 데이터 복구 리허설을 테스트 데이터로 수행하라. 전체 작품 내보내기, 로컬 Application Support 백업, Supabase 백업, Windows 로컬 원고 백업을 만든 뒤 각각에서 독립적으로 새 테스트 환경에 복원한다. 원고 수, 문서 UUID, 상대 경로, UTF-8 바이트 해시, revision, metadata, 휴지통, 보관 백업, 미처리 큐가 기대대로 보존되는지 비교한다.

앱 삭제를 모사한 로컬 손실, 서버 손실, 한 기기 손실, 손상된 최신 파일, 만료된 서명으로 앱 실행 불가 상황에서 사용자가 TXT 원고를 회수할 수 있는 절차를 검증한다. 복원이 서버의 더 최신 원고를 조용히 덮지 않게 격리 프로젝트 또는 새 project_id를 사용하고, 실제 운영 데이터 삭제나 덮어쓰기는 하지 마라.

비기술 사용자도 따라 할 수 있는 `Docs/BackupRestoreRunbook.md`를 작성하라. 각 복구 경로의 소요 시간, 필요한 자격, 검증 해시, 실패 시 대안을 기록하고 결과와 남은 단일 장애 지점을 보고하라.
### 여기까지 ###

### 지금부터 ###
54단계 프롬프트 내용

통합 기획서의 최종 완료 기준을 한 항목씩 실제 증거에 대조하는 최종 감사를 수행하라. 요구사항 추적표의 모든 ID가 구현 파일, 자동 테스트, 필요한 실제 기기 수동 테스트와 연결되어야 한다. 작품 생성·가져오기·전체 내보내기, 9개 바인더, 새 권 25화, 전체·부분 TXT 추출, 한글 IME와 스마트 규칙, 빠른 화 전환 첫 글자, 로컬 저장 상태, 백업·diff·복원·휴지통, 오프라인 집필, 영구 큐 복구, 공통 UUID/revision/RPC, 오래된 저장 차단, 자동 병합, 겹친 충돌 원본 보존, 1,000화 성능, Catalyst 구조를 모두 확인하라.

미완료 항목을 문서만으로 완료 처리하거나 테스트를 삭제·완화해 통과시키지 마라. 발견한 범위 내 결함은 수정하고 관련 회귀 테스트를 실행한다. 외부 장비·계정·운영 승인 때문에 남는 항목은 책임자, 정확한 실행 절차, 위험, 출시 차단 여부를 명시한다. 사용자 원고와 비밀정보가 Git·로그·테스트 산출물에 없는지 마지막으로 확인한다.

최종 보고서는 완료, 부분 완료, 미완료, 검증 불가로 구분하고 근거를 링크하라. 테스트가 모두 통과한 변경만 단계별로 커밋하며 다른 사용자 변경을 함께 stage하지 마라. 완료 기준이 충족된 경우에만 개발 완료라고 선언하라.
### 여기까지 ###
