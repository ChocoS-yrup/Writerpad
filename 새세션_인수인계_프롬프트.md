# 새 세션 프롬프트 — iPad 계약 클라이언트 (8단계 이어서)

아래 블록을 새 창에 그대로 붙여 넣으면 된다.

---

```text
WriterPad iPad 저장소에서 서버 워크플로 8단계를 이어서 진행한다.
작업 폴더: /Users/chocos/Documents/작가님 힘내세요
브랜치: feat/sync-v2-structure-recovery (origin/main 기준 16커밋 앞)

## 먼저 읽을 것

1. sync-contract/contract-lock.json 의 두 값
   contract_version=0.2.0, canonical_contract_sha256=416c1b99…
   세 번째 pin 인 contract_git_commit=fcd99b7098b9a04bd93c585d89b16588aa482530 은
   lock 파일에 없다. 커밋이 자기 SHA 를 담을 수 없어 인수인계 문서에만 있다.
2. WriterPad/Sync/SyncV2Contract.swift — 이번에 만든 계약 코어
3. WriterPadTests/SyncV2VectorHarness.swift — 벡터 실행 하네스
4. Windows 참조: https://github.com/ChocoS-yrup/WriterPad_main
   브랜치 codex/windows-post-stage8-stabilization
   docs/ipad-contract-client-handoff.md, sync_contract.py, sync_v2_store.py

## 지금까지 한 것 (전부 커밋됨, 작업 트리 깨끗)

전체 테스트 714건 중 713 통과, 실패 0, 건너뜀 1(실기기 전용).

계약 코어 (a473df4)
  WriterPad/Sync/SyncV2Contract.swift 신규.
  pin 3값, 클라이언트/서버 능력 각 8개, storage-name-v1(벡터 15개 통과),
  RFC 8785 부분집합 정규 JSON, batch 메타데이터 8필드,
  fail-closed 호환성 검사 4종, document_commit / atomic_structure_commit
  요청 빌더, 응답 검증, 사건 기반 상태 파생.
  기대 다이제스트는 Windows sync_contract.py 를 실제로 돌려 받아 박았다.
  아직 아무 데서도 호출하지 않는다.

벡터 하네스 (c919539, a0a8ba4)
  sync-contract/test_vectors/ 원본을 사본 없이 읽는다.
  기계 판정 가능한 값만 자동 대조하고 영어 문장 assertion 은 그대로 나른다.
  틀린 구현을 일부러 넣어 하네스가 잡아내는지 확인하는 시험이 있다.
  벡터 07(재시작 복구), 11(취소) 이 운영 저장소에서 통과한다.

사건 기반 상태 (0dc11ba ~ ae09db6)
  스키마 V5: sync_operation_events 표. Scripts/fixtures/SyncV2StoreSchemaV5.sql
  status 칸을 고쳐 쓰던 자리 18곳을 모두 사건 남기기로 전환했다.
  (원래 SET status 검색으로 15곳을 셌는데 INSERT 3곳을 놓쳤다. 새로 만드는
   곳은 고쳐 쓰지 않으므로 그 그물에 안 걸린다.)
  operationStatus 는 이제 칸이 아니라 사건에서 계산해 답한다.
  cancelOperation 을 계약대로 넣었다(멱등, 종료 상태 거절).
  어긋남을 찾는 operationStateDivergences() 가 있다. 장부 한 줄이 어긋나도
  저장소가 열리는 것을 막지 않는다. 닫아걸면 사용자가 동기화를 통째로 잃는다.

고아화 복구 (b93633f, 5c11b18)
  연쇄 편집의 뒤쪽은 base_revision 없이 큐에 들어가고 앞선 작업이 완료될 때
  값을 받는다. 앞이 완료 아닌 길로 끝나면 영영 발송 대상에서 빠진다.
  발송 조건에 base_revision IS NOT NULL 이 있기 때문이다.
  문서 줄과 폴더 줄 모두 되세운다. 줄의 맨 앞만 되세우고 그다음은 순서를
  지킨다. 취소하는 자리와 저장소 여는 자리에서 각각 돈다.

## 확인된 사실 (다시 조사하지 마라)

계약
  LEGACY + protocol 3 이 허용이다. 작품 승격 없이 계약 경로를 쓸 수 있고,
  스테이징 실서버에서 committed 로 확인됐다(Docs/stage-7-staging-functional-test-2026-08-11.md).
  minimum_write_protocol: LEGACY 1/1, MIGRATING·ID_BASED 3/3.

Unicode
  계약은 storage-name 을 Unicode 15.0.0 으로 고정하라고 하지만 iPad 런타임은
  17.0 이고 Swift 에 판 번호 API 가 없다. 다만 정규화·케이스폴딩 안정성 정책
  때문에 실제로 갈릴 수 있는 스칼라는 92개뿐이다(전 코드포인트 스캔으로 확정,
  SyncV2StorageName.divergentScalars 에 있다). 벡터 15개는 그대로 통과한다.
  casefold 는 반드시 String.folding(.caseInsensitive) 를 써라.
  lowercased() 는 ß 를 ss 로 접지 못해 SN-007 이 깨진다.
  판 고정을 fail-closed 로 걸지 여부는 계약 개정 안건이라 아직 막지 않았다.

서버 배포 상태 (Supabase 에 접속하지 않고 저장소 기록으로 확인)
  운영 isotfvmlklrxspusjpcn — 계약 RPC 없음. iPad 가 이걸 가리킨다.
  스테이징 mhpnszcorfzrvhyondxr — RPC 배포됨. 그러나 allowlist 0행이라
    fail-closed 로 CONTRACT_NOT_ALLOWED 로 거부한다.
  Windows — 계약 코드는 있으나 실서버 접촉 0회(mock/offline 만).

## 미해결로 남은 것

A. 되감기가 계약을 어긴다 (b03aca5 에 현재 동작이 시험으로 고정돼 있다)
   rebaseAfterRevisionConflict 가 원본 작업을 그 자리에서 덮어쓴다.
   같은 operation_id 아래에서 payload 와 base_revision 이 바뀌고 superseded
   기록도 안 남는다. 계약(벡터 05)은 원본을 그대로 두고 새 operation_id +
   batch_id 로 후속을 만들어 supersedes_operation_id 로 원본을 가리키라고 한다.
   서버는 operation_id 로 멱등 응답을 기억하므로, 같은 신원 아래 내용이 바뀌면
   재전송 때 서버가 "이미 했다"고 답하는 그 내용이 지금 보내려던 것과 다를 수
   있다. 사용자가 쓴 글이 조용히 사라진다.
   고칠 때 가장 위험한 부분은 후속 작업이 원래 자리의 줄 순서를 물려받는
   것이다. 잘못하면 편집 순서가 뒤집힌다.
   Windows 도 같은 문제를 가질 수 있으니 확인해서 알려라.

B. 마지막 세 이름변경 사건 — 손대지 마라
   보존된 incident 에서 직접 원인이 확인된 뒤에 범위를 정한다.
   incident project: 99516096-9c10-402e-988f-8440c1f7fd76
   진단만 해 두었다. 다음 세 시험이 지금 동작을 고정하고 있다.
     testRapidSixRenamesInOneBatchDiagnostic
       서로 다른 폴더 6개를 한 배치로 바꾸면 깨끗하게 끝난다. 재현 안 됨.
     testSameFolderRenamedSixTimesDiagnostic
       같은 폴더를 6번 잇달아 바꾸다 넷째가 충돌로 막히면 뒤 둘이 대기 중으로
       남고 그 뒤로 아무것도 나가지 않는다. 사건이 말한 모양과 같다.
       넷째가 살아 있는 동안 뒤쪽이 기다리는 것은 옳은 동작이다. 그래서
       다음 질문은 "왜 그 충돌이 안 풀렸나" 다.
     testCancellingBlockedFolderLeaderAdoptsFollowers
       막힌 앞을 취소하면 맨 앞이 풀려 발송된다.

C. RPC 전환 — 서버 세 가지가 정리돼야 가능
   지금 iPad 는 commit_document / commit_folder / ensure_project 를 부른다.
   계약은 document_commit / atomic_structure_commit 을 요구한다.
   선행 조건 (전부 승인이 필요한 일이고 에이전트가 하지 마라)
     1. 스테이징 allowlist 에 digest 416c1b99… protocol 3, 능력 8개 등록
     2. 운영 DB 에 계약 migration 적용
     3. Windows 실서버 검증
   A(되감기)는 C와 함께 하는 것이 맞다. 후속 작업 생성이 계약 요청
   빌더(supersedes_operation_id)와 짝이라 따로 하면 두 번 만들게 된다.

## 지금 할 수 있는 일 (권장 순서)

1. 지금까지 결과를 Windows 인수인계 문서로 정리한다.
   계약 위반 A 1건, 사건 재현 조건, 고친 것 3가지.
   Windows 도 같은 되감기 문제를 가질 수 있어 알려야 한다.
2. 벡터 01, 08 하네스 준비 (구조 쓰기가 필요해 실행은 C와 묶인다)
3. 서버 쪽 정리가 끝나면 C, 그다음 A

## 지키는 규칙

- 운영 서버 migration 실행 금지. 기존 작품 자동 승격 금지.
- Supabase 에 접속하지 마라. 저장소 기록으로 확인해라.
- service role key 를 앱에 넣지 마라.
- B(마지막 세 이름변경 사건)는 원인 확인 전까지 수정 금지.
- 실기기 검증은 iPad Pro M4 11-inch 로만 기록하고 시뮬레이터 결과를
  실기기 검증으로 적지 마라.
- 각 세부 단계가 끝날 때마다 중단하고 보고해라.

## 이 저장소에서 테스트 돌리는 법

xcodebuild 가 컴파일 오류를 안고도 EXIT=0 을 낸다. 종료 코드를 믿지 마라.
전체를 돌릴 때 병렬을 끄지 않으면 시뮬레이터 클론이 부팅 60초를 넘겨
통째로 실패한다. 실행 두 개가 겹치면 로그와 결과 번들을 서로 덮어쓴다.

xcodebuild -project WriterPad.xcodeproj -scheme WriterPad \
  -destination 'platform=iOS Simulator,name=iPad Air 11-inch (M4)' \
  -only-testing:WriterPadTests -parallel-testing-enabled NO \
  -resultBundlePath /tmp/run.xcresult test > /tmp/run.log 2>&1

확인은 이렇게 한다. 로그의 통과 표시를 믿지 말고 결과 번들에서 읽어라.
  grep -cE '\.swift:[0-9]+:[0-9]+: error:' /tmp/run.log     # 0이어야 한다
  xcrun xcresulttool get test-results summary --path /tmp/run.xcresult
  totalTestCount 가 소스의 func test 개수와 맞는지 대조해라.

새 파일은 WriterPad.xcodeproj/project.pbxproj 에 4곳(PBXBuildFile,
PBXFileReference, 그룹 children, Sources 또는 Resources) 등록해야 한다.
등록 안 하면 조용히 컴파일에서 빠진다. plutil -lint 로 확인해라.
```
