// Only the two UI defaults referenced by the unchanged product schema. No repository or app factory.
// This dedicated target does not compile the product's full editor/repository implementation.
enum EditorPane: String { case left, right }
enum SwiftDataMetadataRepository { static let defaultBinderWidth = 320.0 }
